---
phase: 01-catalog-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/mgc_bt/ingest/validation.py
  - src/mgc_bt/ingest/reporting.py
  - src/mgc_bt/ingest/service.py
  - src/mgc_bt/cli.py
  - tests/test_ingest_validation.py
autonomous: true
requirements: [DATA-04, DATA-05, DATA-06]
user_setup: []
must_haves:
  truths:
    - "A completed ingest run reports counts for definitions, bars, and trades."
    - "The ingest output includes loaded date range information for the cataloged MGC data."
    - "Structural ingestion problems fail the command, while quality issues are emitted as warnings."
  artifacts:
    - path: "src/mgc_bt/ingest/validation.py"
      provides: "Structural and quality validation rules"
      min_lines: 60
    - path: "src/mgc_bt/ingest/reporting.py"
      provides: "Human-readable ingest summary output"
      min_lines: 40
    - path: "tests/test_ingest_validation.py"
      provides: "Failure-vs-warning regression coverage"
      contains: "warning"
  key_links:
    - from: "src/mgc_bt/ingest/service.py"
      to: "src/mgc_bt/ingest/validation.py"
      via: "post-write validation pass"
      pattern: "validate"
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/ingest/reporting.py"
      via: "final ingest summary output"
      pattern: "report|format"
    - from: "src/mgc_bt/ingest/validation.py"
      to: "C:/dev/mgc-data/*/condition.json"
      via: "quality warning classification"
      pattern: "degraded|condition"
---

<objective>
Add the validation and reporting layer that makes the ingest command trustworthy for downstream backtesting.

Purpose: Phase 1 is only complete when ingestion can prove what it loaded, what date range it covers, and which issues are fatal versus warning-only.
Output: Validation rules, reporting helpers, CLI-facing summaries, and tests for failures and warnings.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-foundation/01-CONTEXT.md
@.planning/phases/01-catalog-foundation/01-RESEARCH.md
@.planning/phases/01-catalog-foundation/01-02-SUMMARY.md
@C:/dev/mgc-data/ohcl-1m/manifest.json
@C:/dev/mgc-data/Trades/condition.json
@C:/dev/mgc-data/definitions/condition.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement structural validation and quality warning classification</name>
  <files>src/mgc_bt/ingest/validation.py, src/mgc_bt/ingest/service.py, tests/test_ingest_validation.py</files>
  <read_first>.planning/phases/01-catalog-foundation/01-CONTEXT.md, .planning/phases/01-catalog-foundation/01-RESEARCH.md, src/mgc_bt/ingest/service.py</read_first>
  <action>Create validation logic that separates hard failures from warnings. Hard failures must include: missing definition files, zero discovered files for required enabled schemas, failed MGC instrument lookup, and empty catalog results after a supposedly successful write. Warning-only conditions should include degraded dates from Databento `condition.json`, sparse sessions, and optional MBP-1 data being ignored. Add tests that lock these categories in place so later phases do not blur them.</action>
  <verify>uv run pytest tests/test_ingest_validation.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/ingest/validation.py` contains logic for both failures and warnings
    - `src/mgc_bt/ingest/validation.py` contains `degraded`
    - `tests/test_ingest_validation.py` contains cases for structural failure and warning-only behavior
  </acceptance_criteria>
  <done>Validation behavior matches the locked Phase 1 policy of structural strictness plus quality warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Add ingest reporting for counts and date ranges</name>
  <files>src/mgc_bt/ingest/reporting.py, src/mgc_bt/ingest/service.py, src/mgc_bt/cli.py</files>
  <read_first>src/mgc_bt/ingest/service.py, C:/dev/mgc-data/ohcl-1m/metadata.json, C:/dev/mgc-data/ohcl-1m/manifest.json</read_first>
  <action>Implement reporting helpers that emit a clear ingest summary with discovered and written counts for definitions, bars, and trades, plus the loaded date range. Feed this summary back through the CLI in a way that stays readable in PowerShell. The service should return structured result data and the CLI should render it, not print ad hoc strings directly from deep ingest helpers.</action>
  <verify>uv run python -m mgc_bt ingest --config configs/settings.toml</verify>
  <acceptance_criteria>
    - `src/mgc_bt/ingest/reporting.py` contains fields for definitions, bars, and trades
    - `src/mgc_bt/ingest/reporting.py` contains date range formatting
    - `src/mgc_bt/cli.py` calls the reporting layer after ingest execution
  </acceptance_criteria>
  <done>The ingest command surfaces the exact operational outputs required by Phase 1.</done>
</task>

<task type="auto">
  <name>Task 3: Lock in Phase 1 regression coverage</name>
  <files>tests/test_cli.py, tests/test_databento_discovery.py, tests/test_ingest_validation.py</files>
  <read_first>tests/test_cli.py, tests/test_databento_discovery.py, tests/test_ingest_validation.py, .planning/phases/01-catalog-foundation/01-VALIDATION.md</read_first>
  <action>Expand tests so Phase 1 has end-to-end confidence in its own contract: the CLI exposes ingest, discovery honors the exact folder layout and overrides, and validation/reporting distinguish between failure and warning conditions. Keep tests local and deterministic; do not require downloading new data or external services.</action>
  <verify>uv run pytest -q</verify>
  <acceptance_criteria>
    - `tests/test_cli.py` exists and still targets the CLI contract
    - `tests/test_databento_discovery.py` exists and covers exact folder handling
    - `tests/test_ingest_validation.py` exists and covers counts/date-range or warning behavior
  </acceptance_criteria>
  <done>Phase 1 ends with automated regression checks around the ingest contract rather than only manual confidence.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Ingest service results into validation classification
- Validation outcomes into CLI-visible reporting
- Raw Databento quality metadata into warning messages
</trust_boundaries>
<threats>
- threat_id: T-01-05
  category: Repudiation
  component: ingest reporting
  disposition: mitigate
  mitigation_plan: Emit per-schema counts and date range summaries so partial writes cannot masquerade as successful full ingest runs.
- threat_id: T-01-06
  category: Integrity
  component: validation classification
  disposition: mitigate
  mitigation_plan: Separate structural failures from warning-only data-quality findings and regression-test both classes explicitly.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_ingest_validation.py -q` passes
- [ ] `uv run pytest -q` passes
- [ ] The ingest output includes counts, date range, and warnings/failures with distinct behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Ingest reports counts for definitions, bars, and trades
- Ingest exposes catalog date range and quality warnings without downgrading structural failures

</success_criteria>

<output>
After completion, create `.planning/phases/01-catalog-foundation/01-03-SUMMARY.md`
</output>
