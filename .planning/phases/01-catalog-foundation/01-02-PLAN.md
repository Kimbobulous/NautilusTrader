---
phase: 01-catalog-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/mgc_bt/ingest/__init__.py
  - src/mgc_bt/ingest/discovery.py
  - src/mgc_bt/ingest/catalog.py
  - src/mgc_bt/ingest/service.py
  - src/mgc_bt/cli.py
  - tests/test_databento_discovery.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
user_setup: []
must_haves:
  truths:
    - "The ingest command discovers definitions, bars, and trades from the configured Databento root without hardcoded source edits."
    - "Instrument definitions are written to the Nautilus catalog before bars or trades."
    - "The v1 ingest path only targets definitions, 1-minute bars, and trades even if MBP-1 files are present."
  artifacts:
    - path: "src/mgc_bt/ingest/discovery.py"
      provides: "Hybrid Databento file discovery"
      min_lines: 60
    - path: "src/mgc_bt/ingest/service.py"
      provides: "Ordered ingest orchestration"
      min_lines: 80
    - path: "src/mgc_bt/ingest/catalog.py"
      provides: "Nautilus catalog creation and write helpers"
      min_lines: 40
    - path: "tests/test_databento_discovery.py"
      provides: "Discovery and ordering coverage"
      contains: "definition"
  key_links:
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/ingest/service.py"
      via: "ingest subcommand dispatch"
      pattern: "run_ingest|ingest_main"
    - from: "src/mgc_bt/ingest/service.py"
      to: "src/mgc_bt/ingest/catalog.py"
      via: "catalog write orchestration"
      pattern: "write_data|ParquetDataCatalog"
    - from: "src/mgc_bt/ingest/service.py"
      to: "src/mgc_bt/ingest/discovery.py"
      via: "file discovery before decode"
      pattern: "discover"
---

<objective>
Implement schema-aware Databento discovery and the ordered Nautilus catalog ingestion pipeline.

Purpose: This is the core Phase 1 capability that turns the raw local MGC dataset into reusable Nautilus catalog data.
Output: Discovery helpers, catalog utilities, ingest orchestration, CLI wiring, and focused discovery/order tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-foundation/01-CONTEXT.md
@.planning/phases/01-catalog-foundation/01-RESEARCH.md
@.planning/phases/01-catalog-foundation/01-01-SUMMARY.md
@nt_docs/integrations/databento.md
@nt_docs/how_to/data_catalog_databento.py
@nt_docs/api_reference/persistence.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build hybrid Databento discovery for the exact local folder layout</name>
  <files>src/mgc_bt/ingest/__init__.py, src/mgc_bt/ingest/discovery.py, tests/test_databento_discovery.py</files>
  <read_first>configs/settings.toml, .planning/phases/01-catalog-foundation/01-CONTEXT.md, C:/dev/mgc-data/definitions/metadata.json, C:/dev/mgc-data/ohcl-1m/metadata.json, C:/dev/mgc-data/Trades/metadata.json, C:/dev/mgc-data/MBP-1_03.09.2021-11.09.2023/metadata.json</read_first>
  <action>Create an ingest package and implement discovery logic that recognizes the real top-level folders `definitions`, `ohcl-1m`, `Trades`, and `MBP-1_03.09.2021-11.09.2023`. Base matching on actual schema/file patterns confirmed by folder metadata: daily `*.definition.dbn.zst`, daily `*.trades.dbn.zst`, daily `*.mbp-1.dbn.zst`, and a date-range `*.ohlcv-1m.dbn.zst` file in the typo-named `ohcl-1m` directory. The default scan path should locate required schemas while allowing TOML overrides through explicit glob settings. Include MBP-1 discovery metadata in the result object, but do not put MBP-1 on the required write path for Phase 1. Add tests proving the `ohcl-1m` folder typo is handled intentionally and that discovery is driven by actual schema/file suffixes rather than folder names alone.</action>
  <verify>uv run pytest tests/test_databento_discovery.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/ingest/discovery.py` contains `definitions`, `ohcl-1m`, and `Trades`
    - `src/mgc_bt/ingest/discovery.py` contains `mbp-1` or `MBP-1`
    - `tests/test_databento_discovery.py` contains assertions for discovery order or schema grouping
  </acceptance_criteria>
  <done>The project can reliably locate required DBN inputs from the current Windows data root and optional override patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Implement definition-first Nautilus catalog ingestion</name>
  <files>src/mgc_bt/ingest/catalog.py, src/mgc_bt/ingest/service.py</files>
  <read_first>nt_docs/integrations/databento.md, nt_docs/how_to/data_catalog_databento.py, nt_docs/api_reference/adapters/databento.md</read_first>
  <action>Implement catalog helpers around `ParquetDataCatalog` and `DatabentoDataLoader`. In `service.py`, orchestrate discovery, create or open the configured catalog path, decode DEFINITION files first with `as_legacy_cython=False`, write them, resolve the target MGC instrument ids from loaded instruments, then decode and write OHLCV-1M bars and trades. Use `instrument_id` hints where valid to speed decoding, but do not hardcode a guessed instrument id before definitions are available. Fail immediately if no instruments are loaded or the MGC instrument cannot be resolved.</action>
  <verify>uv run pytest tests/test_databento_discovery.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/ingest/service.py` contains `as_legacy_cython=False`
    - `src/mgc_bt/ingest/service.py` contains logic that writes definitions before bars or trades
    - `src/mgc_bt/ingest/catalog.py` contains `ParquetDataCatalog`
  </acceptance_criteria>
  <done>The ingest service follows Nautilus' required order and can write catalog-ready definitions, bars, and trades.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the ingest subcommand to the real service entrypoint</name>
  <files>src/mgc_bt/cli.py, src/mgc_bt/ingest/service.py</files>
  <read_first>src/mgc_bt/cli.py, src/mgc_bt/config.py, src/mgc_bt/ingest/service.py</read_first>
  <action>Replace the Phase 1 placeholder ingest handler with the real service entrypoint. The CLI should load settings, run discovery and ordered ingestion, and return structured result data for later reporting work. Keep `backtest` and `optimize` untouched in this plan so file overlap stays limited to the ingest command path.</action>
  <verify>uv run python -m mgc_bt ingest --config configs/settings.toml --help</verify>
  <acceptance_criteria>
    - `src/mgc_bt/cli.py` imports the ingest service
    - `src/mgc_bt/cli.py` contains a callable ingest execution path
    - `src/mgc_bt/ingest/service.py` exposes a top-level function for CLI use
  </acceptance_criteria>
  <done>The CLI can invoke the real ingest pipeline instead of a stub.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Configured raw-data directories into schema discovery
- Raw DBN files into Nautilus object decoding
- Decoded Nautilus objects into the Parquet catalog
</trust_boundaries>
<threats>
- threat_id: T-01-03
  category: Tampering
  component: raw file discovery
  disposition: mitigate
  mitigation_plan: Restrict required ingestion to expected DBN suffixes and configured schemas, and ignore unsupported files by explicit rule.
- threat_id: T-01-04
  category: Integrity
  component: catalog write order
  disposition: mitigate
  mitigation_plan: Enforce definitions-first writes and fail if MGC instrument resolution does not succeed before market-data decoding.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_databento_discovery.py -q` passes
- [ ] `uv run python -m mgc_bt ingest --config configs/settings.toml --help` succeeds
- [ ] The ingest pipeline uses `as_legacy_cython=False` and definition-first writes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The ingest command can discover and write definitions, bars, and trades into a Nautilus catalog
- MBP-1 is not accidentally promoted into the required Phase 1 write path

</success_criteria>

<output>
After completion, create `.planning/phases/01-catalog-foundation/01-02-SUMMARY.md`
</output>
