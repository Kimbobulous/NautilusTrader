---
phase: 01-catalog-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - configs/settings.toml
  - src/mgc_bt/__init__.py
  - src/mgc_bt/__main__.py
  - src/mgc_bt/cli.py
  - src/mgc_bt/config.py
  - tests/test_cli.py
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]
user_setup: []
must_haves:
  truths:
    - "User can run `python -m mgc_bt ingest` from the project root with the venv active."
    - "Project paths come from `configs/settings.toml` instead of source-code edits."
    - "The project has a real Python package and test entrypoint instead of loose scripts."
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata and uv/pytest execution entry"
      contains: "name = \"mgc-bt\""
    - path: "configs/settings.toml"
      provides: "Stable project configuration"
      contains: "[paths]"
    - path: "src/mgc_bt/cli.py"
      provides: "CLI parser and subcommand dispatch"
      min_lines: 40
    - path: "src/mgc_bt/config.py"
      provides: "TOML-backed settings loader"
      min_lines: 40
  key_links:
    - from: "src/mgc_bt/__main__.py"
      to: "src/mgc_bt/cli.py"
      via: "module entrypoint calling main()"
      pattern: "from mgc_bt\\.cli import main"
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/config.py"
      via: "shared settings loading"
      pattern: "load_settings|Settings"
---

<objective>
Create the Python project skeleton, TOML config contract, and CLI shell that Phase 1 depends on.

Purpose: Give later ingestion work a stable package layout, command surface, and path-loading boundary.
Output: `pyproject.toml`, `configs/settings.toml`, the `mgc_bt` package scaffold, and initial CLI/config tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-foundation/01-CONTEXT.md
@.planning/phases/01-catalog-foundation/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the package and uv-friendly project metadata</name>
  <files>pyproject.toml, src/mgc_bt/__init__.py, src/mgc_bt/__main__.py</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, AGENTS.md</read_first>
  <action>Create `pyproject.toml` for a `src`-layout package named `mgc-bt` targeting Python `>=3.13,<3.14`, with pytest in an optional dev dependency group and package discovery rooted at `src`. Create `src/mgc_bt/__init__.py` with a package version constant and `src/mgc_bt/__main__.py` that imports `main` from `mgc_bt.cli` so `python -m mgc_bt` is the canonical entrypoint.</action>
  <verify>uv run python -m mgc_bt --help</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `name = "mgc-bt"` and `requires-python = ">=3.13,<3.14"`
    - `src/mgc_bt/__main__.py` contains `from mgc_bt.cli import main`
    - `src/mgc_bt/__init__.py` contains `__version__`
  </acceptance_criteria>
  <done>Project metadata and package entrypoint exist and `python -m mgc_bt --help` resolves through the package.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TOML settings loading and Windows-safe path normalization</name>
  <files>configs/settings.toml, src/mgc_bt/config.py</files>
  <read_first>.planning/phases/01-catalog-foundation/01-CONTEXT.md, .planning/phases/01-catalog-foundation/01-RESEARCH.md, nt_docs/integrations/databento.md</read_first>
  <action>Create `configs/settings.toml` with `[paths]`, `[ingestion]`, `[backtest]`, and `[optimization]` sections from day one. Use forward-slash-safe defaults rooted in the repo, including `data_root = "C:/dev/mgc-data"` and `catalog_root = "catalog"`. Implement `src/mgc_bt/config.py` with typed settings objects loaded via `tomllib`, immediate conversion to `pathlib.Path`, project-root-relative path resolution, and clear validation errors for missing required sections or keys.</action>
  <verify>uv run pytest tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - `configs/settings.toml` contains `[paths]`, `[ingestion]`, `[backtest]`, and `[optimization]`
    - `src/mgc_bt/config.py` contains `tomllib` and `Path`
    - `src/mgc_bt/config.py` contains a callable settings loader such as `load_settings`
  </acceptance_criteria>
  <done>The project has one authoritative settings file and a typed loader that normalizes Windows paths before command execution.</done>
</task>

<task type="auto">
  <name>Task 3: Build the CLI shell and initial tests</name>
  <files>src/mgc_bt/cli.py, tests/test_cli.py</files>
  <read_first>pyproject.toml, configs/settings.toml, src/mgc_bt/config.py</read_first>
  <action>Implement `src/mgc_bt/cli.py` with an `argparse`-based parser exposing `ingest`, `backtest`, and `optimize` subcommands, plus a shared `--config` option defaulting to `configs/settings.toml`. Only wire `ingest` to a callable placeholder for now; `backtest` and `optimize` should produce explicit not-yet-implemented errors rather than silently succeeding. Add `tests/test_cli.py` to verify subcommand registration, config default resolution, and friendly failure when an unknown command or missing config is supplied.</action>
  <verify>uv run pytest tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/cli.py` contains `ingest`, `backtest`, and `optimize`
    - `src/mgc_bt/cli.py` contains `--config`
    - `tests/test_cli.py` contains at least one assertion for parser behavior
  </acceptance_criteria>
  <done>The CLI contract exists and is test-covered before ingestion logic is added.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Local CLI arguments into `mgc_bt` command parsing
- TOML configuration into normalized filesystem paths
- Project-relative writes into the local catalog/results directories
</trust_boundaries>
<threats>
- threat_id: T-01-01
  category: Tampering
  component: config path handling
  disposition: mitigate
  mitigation_plan: Normalize paths through `pathlib.Path`, fail on missing required roots, and avoid unchecked string concatenation.
- threat_id: T-01-02
  category: Denial of Service
  component: CLI error handling
  disposition: mitigate
  mitigation_plan: Return explicit validation errors for missing config and invalid command inputs instead of ambiguous stack traces.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run python -m mgc_bt --help` succeeds
- [ ] `uv run pytest tests/test_cli.py -q` passes
- [ ] `configs/settings.toml` exposes the four locked top-level sections
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo contains a runnable `mgc_bt` package and CLI shell
- Settings are loaded from TOML with Windows-safe path normalization

</success_criteria>

<output>
After completion, create `.planning/phases/01-catalog-foundation/01-01-SUMMARY.md`
</output>
