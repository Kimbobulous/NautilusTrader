---
phase: 09-reusable-strategy-platform
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [configs/settings.toml, src/mgc_bt/config.py, src/mgc_bt/backtest/strategy_registry.py, src/mgc_bt/backtest/configuration.py, src/mgc_bt/cli.py, tests/test_config.py, tests/test_strategy_registry.py, tests/test_cli.py]
autonomous: true
requirements: [PLT-03]
user_setup: []
must_haves:
  truths:
    - "Strategy selection moves from a hardcoded strategy path to config-driven resolution."
    - "The common case stays simple with `strategy = \"mgc_production\"`."
    - "Advanced users can override the registry with an explicit fully qualified strategy class path."
  artifacts:
    - path: "src/mgc_bt/backtest/strategy_registry.py"
      provides: "Named strategy registry and import override resolution"
      contains: "mgc_production"
    - path: "src/mgc_bt/backtest/configuration.py"
      provides: "Backtest config builder wired to resolved strategy metadata"
      contains: "ImportableStrategyConfig"
    - path: "tests/test_strategy_registry.py"
      provides: "Registry and import override coverage"
      contains: "strategy_class"
  key_links:
    - from: "src/mgc_bt/config.py"
      to: "src/mgc_bt/backtest/strategy_registry.py"
      via: "typed TOML strategy selection feeds registry resolution"
      pattern: "strategy|strategy_class"
    - from: "src/mgc_bt/backtest/configuration.py"
      to: "src/mgc_bt/backtest/strategy_registry.py"
      via: "resolved class/config paths flow into ImportableStrategyConfig"
      pattern: "resolve"
---

<objective>
Make strategy execution configurable through a registry-first selection model with optional explicit import override.

Purpose: `PLT-03` requires that future strategies plug into the existing runner without hardcoded code edits.
Output: Typed config support for strategy selection, a reusable registry, and backtest wiring that preserves the current runner contract.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-reusable-strategy-platform/09-CONTEXT.md
@.planning/phases/09-reusable-strategy-platform/09-RESEARCH.md
@src/mgc_bt/config.py
@src/mgc_bt/backtest/configuration.py
@src/mgc_bt/backtest/runner.py
@src/mgc_bt/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend typed settings with registry-first strategy selection</name>
  <files>configs/settings.toml, src/mgc_bt/config.py, tests/test_config.py</files>
  <read_first>configs/settings.toml, src/mgc_bt/config.py, .planning/phases/09-reusable-strategy-platform/09-CONTEXT.md</read_first>
  <action>Add a typed strategy-selection config block to the project TOML loader. The common-case config should support `strategy = "mgc_production"` while an advanced override can set `strategy_class = "package.module:ClassName"` or the equivalent fully qualified path format the project already uses. Validate that conflicting or incomplete combinations fail clearly. Keep the default project settings pinned to `mgc_production` so existing behavior does not change.</action>
  <verify>uv run pytest tests/test_config.py -q -k "strategy or registry"</verify>
  <acceptance_criteria>
    - `configs/settings.toml` declares `mgc_production` as the default strategy
    - `src/mgc_bt/config.py` validates registry-name and import-override inputs
    - invalid strategy-selection combinations fail with clear config errors
  </acceptance_criteria>
  <done>The settings layer can describe which strategy to run without editing runner code.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve strategies through a reusable registry at the backtest configuration seam</name>
  <files>src/mgc_bt/backtest/strategy_registry.py, src/mgc_bt/backtest/configuration.py, tests/test_strategy_registry.py, tests/test_cli.py</files>
  <read_first>src/mgc_bt/backtest/configuration.py, src/mgc_bt/backtest/runner.py, .planning/phases/09-reusable-strategy-platform/09-RESEARCH.md</read_first>
  <action>Create `src/mgc_bt/backtest/strategy_registry.py` with a named registry that maps `mgc_production` to the current strategy and config classes, plus a resolver that honors the optional explicit import override. Refactor `src/mgc_bt/backtest/configuration.py` to build `ImportableStrategyConfig` from the resolved metadata instead of hardcoded paths, while keeping `run_backtest(settings, params) -> dict` unchanged. Add tests proving registry resolution, import overrides, and CLI/backtest usage all flow through the new seam.</action>
  <verify>uv run pytest tests/test_strategy_registry.py tests/test_cli.py -q -k "strategy or registry or backtest"</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/strategy_registry.py` contains a named `mgc_production` entry
    - `src/mgc_bt/backtest/configuration.py` no longer hardcodes the MGC strategy import path
    - the runner contract remains `run_backtest(settings, params)`
    - tests prove a new registered strategy can be selected without editing runner code
  </acceptance_criteria>
  <done>The platform can choose strategies from config while preserving the current backtest execution path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_config.py tests/test_strategy_registry.py tests/test_cli.py -q -k "strategy or registry or backtest"`
- [ ] Default settings still resolve to `mgc_production`
- [ ] Backtest execution still routes through the existing runner/configuration contract
</verification>

<success_criteria>

- All tasks completed
- Strategy choice is config-driven instead of hardcoded
- Registry default and explicit import override both work
- Existing backtest flow remains intact

</success_criteria>

<output>
After completion, create `.planning/phases/09-reusable-strategy-platform/09-02-SUMMARY.md`
</output>
