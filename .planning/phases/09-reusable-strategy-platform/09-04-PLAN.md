---
phase: 09-reusable-strategy-platform
plan: 04
type: execute
wave: 3
depends_on: ["01", "02", "03"]
files_modified: [tests/test_strategy_golden.py, tests/test_compare.py, tests/test_cli.py, USAGE.md, .planning/phases/09-reusable-strategy-platform/09-VERIFICATION.md]
autonomous: true
requirements: [PLT-01, PLT-03, PLT-04]
user_setup: []
must_haves:
  truths:
    - "Phase 9 remains refactor-only and permanently locks current MGC behavior with a golden fixture."
    - "The full suite must stay green alongside the new reusable-platform coverage."
    - "The operator documentation ends with a concrete add-a-strategy workflow."
  artifacts:
    - path: "tests/test_strategy_golden.py"
      provides: "Permanent bounded regression fixture for exact MGC behavior"
      contains: "exact"
    - path: "USAGE.md"
      provides: "Documented pattern for adding and selecting new strategies"
      contains: "mgc_production"
    - path: ".planning/phases/09-reusable-strategy-platform/09-VERIFICATION.md"
      provides: "Phase verification and bounded compare/backtest evidence"
      contains: "golden"
  key_links:
    - from: "tests/test_strategy_golden.py"
      to: "src/mgc_bt/backtest/strategy.py"
      via: "permanent exact output regression guard"
      pattern: "trade_count|total_pnl|sharpe"
    - from: "USAGE.md"
      to: "src/mgc_bt/backtest/strategy_registry.py"
      via: "operator-facing add-a-strategy instructions"
      pattern: "registry"
---

<objective>
Finish the reusable platform with exact behavior-preservation guards and operator documentation that shows how future strategies fit in.

Purpose: Phase 9 is only successful if the refactor preserves MGC outputs exactly and leaves a clear documented pattern for future contributors.
Output: Permanent golden regression coverage, final hardening, and updated operator guidance.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-reusable-strategy-platform/09-CONTEXT.md
@.planning/phases/09-reusable-strategy-platform/09-RESEARCH.md
@src/mgc_bt/backtest/runner.py
@src/mgc_bt/backtest/strategy.py
@USAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the permanent golden fixture for exact MGC regression protection</name>
  <files>tests/test_strategy_golden.py, tests/test_compare.py, tests/test_cli.py</files>
  <read_first>src/mgc_bt/backtest/runner.py, src/mgc_bt/backtest/strategy.py, tests/test_strategy_logic.py, .planning/phases/09-reusable-strategy-platform/09-CONTEXT.md</read_first>
  <action>Create `tests/test_strategy_golden.py` using a bounded deterministic MGC backtest window and lock the exact expected trade count, total PnL, and Sharpe produced by the pre-refactor strategy. Keep the fixture lightweight enough for normal pytest runs. Extend surrounding tests only as needed so the reusable-platform refactor remains protected from accidental behavior drift.</action>
  <verify>uv run pytest tests/test_strategy_golden.py tests/test_strategy_logic.py -q</verify>
  <acceptance_criteria>
    - `tests/test_strategy_golden.py` asserts exact trade count, total PnL, and Sharpe
    - the golden fixture uses a bounded deterministic window
    - the golden test becomes part of the normal suite instead of an ad hoc verification script
  </acceptance_criteria>
  <done>The current MGC strategy behavior is permanently locked against accidental refactor drift.</done>
</task>

<task type="auto">
  <name>Task 2: Document the add-a-strategy workflow and close out Phase 9 verification</name>
  <files>USAGE.md, .planning/phases/09-reusable-strategy-platform/09-VERIFICATION.md</files>
  <read_first>USAGE.md, .planning/phases/09-reusable-strategy-platform/09-CONTEXT.md, src/mgc_bt/backtest/strategy_registry.py, src/mgc_bt/cli.py</read_first>
  <action>Update `USAGE.md` with a concrete add-a-strategy pattern that shows how to define a new strategy, register it, select it in config, optionally override it by import path, and compare it against `mgc_production`. After implementation, capture the full Phase 9 verification evidence in `09-VERIFICATION.md`, including the golden fixture result, full-suite pass, and a bounded comparison smoke result.</action>
  <verify>uv run pytest -q</verify>
  <acceptance_criteria>
    - `USAGE.md` explains how to add and register a new strategy without editing runner or CLI code
    - documentation shows both named-registry and import-override selection paths
    - phase verification records the golden regression and compare smoke results
    - the full suite passes with the new Phase 9 coverage included
  </acceptance_criteria>
  <done>The reusable platform is documented, verified, and protected against regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q`
- [ ] The new golden fixture passes with exact metrics
- [ ] A bounded compare run succeeds and produces the expected comparison artifacts
- [ ] `USAGE.md` documents how to add, select, and compare a new strategy
</verification>

<success_criteria>

- All tasks completed
- Exact MGC behavior is preserved and locked by test
- Future strategies have a documented extension path
- Phase 9 closes with full-suite confidence

</success_criteria>

<output>
After completion, create `.planning/phases/09-reusable-strategy-platform/09-04-SUMMARY.md`
</output>
