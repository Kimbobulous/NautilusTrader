---
phase: 09-reusable-strategy-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/mgc_bt/backtest/strategy_base.py, src/mgc_bt/backtest/strategy_primitives.py, src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/indicators/__init__.py, tests/test_strategy_base.py, tests/test_strategy_indicators.py, tests/test_strategy_logic.py]
autonomous: true
requirements: [PLT-01, PLT-02]
user_setup: []
must_haves:
  truths:
    - "Phase 9 introduces a thin reusable strategy base without changing existing MGC signal behavior."
    - "Reusable signal primitives are extracted into standalone modules, but MGC-specific signal-combination logic stays in the MGC strategy."
    - "New shared infrastructure is tested independently from the MGC strategy behavior tests."
    - "A pre-refactor golden output fixture is captured before Task 1 begins and exact bounded backtest outputs must still match it after the refactor."
  artifacts:
    - path: "src/mgc_bt/backtest/strategy_base.py"
      provides: "Thin event-driven reusable strategy base with shared lifecycle and audit plumbing"
      contains: "class BaseResearchStrategy"
    - path: "src/mgc_bt/backtest/strategy_primitives.py"
      provides: "Reusable signal primitives extracted from the MGC strategy"
      contains: "pin_bar"
    - path: "tests/test_strategy_base.py"
      provides: "Independent shared-base coverage"
      contains: "BaseResearchStrategy"
  key_links:
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "src/mgc_bt/backtest/strategy_base.py"
      via: "MGC strategy inherits thin shared base"
      pattern: "BaseResearchStrategy"
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "src/mgc_bt/backtest/strategy_primitives.py"
      via: "MGC strategy consumes extracted candle, delta, absorption, and volume helpers"
      pattern: "pin_bar|absorption|delta|volume"
---

<objective>
Extract the reusable strategy foundation and true signal primitives while preserving the validated MGC strategy behavior exactly.

Purpose: `PLT-01` and `PLT-02` begin with the shared runtime plumbing and standalone indicator/primitive boundaries future strategies can reuse.
Output: A thin shared base, extracted primitives, MGC refactor onto that base, and independent tests that prove the abstraction is reusable without changing behavior.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-reusable-strategy-platform/09-CONTEXT.md
@.planning/phases/09-reusable-strategy-platform/09-RESEARCH.md
@.planning/phases/09-reusable-strategy-platform/09-VALIDATION.md
@nt_docs/concepts/strategies.md
@src/mgc_bt/backtest/strategy.py
@src/mgc_bt/backtest/analytics.py
@src/mgc_bt/backtest/risk.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the thin reusable strategy base and shared audit/risk plumbing</name>
  <files>src/mgc_bt/backtest/strategy_base.py, src/mgc_bt/backtest/strategy.py, tests/test_strategy_base.py</files>
  <read_first>nt_docs/concepts/strategies.md, src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/analytics.py, src/mgc_bt/backtest/risk.py</read_first>
  <action>Create `src/mgc_bt/backtest/strategy_base.py` with a thin `BaseResearchStrategy` that still inherits Nautilus `Strategy` and owns only shared lifecycle hooks, audit-log file handle management, trade metadata capture hooks, and automatic `RiskManager` integration points. Refactor `src/mgc_bt/backtest/strategy.py` so `MgcProductionStrategy` inherits the base while keeping its current signal engine and signal-combination rules unchanged. Add `tests/test_strategy_base.py` to verify the base plumbing independently of MGC-specific logic.</action>
  <verify>uv run pytest tests/test_strategy_base.py tests/test_strategy_logic.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/strategy_base.py` contains `class BaseResearchStrategy`
    - the base owns audit-log lifecycle and risk hook plumbing only
    - `src/mgc_bt/backtest/strategy.py` still contains the MGC-specific signal engine and decision logic
    - `tests/test_strategy_base.py` validates shared behavior without relying on MGC-only assertions
  </acceptance_criteria>
  <done>The project has a reusable thin strategy base and MGC now sits on top of it without changing its edge logic.</done>
</task>

<task type="auto">
  <name>Task 2: Extract reusable signal primitives and indicator exports without moving MGC-specific decision rules</name>
  <files>src/mgc_bt/backtest/strategy_primitives.py, src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/indicators/__init__.py, tests/test_strategy_indicators.py, tests/test_strategy_logic.py</files>
  <read_first>src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/indicators/__init__.py, .planning/phases/09-reusable-strategy-platform/09-CONTEXT.md</read_first>
  <action>Extract standalone reusable primitives for candle patterns, delta accumulation/evaluation, absorption detection, and volume-average helpers into `src/mgc_bt/backtest/strategy_primitives.py` and expose any relevant reusable indicator exports cleanly through `src/mgc_bt/backtest/indicators/__init__.py`. Keep the MGC-specific composition of SuperTrend, VWAP, WaveTrend, delta, absorption, and candle confirmations in the MGC strategy rather than in the shared primitive layer. Expand indicator and strategy tests to lock this boundary.</action>
  <verify>uv run pytest tests/test_strategy_indicators.py tests/test_strategy_logic.py -q</verify>
  <acceptance_criteria>
    - extracted primitive helpers live outside `src/mgc_bt/backtest/strategy.py`
    - candle pattern, delta, absorption, and volume helpers are reusable standalone units
    - MGC-specific signal-combination logic remains in `src/mgc_bt/backtest/strategy.py`
    - tests cover the extracted primitives and preserve existing MGC logic outcomes
  </acceptance_criteria>
  <done>The platform has a real reusable primitive layer without over-abstracting the MGC strategy rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_strategy_base.py tests/test_strategy_indicators.py tests/test_strategy_logic.py -q`
- [ ] `tests/fixtures/mgc_golden_output.json` exists from the pre-refactor bounded backtest capture
- [ ] Re-running `uv run python -m mgc_bt backtest --instrument-id MGCJ1.GLBX --start-date 2021-03-09T00:00:00+00:00 --end-date 2021-03-10T23:59:00+00:00` still matches the golden fixture exactly for `total_pnl`, `total_trades`, `win_rate`, and `max_drawdown`
- [ ] `MgcProductionStrategy` still uses the same signal-combination rules after inheriting the new base
- [ ] Reusable primitives are importable outside the MGC strategy module
</verification>

<success_criteria>

- All tasks completed
- A thin shared base exists for future strategies
- Reusable signal primitives are extracted cleanly
- MGC behavior remains unchanged through the refactor

</success_criteria>

<output>
After completion, create `.planning/phases/09-reusable-strategy-platform/09-01-SUMMARY.md`
</output>
