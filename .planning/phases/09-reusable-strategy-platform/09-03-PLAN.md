---
phase: 09-reusable-strategy-platform
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified: [src/mgc_bt/compare.py, src/mgc_bt/cli.py, src/mgc_bt/reporting/comparison.py, src/mgc_bt/reporting/tearsheet.py, tests/test_compare.py, tests/test_cli.py, tests/test_tearsheet.py]
autonomous: true
requirements: [PLT-04]
user_setup: []
must_haves:
  truths:
    - "Comparison is a dedicated CLI command rather than a flag on `backtest`."
    - "Each strategy still gets its own normal run folder and normal artifact bundle."
    - "The comparison folder is a lightweight orchestration output that reuses existing reporting infrastructure."
  artifacts:
    - path: "src/mgc_bt/compare.py"
      provides: "Dedicated comparison orchestration over two normal strategy runs"
      contains: "comparison_summary.json"
    - path: "src/mgc_bt/reporting/comparison.py"
      provides: "Side-by-side comparison rendering and metric delta helpers"
      contains: "comparison_tearsheet.html"
    - path: "tests/test_compare.py"
      provides: "Comparison workflow coverage"
      contains: "metrics_delta.csv"
  key_links:
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/compare.py"
      via: "new compare command"
      pattern: "compare"
    - from: "src/mgc_bt/reporting/comparison.py"
      to: "src/mgc_bt/reporting/tearsheet.py"
      via: "comparison reuses shared tearsheet/reporting primitives"
      pattern: "tearsheet"
---

<objective>
Add the dedicated strategy comparison workflow without creating a second-class reporting or backtest path.

Purpose: `PLT-04` requires side-by-side strategy evaluation on the same data while preserving the normal artifact bundles for each strategy.
Output: A `compare` command, two ordinary run folders, one comparison folder, and a lightweight comparison tearsheet built on shared reporting assets.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-reusable-strategy-platform/09-CONTEXT.md
@.planning/phases/09-reusable-strategy-platform/09-RESEARCH.md
@src/mgc_bt/cli.py
@src/mgc_bt/backtest/runner.py
@src/mgc_bt/backtest/artifacts.py
@src/mgc_bt/reporting/tearsheet.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the comparison orchestrator on top of two normal backtest runs</name>
  <files>src/mgc_bt/compare.py, src/mgc_bt/cli.py, tests/test_compare.py, tests/test_cli.py</files>
  <read_first>src/mgc_bt/cli.py, src/mgc_bt/backtest/runner.py, src/mgc_bt/backtest/artifacts.py, .planning/phases/09-reusable-strategy-platform/09-CONTEXT.md</read_first>
  <action>Create a dedicated comparison orchestration module and wire a new `compare` CLI command. The command should accept strategy A and strategy B selectors, run each strategy against the same bounded data window through the existing backtest workflow, and persist each strategy's results as a standard backtest run folder. Then create a comparison root under `results/comparisons/<timestamp>/` with `comparison_summary.json` and `metrics_delta.csv` derived from the two normal result bundles.</action>
  <verify>uv run pytest tests/test_compare.py tests/test_cli.py -q -k "compare"</verify>
  <acceptance_criteria>
    - `src/mgc_bt/cli.py` implements a dedicated `compare` command
    - each side of the comparison produces a normal run folder with standard artifacts
    - `results/comparisons/<timestamp>/comparison_summary.json` and `metrics_delta.csv` are created
    - comparison tests verify both normal-run preservation and comparison-folder output
  </acceptance_criteria>
  <done>The platform can compare two strategies without mutating the standard backtest workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse the reporting layer for a lightweight comparison tearsheet</name>
  <files>src/mgc_bt/reporting/comparison.py, src/mgc_bt/reporting/tearsheet.py, tests/test_compare.py, tests/test_tearsheet.py</files>
  <read_first>src/mgc_bt/reporting/tearsheet.py, src/mgc_bt/reporting/loaders.py, .planning/phases/09-reusable-strategy-platform/09-CONTEXT.md</read_first>
  <action>Add a lightweight comparison reporting module that reads the two normal run folders plus the comparison summary and renders `comparison_tearsheet.html` with overlaid equity curves and a side-by-side metrics table. Reuse the existing reporting/tearsheet infrastructure rather than implementing a separate visualization stack. Add tests that confirm the comparison tearsheet is built from filesystem artifacts and that it stays aligned with the two underlying run folders.</action>
  <verify>uv run pytest tests/test_compare.py tests/test_tearsheet.py -q -k "compare or comparison"</verify>
  <acceptance_criteria>
    - `comparison_tearsheet.html` is generated in the comparison folder
    - the comparison renderer reads persisted artifacts from the two normal runs
    - no parallel reporting engine is introduced just for comparison
    - tests assert the comparison tearsheet references both strategies and shared metric deltas
  </acceptance_criteria>
  <done>Strategy comparison produces a coherent side-by-side report while reusing the shipped reporting layer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_compare.py tests/test_cli.py tests/test_tearsheet.py -q -k "compare or comparison"`
- [ ] A bounded compare run creates two normal run folders and one comparison folder
- [ ] `comparison_tearsheet.html` is generated from persisted files rather than rerunning analytics
</verification>

<success_criteria>

- All tasks completed
- Dedicated comparison workflow exists
- Two normal run folders are preserved
- Comparison output is lightweight and reporting-first

</success_criteria>

<output>
After completion, create `.planning/phases/09-reusable-strategy-platform/09-03-SUMMARY.md`
</output>
