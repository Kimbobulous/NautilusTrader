---
phase: 04-optimization-workflow
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/mgc_bt/optimization/results.py
  - src/mgc_bt/optimization/storage.py
  - src/mgc_bt/optimization/study.py
  - src/mgc_bt/cli.py
  - tests/test_optimization_results.py
autonomous: true
requirements: [OPT-02, OPT-03, OPT-05]
user_setup: []
must_haves:
  truths:
    - "Every optimization run writes a canonical timestamped result folder plus refreshes `results/optimization/latest/`."
    - "Ranked trial output includes `objective_score`, raw metrics, and optimized parameter columns."
    - "Failed trials and resumable SQLite study state are both persisted without aborting the overall run."
  artifacts:
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Ranked CSV generation, failed-trial logging, summary persistence, and latest refresh"
      min_lines: 120
    - path: "src/mgc_bt/optimization/storage.py"
      provides: "SQLite storage path helpers and resume-safe study naming behavior"
      min_lines: 60
    - path: "tests/test_optimization_results.py"
      provides: "Optimization persistence and ranking regression coverage"
      contains: "ranked_results.csv"
  key_links:
    - from: "src/mgc_bt/optimization/study.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "persist completed optimization outputs"
      pattern: "ranked_results|failed_trials|optimization_summary"
    - from: "src/mgc_bt/optimization/study.py"
      to: "src/mgc_bt/optimization/storage.py"
      via: "SQLite storage and resume wiring"
      pattern: "sqlite|resume|study_name"
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "final optimization output reporting"
      pattern: "latest|results|summary"
---

<objective>
Persist canonical optimization outputs, ranked results, failed trials, and resumable SQLite study state.

Purpose: Turn the Phase 4 optimization loop into a reproducible research workflow with stable machine-readable outputs and interruption-safe resume behavior.
Output: Ranked CSV persistence, optimization summary JSON, failed-trial logging, SQLite storage helpers, `latest/` refresh, and regression tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-optimization-workflow/04-CONTEXT.md
@.planning/phases/04-optimization-workflow/04-RESEARCH.md
@.planning/phases/04-optimization-workflow/04-01-SUMMARY.md
@src/mgc_bt/backtest/artifacts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist ranked optimization outputs and failed-trial logs</name>
  <files>src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/study.py, tests/test_optimization_results.py</files>
  <read_first>.planning/phases/04-optimization-workflow/04-CONTEXT.md, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/study.py</read_first>
  <action>Create optimization result helpers that write canonical run folders under `results/optimization/YYYY-MM-DD_HHMMSS/` and persist `ranked_results.csv`, `optimization_summary.json`, and `failed_trials.json`. Ensure `ranked_results.csv` includes `rank`, `trial_number`, `objective_score`, `sharpe_ratio`, `total_pnl`, `win_rate`, `max_drawdown_pct`, `total_trades`, and one column per optimized parameter. Apply the locked ranking tie-break of objective score, then Sharpe, then lower drawdown.</action>
  <verify>uv run pytest tests/test_optimization_results.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/optimization/results.py` writes `ranked_results.csv`, `optimization_summary.json`, and `failed_trials.json`
    - ranking logic matches the locked tie-break order
    - failed trials capture both parameters and error details
  </acceptance_criteria>
  <done>The optimization workflow produces stable machine-readable results for every run, including failures.</done>
</task>

<task type="auto">
  <name>Task 2: Add SQLite study persistence and resume-safe orchestration</name>
  <files>src/mgc_bt/optimization/storage.py, src/mgc_bt/optimization/study.py, tests/test_optimization_results.py</files>
  <read_first>.planning/phases/04-optimization-workflow/04-RESEARCH.md, .planning/phases/04-optimization-workflow/04-CONTEXT.md</read_first>
  <action>Implement SQLite storage helpers rooted at `results/optimization/optuna_storage.db` and wire them into study creation so interrupted runs can resume by study name when `--resume` is passed. Preserve the distinction between Optuna study persistence and user-facing artifact folders. Ensure failed trials are marked failed while the study continues instead of crashing the whole optimization session.</action>
  <verify>uv run pytest tests/test_optimization_results.py -q -k "resume or storage or failed"</verify>
  <acceptance_criteria>
    - SQLite storage is rooted at `results/optimization/optuna_storage.db`
    - `--resume` can reconnect to an existing named study
    - failed trials do not abort the overall run
  </acceptance_criteria>
  <done>Optimization runs are interruption-safe and can be resumed without losing prior study state.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh `latest/` and finalize optimization CLI reporting</name>
  <files>src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py, tests/test_optimization_results.py, tests/test_cli.py</files>
  <read_first>src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py, .planning/phases/04-optimization-workflow/04-VALIDATION.md</read_first>
  <action>After each completed optimization run, refresh `results/optimization/latest/` from the canonical timestamped folder, following the same safe persistence pattern used for backtest artifacts. Update the CLI summary so the user sees the canonical run directory, latest directory, seed, study name, completed-trial count, and whether any trials failed. Keep the output concise and explicitly machine-backed rather than text-only reporting.</action>
  <verify>uv run pytest tests/test_optimization_results.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - `results/optimization/latest/` is refreshed after each run
    - CLI output references canonical and latest result locations
    - CLI output includes seed, study name, and completed-trial count
  </acceptance_criteria>
  <done>Optimization output persistence is reproducible, user-friendly, and safe to inspect after interrupted or repeated runs.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Trial summaries into ranked CSV and summary JSON artifacts
- Failed-trial exceptions into persisted error logs
- Optuna study state into SQLite resume storage
</trust_boundaries>
<threats>
- threat_id: T-04-04
  category: Repudiation
  component: optimization results
  disposition: mitigate
  mitigation_plan: Persist ranked machine-readable outputs, seed, study name, and exact trial metrics for auditability and reruns.
- threat_id: T-04-05
  category: Availability
  component: long-running study execution
  disposition: mitigate
  mitigation_plan: Use SQLite-backed study state plus resume-by-name behavior so interruptions do not force full restarts.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_optimization_results.py -q` passes
- [ ] `uv run pytest tests/test_optimization_results.py tests/test_cli.py -q` passes
- [ ] `ranked_results.csv`, `optimization_summary.json`, and `failed_trials.json` are persisted in canonical output folders
- [ ] `results/optimization/latest/` refreshes without replacing the canonical timestamped run
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Completed optimization runs persist stable ranked outputs and failure logs
- Resume behavior works against SQLite study storage by study name

</success_criteria>

<output>
After completion, create `.planning/phases/04-optimization-workflow/04-02-SUMMARY.md`
</output>
