---
phase: 04-optimization-workflow
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/mgc_bt/optimization/export.py
  - src/mgc_bt/optimization/results.py
  - src/mgc_bt/optimization/study.py
  - src/mgc_bt/cli.py
  - tests/test_optimization_results.py
autonomous: true
requirements: [OPT-03, OPT-04, OPT-05]
user_setup: []
must_haves:
  truths:
    - "The best in-sample trial is automatically re-run into a full artifact bundle."
    - "Holdout evaluation is executed only after optimization completes and is clearly labeled as out-of-sample."
    - "The CLI warns when holdout Sharpe materially underperforms in-sample Sharpe."
  artifacts:
    - path: "src/mgc_bt/optimization/export.py"
      provides: "Best-run and holdout export helpers"
      min_lines: 100
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Top-10 light bundle and best-run full bundle persistence"
      min_lines: 140
    - path: "tests/test_optimization_results.py"
      provides: "Best-run, top-10, holdout, and overfitting-warning regression coverage"
      contains: "holdout"
  key_links:
    - from: "src/mgc_bt/optimization/export.py"
      to: "src/mgc_bt/backtest/runner.py"
      via: "best-params rerun and holdout evaluation"
      pattern: "run_backtest"
    - from: "src/mgc_bt/optimization/results.py"
      to: "src/mgc_bt/backtest/artifacts.py"
      via: "reuse and extend backtest artifact persistence for best-run outputs"
      pattern: "summary.json|trades.csv|equity_curve.png|run_config.toml"
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/optimization/export.py"
      via: "post-optimization holdout and warning reporting"
      pattern: "holdout|warning|best_run"
---

<objective>
Export the best optimization result, generate top-10 artifacts, and run a clearly labeled holdout evaluation.

Purpose: Finish Phase 4 with honest post-study evaluation and user-facing artifact bundles that distinguish in-sample optimization from out-of-sample validation.
Output: Best-run full bundle, top-10 light bundles, holdout results and equity curve, overfitting warning, and regression tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-optimization-workflow/04-CONTEXT.md
@.planning/phases/04-optimization-workflow/04-RESEARCH.md
@.planning/phases/04-optimization-workflow/04-02-SUMMARY.md
@src/mgc_bt/backtest/artifacts.py
@src/mgc_bt/backtest/runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the best in-sample configuration into a full best-run bundle</name>
  <files>src/mgc_bt/optimization/export.py, src/mgc_bt/optimization/results.py, tests/test_optimization_results.py</files>
  <read_first>.planning/phases/04-optimization-workflow/04-CONTEXT.md, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/backtest/runner.py</read_first>
  <action>After the study completes, automatically take the best in-sample parameter set and run a full backtest through the shared runner. Persist the best-run bundle under `best_run/` with `summary.json`, `trades.csv`, `equity_curve.png`, and `run_config.toml`, ensuring the run config includes the exact optimized parameters so the result can be reproduced manually.</action>
  <verify>uv run pytest tests/test_optimization_results.py -q -k "best_run"</verify>
  <acceptance_criteria>
    - `best_run/` is created automatically after optimization
    - `best_run/` contains the full required artifact bundle
    - the persisted run config includes the optimization parameters used
  </acceptance_criteria>
  <done>The optimization workflow produces a complete canonical artifact bundle for the best in-sample parameter set.</done>
</task>

<task type="auto">
  <name>Task 2: Export top-10 light bundles and a clearly labeled holdout evaluation</name>
  <files>src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/export.py, tests/test_optimization_results.py</files>
  <read_first>.planning/phases/04-optimization-workflow/04-CONTEXT.md, src/mgc_bt/optimization/results.py</read_first>
  <action>Create light bundles for the remaining top 10 ranked trials with `summary.json` and `equity_curve.png` only, excluding the best run which already has the full bundle. Then rerun the best parameters on the configured holdout window and persist `holdout_results.json` and `holdout_equity_curve.png` with explicit out-of-sample labeling so these artifacts cannot be mistaken for in-sample trial results.</action>
  <verify>uv run pytest tests/test_optimization_results.py -q -k "top_10 or holdout"</verify>
  <acceptance_criteria>
    - `top_10/` contains light bundles for the top-ranked non-best trials
    - holdout artifacts are written with explicit holdout naming
    - holdout execution uses the configured holdout dates rather than in-sample trial dates
  </acceptance_criteria>
  <done>The optimization workflow preserves enough artifact depth for the best and top-ranked runs while keeping holdout evaluation clearly separate.</done>
</task>

<task type="auto">
  <name>Task 3: Surface the overfitting warning and finalize optimization completion reporting</name>
  <files>src/mgc_bt/cli.py, src/mgc_bt/optimization/export.py, tests/test_optimization_results.py, tests/test_cli.py</files>
  <read_first>src/mgc_bt/cli.py, .planning/phases/04-optimization-workflow/04-CONTEXT.md, .planning/phases/04-optimization-workflow/04-VALIDATION.md</read_first>
  <action>Compare the best in-sample Sharpe to the holdout Sharpe after holdout execution completes. If holdout Sharpe is more than `0.3` below in-sample Sharpe, print an explicit overfitting warning in the CLI completion output. Finalize the optimize command summary so it reports ranked output paths, best-run paths, holdout paths, and the key in-sample versus holdout metrics concisely.</action>
  <verify>uv run pytest tests/test_optimization_results.py tests/test_cli.py -q -k "holdout or warning or optimize"</verify>
  <acceptance_criteria>
    - CLI emits the locked overfitting warning when the Sharpe gap exceeds `0.3`
    - CLI output references ranked results, best-run, and holdout artifact locations
    - completion reporting distinguishes in-sample and holdout metrics clearly
  </acceptance_criteria>
  <done>Phase 4 closes with an honest, clearly labeled evaluation story rather than only an in-sample optimization leaderboard.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Ranked optimization outcomes into best-run and top-10 export selection
- Best-params rerun into holdout evaluation outputs
- Holdout-vs-in-sample comparison into user-facing warnings
</trust_boundaries>
<threats>
- threat_id: T-04-06
  category: Integrity
  component: post-study evaluation
  disposition: mitigate
  mitigation_plan: Re-run the best parameters through the shared runner for both best-run export and holdout evaluation instead of trusting trial-side partial outputs alone.
- threat_id: T-04-07
  category: Integrity
  component: overfitting visibility
  disposition: mitigate
  mitigation_plan: Label holdout artifacts explicitly and surface a CLI warning when holdout Sharpe materially underperforms in-sample Sharpe.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_optimization_results.py -q` passes
- [ ] `uv run pytest tests/test_optimization_results.py tests/test_cli.py -q` passes
- [ ] `best_run/` contains the full bundle and `top_10/` contains the light bundles
- [ ] `holdout_results.json` and `holdout_equity_curve.png` are persisted and clearly labeled as holdout outputs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Optimization finishes with best-run, top-10, and holdout artifacts
- Holdout underperformance is surfaced clearly to the user

</success_criteria>

<output>
After completion, create `.planning/phases/04-optimization-workflow/04-03-SUMMARY.md`
</output>
