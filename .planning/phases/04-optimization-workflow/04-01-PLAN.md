---
phase: 04-optimization-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/settings.toml
  - src/mgc_bt/config.py
  - src/mgc_bt/cli.py
  - src/mgc_bt/optimization/__init__.py
  - src/mgc_bt/optimization/objective.py
  - src/mgc_bt/optimization/search_space.py
  - src/mgc_bt/optimization/study.py
  - tests/test_optimize_objective.py
  - tests/test_cli.py
autonomous: true
requirements: [OPT-01, OPT-02, OPT-05]
user_setup: []
must_haves:
  truths:
    - "Optimization trials call the shared `run_backtest(settings, params) -> dict` function instead of launching subprocesses."
    - "The Optuna objective uses Sharpe as the primary score with hard penalties for too few trades and excessive drawdown."
    - "Optimization settings, search-space bounds, seed, stop policy, and holdout dates are validated through the typed TOML config layer."
    - "Phase 4 execution must verify whether the shared runner can reuse a Nautilus backtest engine via `BacktestEngine.reset()` or whether fresh per-call engine creation is acceptably cheap for the catalog-backed workflow."
  artifacts:
    - path: "src/mgc_bt/optimization/objective.py"
      provides: "Objective scoring, penalty logic, and trial-to-runner execution wrapper"
      min_lines: 100
    - path: "src/mgc_bt/optimization/study.py"
      provides: "Optuna study bootstrap, sampler setup, progress reporting, and CLI-facing orchestration"
      min_lines: 120
    - path: "tests/test_optimize_objective.py"
      provides: "Objective, search-space, and early-stop regression coverage"
      contains: "objective_score"
  key_links:
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/optimization/study.py"
      via: "optimize command wrapper"
      pattern: "run_optimization|resume|study"
    - from: "src/mgc_bt/optimization/objective.py"
      to: "src/mgc_bt/backtest/runner.py"
      via: "in-process trial execution"
      pattern: "run_backtest"
    - from: "src/mgc_bt/config.py"
      to: "configs/settings.toml"
      via: "expanded optimization config validation"
      pattern: "in_sample|holdout|seed|max_trials|max_runtime"
---

<objective>
Build the Optuna orchestration foundation: config, search space, objective scoring, progress reporting, and CLI entrypoint.

Purpose: Give Phase 4 a real optimization command that reuses the verified backtest runner and produces deterministic trial scores without yet finalizing all persistence/export work.
Output: Expanded optimization config, search-space helpers, objective logic, Optuna study bootstrap, live progress reporting, and focused tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-optimization-workflow/04-CONTEXT.md
@.planning/phases/04-optimization-workflow/04-RESEARCH.md
@.planning/phases/03-strategy-logic/03-05-SUMMARY.md
@nt_docs/concepts/backtesting.md
@nt_docs/api_reference/backtest.md
@nt_docs/concepts/execution.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand optimization config and validate the locked study contract</name>
  <files>configs/settings.toml, src/mgc_bt/config.py</files>
  <read_first>.planning/phases/04-optimization-workflow/04-CONTEXT.md, configs/settings.toml, src/mgc_bt/config.py</read_first>
  <action>Expand the `[optimization]` section to include the locked Phase 4 settings: seed, max trials, max runtime, early-stop window and minimum improvement, in-sample and holdout date windows, and any stable storage/result subdirectory keys needed for implementation. Validate these through the typed config loader so malformed windows or missing runtime knobs fail early. Keep the config shape TOML-native and compatible with the existing project settings pattern.</action>
  <verify>uv run pytest tests/test_config.py -q -k "optimization"</verify>
  <acceptance_criteria>
    - `configs/settings.toml` contains the new `[optimization]` keys for runtime, seed, and date windows
    - `src/mgc_bt/config.py` validates and exposes the expanded optimization settings
    - the config model distinguishes in-sample and holdout windows clearly
  </acceptance_criteria>
  <done>The project can express the full locked optimization contract through TOML and typed settings.</done>
</task>

<task type="auto">
  <name>Task 2: Implement search-space sampling and objective scoring around `run_backtest(...)`</name>
  <files>src/mgc_bt/optimization/__init__.py, src/mgc_bt/optimization/search_space.py, src/mgc_bt/optimization/objective.py, tests/test_optimize_objective.py</files>
  <read_first>.planning/phases/04-optimization-workflow/04-RESEARCH.md, src/mgc_bt/backtest/runner.py, .planning/phases/03-strategy-logic/03-CONTEXT.md, nt_docs/concepts/backtesting.md</read_first>
  <action>Create optimization helpers that sample the locked strategy and custom-risk parameter ranges using Optuna, then execute each trial by calling `run_backtest(settings, params)` in process. Before finalizing the wrapper, inspect `src/mgc_bt/backtest/runner.py` and verify whether it creates a fresh backtest engine on every call or can reuse one. If it creates a fresh engine each call, evaluate the `BacktestEngine.reset()` pattern documented in `nt_docs/concepts/backtesting.md` and determine whether refactoring the shared runner for engine reuse across Optuna trials would meaningfully reduce per-trial overhead on the catalog-backed 5-year MGC workflow. If engine creation is already cheap enough because the catalog-backed path avoids full reload cost, keep the simpler per-call path and document that decision clearly in `04-01-SUMMARY.md`. Implement the objective so it returns `-10` when `total_trades < 30` or `max_drawdown_pct > 25`, otherwise returning the Sharpe ratio. Keep native Nautilus `RiskEngineConfig` limits out of the search space and preserve the exact optimization-facing parameter names already locked in Phase 3.</action>
  <verify>uv run pytest tests/test_optimize_objective.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/optimization/objective.py` calls `run_backtest`
    - the objective applies the two hard-penalty rules exactly
    - the search space includes strategy parameters plus the approved custom risk parameters only
    - `04-01-SUMMARY.md` records whether the shared runner uses engine reuse with `BacktestEngine.reset()` or why fresh per-call engine creation was retained
  </acceptance_criteria>
  <done>The project can score Optuna trials deterministically through the shared backtest runner.</done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap the Optuna study and wire the CLI `optimize` command</name>
  <files>src/mgc_bt/cli.py, src/mgc_bt/optimization/study.py, tests/test_cli.py, tests/test_optimize_objective.py</files>
  <read_first>src/mgc_bt/cli.py, .planning/phases/04-optimization-workflow/04-CONTEXT.md, nt_docs/concepts/backtesting.md</read_first>
  <action>Install Optuna through the project's `uv` workflow if it is still missing, then create a study-bootstrap layer that configures the TPE sampler with the locked seed, enforces max-trial and max-runtime limits, exposes early stopping based on stalled best-score improvement, and prints live progress summaries with current trial, best Sharpe, best params, and ETA. Add `optimize` CLI support including `--resume` and any bounded run overrides needed for testing, while keeping the CLI as a thin wrapper over the shared optimization orchestration layer.</action>
  <verify>uv run pytest tests/test_optimize_objective.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/cli.py` implements the `optimize` command instead of raising a placeholder error
    - `src/mgc_bt/optimization/study.py` configures a TPE sampler with the configured seed
    - progress output includes current trial, best Sharpe, best params, and ETA
  </acceptance_criteria>
  <done>The user can start a real optimization run through the CLI, and the study contract matches the locked Phase 4 decisions.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- TOML optimization settings into study bootstrap and date-window selection
- Optuna trial suggestions into the shared backtest runner
- CLI arguments into resume and runtime-control behavior
</trust_boundaries>
<threats>
- threat_id: T-04-01
  category: Integrity
  component: trial execution path
  disposition: mitigate
  mitigation_plan: Route every trial through `run_backtest(...)` so optimization cannot drift from the verified backtest workflow.
- threat_id: T-04-02
  category: Integrity
  component: objective scoring
  disposition: mitigate
  mitigation_plan: Lock the two hard-penalty rules in focused tests so degenerate low-trade or high-drawdown trials cannot rank as best.
- threat_id: T-04-03
  category: Availability
  component: local optimization runtime
  disposition: mitigate
  mitigation_plan: Validate runtime caps, seed, and early-stopping controls up front and expose bounded CLI-driven study execution.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_optimize_objective.py -q` passes
- [ ] `uv run pytest tests/test_optimize_objective.py tests/test_cli.py -q` passes
- [ ] The `optimize` CLI command uses shared orchestration rather than subprocess backtest calls
- [ ] The objective score returns `-10` for `< 30` trades or `> 25%` drawdown
- [ ] The Phase 4 summary records the engine-reuse decision for `run_backtest(...)`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The project can launch a real Optuna study through the shared in-process runner
- Optimization config, search-space bounds, and objective scoring match the locked Phase 4 decisions

</success_criteria>

<output>
After completion, create `.planning/phases/04-optimization-workflow/04-01-SUMMARY.md`
</output>
