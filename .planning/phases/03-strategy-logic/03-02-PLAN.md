---
phase: 03-strategy-logic
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/mgc_bt/backtest/strategy.py
  - src/mgc_bt/backtest/state.py
  - src/mgc_bt/backtest/indicators/rolling_stats.py
  - tests/test_strategy_logic.py
autonomous: true
requirements: [STRAT-01, STRAT-02, STRAT-05]
user_setup: []
must_haves:
  truths:
    - "Trend qualification is a hard binary gate: SuperTrend primary, VWAP confirmation, both must agree."
    - "Pullback arming respects confirmed fractal pivots and minimum pullback bars."
    - "Repeated pullback attempts are allowed while trend validity remains intact."
  artifacts:
    - path: "src/mgc_bt/backtest/strategy.py"
      provides: "Trend-gate and pullback state-machine logic"
      min_lines: 200
    - path: "tests/test_strategy_logic.py"
      provides: "Trend and pullback qualification coverage"
      contains: "PULLBACK_ARMED"
  key_links:
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "src/mgc_bt/backtest/indicators/supertrend.py"
      via: "primary trend-direction gate"
      pattern: "supertrend"
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "src/mgc_bt/backtest/indicators/vwap.py"
      via: "secondary directional confirmation"
      pattern: "vwap"
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "src/mgc_bt/backtest/state.py"
      via: "pullback arming and reset transitions"
      pattern: "FLAT|PULLBACK_ARMED"
---

<objective>
Implement trend qualification and pullback arming on completed bars.

Purpose: Turn the raw rolling indicators into a deterministic setup engine that knows when the strategy is allowed to watch for entries.
Output: Binary trend-gate logic, fractal-pivot pullback qualification, repeated-attempt handling, and strategy logic tests around those transitions.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-logic/03-CONTEXT.md
@.planning/phases/03-strategy-logic/03-RESEARCH.md
@.planning/phases/03-strategy-logic/03-VALIDATION.md
@.planning/phases/03-strategy-logic/03-01-PLAN.md
@nt_docs/concepts/strategies.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement binary trend qualification inside the production strategy</name>
  <files>src/mgc_bt/backtest/strategy.py, tests/test_strategy_logic.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-CONTEXT.md, src/mgc_bt/backtest/strategy.py, nt_docs/concepts/strategies.md</read_first>
  <action>Implement the hard binary trend gate in the production strategy. Long setups must require bullish Adaptive SuperTrend and price above VWAP. Short setups must require bearish Adaptive SuperTrend and price below VWAP. Keep this logic explicit and inspectable rather than hidden behind a score or heuristic. Add focused tests that prove the strategy refuses to arm setups when only one of the two directional gates is aligned.</action>
  <verify>uv run pytest tests/test_strategy_logic.py -q -k "trend or readiness"</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/strategy.py` contains explicit dual-gate trend checks for long and short directions
    - tests cover the case where SuperTrend and VWAP disagree
    - no scoring or weighted-combination logic is introduced
  </acceptance_criteria>
  <done>The strategy has a deterministic directional gate that matches the locked Phase 3 rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add fractal pivot tracking and minimum-pullback qualification</name>
  <files>src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/state.py, src/mgc_bt/backtest/indicators/rolling_stats.py, tests/test_strategy_logic.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-CONTEXT.md, .planning/phases/03-strategy-logic/03-RESEARCH.md, src/mgc_bt/backtest/state.py</read_first>
  <action>Implement fractal pivot tracking using the locked 2-bars-on-each-side confirmation rule. Use the most recent confirmed swing low for longs and swing high for shorts when evaluating `min_pullback_bars`. Keep the logic clearly separated from entry triggers so later plans can reuse the qualified setup state without recomputing pivots.</action>
  <verify>uv run pytest tests/test_strategy_logic.py -q -k "pivot or pullback"</verify>
  <acceptance_criteria>
    - pivot confirmation uses the exact 2-left / 2-right fractal rule
    - `min_pullback_bars` is measured from the most recent confirmed direction-appropriate swing
    - strategy logic can distinguish between trend-valid but not-yet-deep-enough pullbacks and truly armed pullbacks
  </acceptance_criteria>
  <done>The strategy can tell when a pullback is structurally valid before entry confirmation is allowed.</done>
</task>

<task type="auto">
  <name>Task 3: Implement repeated pullback attempts and state transitions</name>
  <files>src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/state.py, tests/test_strategy_logic.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-CONTEXT.md, src/mgc_bt/backtest/strategy.py</read_first>
  <action>Wire the state machine so repeated pullback attempts are allowed as long as the active trend gate stays valid. Do not require a fresh trend expansion after a flat exit. Add tests covering FLAT to PULLBACK_ARMED transitions, disarming when the trend gate breaks, and re-arming when a new qualifying pullback appears while the trend remains valid.</action>
  <verify>uv run pytest tests/test_strategy_logic.py -q -k "pullback or state"</verify>
  <acceptance_criteria>
    - FLAT to PULLBACK_ARMED transitions depend on both trend validity and pullback qualification
    - the strategy can re-arm new pullback attempts after a flat exit without requiring an extra trend-expansion phase
    - tests cover disarm/re-arm behavior when trend validity changes
  </acceptance_criteria>
  <done>The strategy has a reliable setup engine that can feed entry logic without ambiguity.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Rolling indicator outputs into binary trend qualification
- Pivot confirmation into pullback-arming decisions
- State transitions into future entry and exit behavior
</trust_boundaries>
<threats>
- threat_id: T-03-03
  category: Integrity
  component: trend gate
  disposition: mitigate
  mitigation_plan: Keep directional gating explicit in code and tests so setups cannot sneak through on partial agreement.
- threat_id: T-03-04
  category: Integrity
  component: pullback qualification
  disposition: mitigate
  mitigation_plan: Separate pivot confirmation from entry triggers and test the exact fractal-pivot and minimum-bar rules using synthetic sequences.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_strategy_logic.py -q -k "trend or pullback or state"` passes
- [ ] The strategy only arms pullbacks when both trend gates agree and pullback depth is satisfied
- [ ] Repeated pullback attempts remain possible while the trend gate stays valid
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Trend qualification and pullback arming are implemented as explicit state-machine behavior
- Tests prove the strategy respects the locked binary gate and fractal-pivot pullback rules

</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-logic/03-02-SUMMARY.md`
</output>
