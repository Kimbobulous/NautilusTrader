---
phase: 03-strategy-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/settings.toml
  - src/mgc_bt/config.py
  - src/mgc_bt/backtest/strategy.py
  - src/mgc_bt/backtest/state.py
  - src/mgc_bt/backtest/indicators/__init__.py
  - src/mgc_bt/backtest/indicators/rolling_stats.py
  - src/mgc_bt/backtest/indicators/atr.py
  - src/mgc_bt/backtest/indicators/vwap.py
  - src/mgc_bt/backtest/indicators/supertrend.py
  - src/mgc_bt/backtest/indicators/wavetrend.py
  - tests/test_strategy_indicators.py
autonomous: true
requirements: [STRAT-01, STRAT-02, STRAT-05]
user_setup: []
must_haves:
  truths:
    - "The production strategy exposes the exact parameter names Phase 4 will optimize."
    - "All custom indicators are pure Python rolling-state classes updated one event at a time."
    - "The strategy has an explicit readiness gate and state model before any signal logic is added."
  artifacts:
    - path: "src/mgc_bt/backtest/strategy.py"
      provides: "Production strategy config and lifecycle skeleton"
      min_lines: 120
    - path: "src/mgc_bt/backtest/indicators/supertrend.py"
      provides: "Adaptive SuperTrend implementation with K-Means-based ATR centroid selection"
      min_lines: 120
    - path: "tests/test_strategy_indicators.py"
      provides: "Synthetic-data verification for the rolling indicator classes"
      contains: "AdaptiveSuperTrend"
  key_links:
    - from: "configs/settings.toml"
      to: "src/mgc_bt/config.py"
      via: "strategy parameter defaults"
      pattern: "supertrend|wavetrend|atr_trail|pullback"
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "src/mgc_bt/backtest/indicators/"
      via: "rolling per-bar indicator composition"
      pattern: "update|is_ready|value"
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "src/mgc_bt/backtest/state.py"
      via: "explicit FLAT/PULLBACK_ARMED/IN_TRADE state tracking"
      pattern: "FLAT|PULLBACK_ARMED|IN_TRADE"
---

<objective>
Build the production strategy config, rolling indicator primitives, and explicit state scaffolding.

Purpose: Establish the exact parameter contract and deterministic event-driven building blocks that every later Phase 3 plan depends on.
Output: Strategy config wired to the locked parameter names, custom rolling indicator classes, state-model helpers, and indicator unit tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-logic/03-CONTEXT.md
@.planning/phases/03-strategy-logic/03-RESEARCH.md
@.planning/phases/03-strategy-logic/03-VALIDATION.md
@nt_docs/concepts/strategies.md
@nt_docs/api_reference/indicators.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend config with production strategy parameters and strategy skeleton</name>
  <files>configs/settings.toml, src/mgc_bt/config.py, src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/state.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-CONTEXT.md, configs/settings.toml, src/mgc_bt/config.py, nt_docs/concepts/strategies.md</read_first>
  <action>Add the locked Phase 3 production-strategy defaults to the TOML and typed settings layer using the exact Phase 4-facing parameter names from CONTEXT.md. Create the production strategy config and lifecycle skeleton in `src/mgc_bt/backtest/strategy.py`, plus a small explicit state-model module if helpful. The strategy skeleton should initialize rolling components, hold an `is_ready` gate, and define the FLAT/PULLBACK_ARMED/IN_TRADE state boundary without yet implementing full signal decisions.</action>
  <verify>uv run pytest tests/test_strategy_indicators.py -q</verify>
  <acceptance_criteria>
    - `configs/settings.toml` contains defaults for the locked strategy parameters
    - `src/mgc_bt/config.py` can load and validate those parameters
    - `src/mgc_bt/backtest/strategy.py` defines a production strategy config using the exact optimization-facing parameter names
    - `src/mgc_bt/backtest/state.py` or equivalent contains explicit state identifiers for FLAT, PULLBACK_ARMED, and IN_TRADE
  </acceptance_criteria>
  <done>The project has a stable production strategy configuration contract and state scaffold.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pure Python rolling indicators and shared utilities</name>
  <files>src/mgc_bt/backtest/indicators/__init__.py, src/mgc_bt/backtest/indicators/rolling_stats.py, src/mgc_bt/backtest/indicators/atr.py, src/mgc_bt/backtest/indicators/vwap.py, src/mgc_bt/backtest/indicators/supertrend.py, src/mgc_bt/backtest/indicators/wavetrend.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-RESEARCH.md, .planning/phases/03-strategy-logic/03-CONTEXT.md, nt_docs/api_reference/indicators.md</read_first>
  <action>Implement the custom rolling indicator classes required by the locked Phase 3 behavior. This should include rolling ATR support, session-reset VWAP, Adaptive SuperTrend with K-Means-trained ATR centroids, WaveTrend/Z-score state, and any reusable rolling-window helpers needed to avoid duplicated logic. Keep every component incremental and event-driven with `update(...)` and current-value accessors rather than vectorized batch computation.</action>
  <verify>uv run pytest tests/test_strategy_indicators.py -q</verify>
  <acceptance_criteria>
    - each custom indicator is implemented as a pure Python rolling-state class
    - no vectorized strategy library such as `pandas_ta` is introduced
    - Adaptive SuperTrend contains the ATR-length, factor, and training-period behavior from CONTEXT.md
    - VWAP supports UTC session resets and typical-price calculation
  </acceptance_criteria>
  <done>The production strategy has reusable, deterministic rolling indicator primitives.</done>
</task>

<task type="auto">
  <name>Task 3: Add indicator-focused synthetic-data tests and readiness coverage</name>
  <files>tests/test_strategy_indicators.py, src/mgc_bt/backtest/strategy.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-VALIDATION.md, .planning/phases/03-strategy-logic/03-CONTEXT.md</read_first>
  <action>Create synthetic-data tests that verify each indicator class produces sensible outputs, warms up correctly, and exposes readiness in a way the strategy can trust. Add at least one test that proves the production strategy does not consider itself ready before the required minimum warm-up window is satisfied.</action>
  <verify>uv run pytest tests/test_strategy_indicators.py -q</verify>
  <acceptance_criteria>
    - `tests/test_strategy_indicators.py` contains coverage for every custom indicator class introduced in this plan
    - at least one test verifies the strategy or indicator readiness gate prior to full warm-up
    - the test file uses synthetic bars rather than depending on the live catalog
  </acceptance_criteria>
  <done>Wave 0 validation exists for the rolling indicator layer before higher-level strategy logic is added.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Strategy configuration values into production signal-state initialization
- Completed bars into rolling indicator state
- Rolling indicator outputs into readiness gating and future signal decisions
</trust_boundaries>
<threats>
- threat_id: T-03-01
  category: Integrity
  component: parameter contract
  disposition: mitigate
  mitigation_plan: Use the exact locked optimization-facing parameter names now so later phases do not silently remap or reinterpret them.
- threat_id: T-03-02
  category: Integrity
  component: indicator computation
  disposition: mitigate
  mitigation_plan: Keep all indicators incremental, explicitly warmed up, and covered by synthetic-data tests rather than hidden vectorized state.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_strategy_indicators.py -q` passes
- [ ] Production strategy config uses the exact locked parameter names from Phase 3 context
- [ ] Indicator code remains pure Python rolling state with no vectorized dependency shortcuts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The codebase has a production strategy config, rolling indicator primitives, and explicit state scaffolding
- Indicator unit tests exist before higher-level signal logic is layered on top

</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-logic/03-01-SUMMARY.md`
</output>
