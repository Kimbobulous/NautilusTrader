---
phase: 03-strategy-logic
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - configs/settings.toml
  - src/mgc_bt/config.py
  - src/mgc_bt/backtest/risk.py
  - src/mgc_bt/backtest/strategy.py
  - src/mgc_bt/backtest/configuration.py
  - src/mgc_bt/backtest/runner.py
  - src/mgc_bt/backtest/artifacts.py
  - tests/test_risk.py
  - tests/test_strategy_logic.py
autonomous: true
requirements: [STRAT-04, STRAT-05]
user_setup: []
must_haves:
  truths:
    - "Native Nautilus RiskEngineConfig handles framework-supported pre-trade validation in the backtest engine."
    - "RiskManager is a standalone class with its own tests and handles only session-level controls Nautilus does not provide natively."
    - "Risk parameters flow through the shared runner so Phase 4 can optimize them without refactoring."
  artifacts:
    - path: "src/mgc_bt/backtest/risk.py"
      provides: "Standalone session-level pre-entry and per-bar risk checks with independent state"
      min_lines: 90
    - path: "tests/test_risk.py"
      provides: "Standalone RiskManager coverage"
      contains: "daily_reset"
  key_links:
    - from: "configs/settings.toml"
      to: "src/mgc_bt/config.py"
      via: "risk parameter loading"
      pattern: "max_daily_trades|max_drawdown_pct"
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "src/mgc_bt/backtest/risk.py"
      via: "entry and in-trade consultation"
      pattern: "can_enter|should_exit"
    - from: "src/mgc_bt/backtest/runner.py"
      to: "src/mgc_bt/backtest/configuration.py"
      via: "risk parameters preserved in shared run path"
      pattern: "max_daily_trades|max_drawdown_pct"
---

<objective>
Add a dedicated risk-management layer to the Phase 3 production strategy.

Purpose: enforce independent pre-entry and in-trade controls without tangling risk bookkeeping into the strategy state machine.
Output: native `RiskEngineConfig` wiring, standalone `RiskManager`, risk config section, strategy integration, and dedicated risk tests.
</objective>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_risk.py tests/test_strategy_logic.py -q` passes
- [ ] `RiskManager` remains a standalone module under `src/mgc_bt/backtest/risk.py`
- [ ] Risk parameters are preserved in settings, runner parameters, and run artifacts for Phase 4 reuse
</verification>
