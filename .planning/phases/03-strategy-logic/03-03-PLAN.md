---
phase: 03-strategy-logic
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/mgc_bt/backtest/strategy.py
  - src/mgc_bt/backtest/state.py
  - tests/test_strategy_logic.py
autonomous: true
requirements: [STRAT-03, STRAT-05]
user_setup: []
must_haves:
  truths:
    - "Entry confirmation requires delta imbalance in trend direction plus above-average volume."
    - "At least one optional confirmation must be present: absorption, candle confirmation, WaveTrend divergence, or WaveTrend Z-score extreme."
    - "Trades drive delta; bars drive absorption and candle confirmation."
  artifacts:
    - path: "src/mgc_bt/backtest/strategy.py"
      provides: "Trade-tick delta accumulation and entry-trigger confirmation logic"
      min_lines: 260
    - path: "tests/test_strategy_logic.py"
      provides: "Entry confirmation and trigger-combination coverage"
      contains: "delta"
  key_links:
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "nt_docs/concepts/backtesting.md"
      via: "trade-tick aggressor-side interpretation"
      pattern: "aggressor_side"
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "src/mgc_bt/backtest/state.py"
      via: "transition from PULLBACK_ARMED to IN_TRADE"
      pattern: "IN_TRADE"
---

<objective>
Implement entry-trigger confirmation from trades, bars, and optional confirmations.

Purpose: Turn an armed pullback into a real entry only when the required core evidence and at least one optional confirmation are both present.
Output: Delta accumulation from trade ticks, absorption and candle confirmation logic, WaveTrend/Z-score optional confirmation wiring, and entry logic tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-logic/03-CONTEXT.md
@.planning/phases/03-strategy-logic/03-RESEARCH.md
@.planning/phases/03-strategy-logic/03-VALIDATION.md
@.planning/phases/03-strategy-logic/03-02-PLAN.md
@nt_docs/concepts/strategies.md
@nt_docs/concepts/backtesting.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accumulate per-bar delta from trade ticks using aggressor side</name>
  <files>src/mgc_bt/backtest/strategy.py, tests/test_strategy_logic.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-CONTEXT.md, .planning/phases/03-strategy-logic/03-RESEARCH.md, nt_docs/concepts/backtesting.md</read_first>
  <action>Implement trade-tick handling that accumulates buy and sell volume per minute bucket using `TradeTick.aggressor_side`. Finalize delta for each completed bar so `on_bar` can evaluate the locked delta imbalance rule without querying the catalog ad hoc. Add tests proving buy- and sell-initiated trades produce the expected signed delta and that the strategy only sees finalized delta on the completed bar boundary.</action>
  <verify>uv run pytest tests/test_strategy_logic.py -q -k "delta or trade"</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/strategy.py` contains per-bar delta accumulation using trade aggressor side
    - absolute delta threshold is evaluated as a fraction of total bar volume
    - tests cover both bullish and bearish delta accumulation paths
  </acceptance_criteria>
  <done>The strategy can compute the required core delta trigger from real trade-tick events.</done>
</task>

<task type="auto">
  <name>Task 2: Implement optional confirmation families from bars and WaveTrend state</name>
  <files>src/mgc_bt/backtest/strategy.py, tests/test_strategy_logic.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-CONTEXT.md, src/mgc_bt/backtest/strategy.py</read_first>
  <action>Implement the locked optional confirmation pool: bars-only absorption, candle confirmations (pin bar, shaved bar, inside-bar breakout), and WaveTrend divergence/Z-score extreme. Keep the candle geometry exact to the CONTEXT.md thresholds and ensure any one optional confirmation can satisfy the optional-confirmation requirement. Add focused tests for each family and at least one inside-bar breakout sequence proving the next-bar confirmation rule.</action>
  <verify>uv run pytest tests/test_strategy_logic.py -q -k "absorption or candle or wavetrend or zscore"</verify>
  <acceptance_criteria>
    - candle confirmation follows the exact pin-bar, shaved-bar, and inside-bar rules from CONTEXT.md
    - absorption uses rolling average volume and range with the locked multipliers and close-location tests
    - either WaveTrend divergence or Z-score extreme can satisfy the optional-confirmation rule
  </acceptance_criteria>
  <done>The strategy can detect the full optional-confirmation set without ambiguity.</done>
</task>

<task type="auto">
  <name>Task 3: Gate entries on core plus optional confirmation and transition into IN_TRADE</name>
  <files>src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/state.py, tests/test_strategy_logic.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-CONTEXT.md, src/mgc_bt/backtest/state.py, src/mgc_bt/backtest/strategy.py</read_first>
  <action>Wire entry logic so a trade is submitted only when the strategy is ready, in `PULLBACK_ARMED`, trend validity still holds, delta imbalance is aligned with bar direction, volume is above average, and at least one optional confirmation is present. Respect the one-position-at-a-time, one-contract, no-pyramiding rules. Add tests proving entries do not fire when core triggers are incomplete or when no optional confirmation is present.</action>
  <verify>uv run pytest tests/test_strategy_logic.py -q -k "entry or trigger or state"</verify>
  <acceptance_criteria>
    - entries require both core triggers and at least one optional confirmation
    - no entry fires while a position is already open
    - same-direction re-entry remains possible only after the prior position is flat and a fresh valid setup forms
  </acceptance_criteria>
  <done>The production strategy can turn qualified pullbacks into compliant entries without breaking the state rules.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Trade ticks into per-bar delta state
- Completed bars into optional confirmation logic
- Setup state into order-submission decisions
</trust_boundaries>
<threats>
- threat_id: T-03-05
  category: Integrity
  component: delta accumulation
  disposition: mitigate
  mitigation_plan: Bucket trades by completed bar, use aggressor-side semantics explicitly, and test the finalized-delta boundary.
- threat_id: T-03-06
  category: Integrity
  component: entry confirmation
  disposition: mitigate
  mitigation_plan: Keep core versus optional trigger rules explicit and test that missing optional confirmation blocks entries even when delta and volume are aligned.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_strategy_logic.py -q -k "delta or entry or candle or absorption or wavetrend"` passes
- [ ] Entry logic requires both core triggers and at least one optional confirmation
- [ ] One-position-at-a-time and no-pyramiding rules remain enforced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The strategy can accumulate delta from trade ticks and combine it with the locked optional confirmation families
- Tests prove entries are gated correctly and cannot bypass missing trigger requirements

</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-logic/03-03-SUMMARY.md`
</output>
