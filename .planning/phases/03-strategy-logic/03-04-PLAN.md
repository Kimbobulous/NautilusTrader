---
phase: 03-strategy-logic
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/mgc_bt/backtest/strategy.py
  - src/mgc_bt/backtest/configuration.py
  - src/mgc_bt/backtest/runner.py
  - tests/test_strategy_logic.py
  - tests/test_backtest_runner.py
autonomous: true
requirements: [STRAT-04, STRAT-05]
user_setup: []
must_haves:
  truths:
    - "Exits occur on ATR trailing stop or hard opposite trend flip, whichever comes first."
    - "The production strategy replaces the harness in the active backtest path."
    - "Phase 2 runner and artifact contracts remain intact after the swap."
  artifacts:
    - path: "src/mgc_bt/backtest/strategy.py"
      provides: "ATR trailing-stop logic and hard opposite-trend-flip exits"
      min_lines: 320
    - path: "src/mgc_bt/backtest/configuration.py"
      provides: "Production strategy wiring into Nautilus run configuration"
      min_lines: 100
    - path: "tests/test_strategy_logic.py"
      provides: "Exit-behavior and end-to-end state-machine coverage"
      contains: "atr"
  key_links:
    - from: "src/mgc_bt/backtest/configuration.py"
      to: "src/mgc_bt/backtest/strategy.py"
      via: "production strategy importable config path"
      pattern: "strategy_path|config_path"
    - from: "src/mgc_bt/backtest/runner.py"
      to: "src/mgc_bt/backtest/configuration.py"
      via: "shared runner remains the single execution path"
      pattern: "build_segment_run_specs"
    - from: "src/mgc_bt/backtest/strategy.py"
      to: ".planning/phases/01-catalog-foundation/01-VERIFICATION.md"
      via: "carry-forward assumptions about catalog-backed data continuity"
      pattern: "legacy|as_legacy_cython"
---

<objective>
Implement ATR trailing exits, hard trend-flip exits, and switch the runner to the production strategy.

Purpose: Finish the Phase 3 production strategy so the existing backtest runner executes real MGC logic end to end.
Output: Exit logic, production strategy wiring into the run configuration, state-machine integration tests, and a bounded runner smoke path with the real strategy.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-logic/03-CONTEXT.md
@.planning/phases/03-strategy-logic/03-RESEARCH.md
@.planning/phases/03-strategy-logic/03-VALIDATION.md
@.planning/phases/02-backtest-runner/02-VERIFICATION.md
@nt_docs/concepts/backtesting.md
@nt_docs/concepts/strategies.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ATR trailing-stop and hard opposite-trend-flip exits</name>
  <files>src/mgc_bt/backtest/strategy.py, tests/test_strategy_logic.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-CONTEXT.md, src/mgc_bt/backtest/strategy.py</read_first>
  <action>Implement ATR trailing-stop behavior for long and short positions using the locked defaults and ratcheting rules. Add the hard opposite-trend-flip exit so positions close when both SuperTrend and VWAP reverse against the trade on the same completed bar. Keep the exit logic inside the strategy rather than changing the Phase 2 venue model. Add tests proving the stop ratchets only in the favorable direction and that a single-gate flip does not force exit.</action>
  <verify>uv run pytest tests/test_strategy_logic.py -q -k "atr or exit or flip"</verify>
  <acceptance_criteria>
    - ATR trailing stops ratchet correctly for long and short positions
    - a hard opposite trend flip requires both SuperTrend and VWAP to reverse against the position
    - tests cover the difference between one-gate and two-gate flip scenarios
  </acceptance_criteria>
  <done>The production strategy can manage and close open risk according to the locked v1 rules.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the harness strategy in the active backtest configuration path</name>
  <files>src/mgc_bt/backtest/configuration.py, src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/runner.py</files>
  <read_first>.planning/phases/02-backtest-runner/02-VERIFICATION.md, .planning/phases/03-strategy-logic/03-CONTEXT.md, src/mgc_bt/backtest/configuration.py</read_first>
  <action>Update the backtest configuration layer so the shared runner uses the production strategy config/class instead of the temporary Phase 2 harness. Preserve the verified Phase 2 next-bar execution model and keep the reusable `run_backtest(...)` contract unchanged. If any code comments reference catalog-backed assumptions, preserve the Phase 1/2 Databento decode split: definitions use legacy Cython decoding and bars/trades use `as_legacy_cython=False`.</action>
  <verify>uv run pytest tests/test_backtest_runner.py tests/test_strategy_logic.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/configuration.py` points at the production strategy import path
    - `run_backtest(...)` remains the single source of truth for backtest execution
    - the catalog continuity note about definitions legacy Cython and bars/trades `as_legacy_cython=False` remains visible wherever relevant
  </acceptance_criteria>
  <done>The Phase 2 runner now executes the real strategy without losing previously verified backtest behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end strategy validation through the shared runner</name>
  <files>tests/test_strategy_logic.py, tests/test_backtest_runner.py</files>
  <read_first>.planning/phases/03-strategy-logic/03-VALIDATION.md, src/mgc_bt/backtest/runner.py, src/mgc_bt/backtest/strategy.py</read_first>
  <action>Add at least one end-to-end test that runs a short controlled sequence through the full strategy state machine and verifies expected state transitions and exit handling. Then extend runner coverage or smoke coverage so the production strategy is exercised through the shared Phase 2 runner path without subprocess duplication. Keep the validation bounded and fast enough for the Phase 3 Nyquist strategy.</action>
  <verify>uv run pytest tests/test_strategy_logic.py tests/test_backtest_runner.py -q</verify>
  <acceptance_criteria>
    - at least one end-to-end strategy test verifies full state-machine progression
    - runner-focused coverage still passes with the production strategy wired in
    - the Phase 2 artifact and runner contracts remain intact after the production strategy swap
  </acceptance_criteria>
  <done>Phase 3 finishes with the real strategy integrated into the existing research workflow and covered by end-to-end tests.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Strategy state into exit and order-submission decisions
- Production strategy wiring into the shared backtest runner path
- Catalog-backed backtest assumptions into the production execution flow
</trust_boundaries>
<threats>
- threat_id: T-03-07
  category: Integrity
  component: exit behavior
  disposition: mitigate
  mitigation_plan: Test ATR ratcheting and opposite-trend-flip exits separately so one-gate reversals cannot accidentally flatten trades.
- threat_id: T-03-08
  category: Maintainability
  component: production strategy integration
  disposition: mitigate
  mitigation_plan: Replace the harness through the existing configuration path and preserve the shared runner contract rather than adding a parallel execution route.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_strategy_logic.py tests/test_backtest_runner.py -q` passes
- [ ] The production strategy is the active strategy used by the shared runner
- [ ] ATR exits and hard opposite-trend-flip exits behave exactly as locked in CONTEXT.md
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The production strategy fully replaces the harness in the active backtest path
- Exit logic and end-to-end runner behavior are covered by tests

</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-logic/03-04-SUMMARY.md`
</output>
