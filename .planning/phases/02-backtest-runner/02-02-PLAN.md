---
phase: 02-backtest-runner
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/mgc_bt/cli.py
  - src/mgc_bt/backtest/runner.py
  - src/mgc_bt/backtest/results.py
  - src/mgc_bt/backtest/strategy_stub.py
  - tests/test_backtest_runner.py
autonomous: true
requirements: [BT-01, BT-05, BT-06]
user_setup: []
must_haves:
  truths:
    - "The core backtest runner is callable from Python as `run_backtest(config, params) -> dict`."
    - "The CLI `backtest` command uses the same core runner instead of maintaining a separate execution path."
    - "A completed run returns structured summary metrics and a trade-log representation."
  artifacts:
    - path: "src/mgc_bt/backtest/runner.py"
      provides: "Reusable Python backtest entrypoint"
      min_lines: 100
    - path: "src/mgc_bt/backtest/results.py"
      provides: "Metric extraction and trade-log shaping"
      min_lines: 80
    - path: "tests/test_backtest_runner.py"
      provides: "Runner API and CLI integration coverage"
      contains: "run_backtest"
  key_links:
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/backtest/runner.py"
      via: "CLI backtest wrapper"
      pattern: "run_backtest"
    - from: "src/mgc_bt/backtest/runner.py"
      to: "src/mgc_bt/backtest/results.py"
      via: "summary extraction after engine execution"
      pattern: "summary|trade"
    - from: "src/mgc_bt/backtest/runner.py"
      to: "src/mgc_bt/backtest/strategy_stub.py"
      via: "temporary executable strategy boundary before Phase 3"
      pattern: "Strategy"
---

<objective>
Implement the reusable Python backtest runner and structured result extraction layer.

Purpose: Make the backtest callable from code for Phase 4 while keeping the CLI as a thin wrapper over the same execution path.
Output: `run_backtest(config, params) -> dict`, summary/trade extraction helpers, CLI wiring, and focused runner tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backtest-runner/02-CONTEXT.md
@.planning/phases/02-backtest-runner/02-RESEARCH.md
@.planning/phases/02-backtest-runner/02-01-SUMMARY.md
@nt_docs/concepts/backtesting.md
@nt_docs/concepts/strategies.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the reusable `run_backtest` Python API</name>
  <files>src/mgc_bt/backtest/runner.py, src/mgc_bt/backtest/results.py</files>
  <read_first>.planning/phases/02-backtest-runner/02-CONTEXT.md, src/mgc_bt/config.py, src/mgc_bt/backtest/configuration.py</read_first>
  <action>Create `src/mgc_bt/backtest/runner.py` with a top-level `run_backtest(config, params) -> dict` function that builds the Nautilus run configuration, executes the backtest, and returns structured summary data. Keep the return value centered on Python data structures rather than formatted text so Phase 4 Optuna trials can consume it directly. In `results.py`, add extraction helpers for summary metrics, instrument identifiers, run dates, parameters, and a normalized trade-log representation.</action>
  <verify>uv run pytest tests/test_backtest_runner.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/runner.py` contains `run_backtest`
    - `src/mgc_bt/backtest/runner.py` returns a dictionary-like summary payload
    - `src/mgc_bt/backtest/results.py` extracts summary metrics including total PnL, Sharpe, win rate, max drawdown, and total trades
  </acceptance_criteria>
  <done>The project has a programmatic backtest API suitable for future optimization loops.</done>
</task>

<task type="auto">
  <name>Task 2: Keep Phase 2 executable without absorbing Phase 3 strategy logic</name>
  <files>src/mgc_bt/backtest/strategy_stub.py, src/mgc_bt/backtest/runner.py</files>
  <read_first>.planning/phases/02-backtest-runner/02-RESEARCH.md, nt_docs/concepts/strategies.md</read_first>
  <action>If the runner needs a concrete strategy to execute, add a minimal Phase 2 strategy harness or stub that satisfies Nautilus `Strategy` wiring without implementing the full production logic. Make its temporary role explicit so Phase 3 can replace or extend it cleanly. Keep completed-bar handling aligned with the locked next-bar execution assumption.</action>
  <verify>uv run pytest tests/test_backtest_runner.py -q</verify>
  <acceptance_criteria>
    - any temporary strategy file clearly indicates it is a runner-enablement stub or harness
    - full trend/pullback/trigger/exit logic is not implemented in this plan
    - the runner remains centered on reusable execution plumbing rather than production signal logic
  </acceptance_criteria>
  <done>Phase 2 proves the runner path without prematurely collapsing into full strategy implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the CLI `backtest` command to the reusable runner</name>
  <files>src/mgc_bt/cli.py, src/mgc_bt/backtest/runner.py, tests/test_backtest_runner.py</files>
  <read_first>src/mgc_bt/cli.py, src/mgc_bt/backtest/runner.py</read_first>
  <action>Replace the current `backtest` stub with a thin CLI wrapper that loads settings, forwards optional CLI parameters such as `--instrument-id`, calls `run_backtest(...)`, and prints a concise summary. Add tests proving the CLI calls the shared runner path instead of maintaining a separate execution branch.</action>
  <verify>uv run pytest tests/test_backtest_runner.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/cli.py` imports and calls `run_backtest`
    - `tests/test_backtest_runner.py` contains CLI-to-runner wiring assertions
    - the CLI path does not compute metrics independently of the runner
  </acceptance_criteria>
  <done>The shell command and Python API share one source of truth for backtest execution.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- CLI arguments into the reusable Python runner
- Nautilus backtest results into structured metric extraction
- Temporary strategy wiring into the future production strategy boundary
</trust_boundaries>
<threats>
- threat_id: T-02-03
  category: Integrity
  component: execution path duplication
  disposition: mitigate
  mitigation_plan: Keep the CLI as a thin wrapper over `run_backtest(...)` so there is only one execution path to maintain.
- threat_id: T-02-04
  category: Maintainability
  component: phase boundary drift
  disposition: mitigate
  mitigation_plan: If a temporary strategy harness is needed, label it clearly and keep it minimal so Phase 3 can own real signal logic cleanly.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_backtest_runner.py -q` passes
- [ ] `uv run pytest tests/test_backtest_runner.py tests/test_cli.py -q` passes
- [ ] `run_backtest(config, params) -> dict` exists and is the source of truth for backtest execution
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The project exposes a reusable Python backtest runner and structured metrics extraction
- The CLI and Python API share one execution path

</success_criteria>

<output>
After completion, create `.planning/phases/02-backtest-runner/02-02-SUMMARY.md`
</output>
