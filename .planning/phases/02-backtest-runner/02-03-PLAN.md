---
phase: 02-backtest-runner
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/mgc_bt/backtest/artifacts.py
  - src/mgc_bt/backtest/plotting.py
  - src/mgc_bt/backtest/runner.py
  - src/mgc_bt/cli.py
  - tests/test_backtest_artifacts.py
autonomous: true
requirements: [BT-05, BT-06, BT-07]
user_setup: []
must_haves:
  truths:
    - "Each canonical backtest run writes a reproducible artifact bundle under a timestamped directory."
    - "The `latest/` folder is refreshed after each run for convenience without replacing the canonical timestamped run."
    - "The results bundle is machine-readable first and includes an equity-curve PNG."
  artifacts:
    - path: "src/mgc_bt/backtest/artifacts.py"
      provides: "Result directory management and persisted summary/trade/config outputs"
      min_lines: 100
    - path: "src/mgc_bt/backtest/plotting.py"
      provides: "Equity-curve PNG generation"
      min_lines: 60
    - path: "tests/test_backtest_artifacts.py"
      provides: "Artifact-bundle regression coverage"
      contains: "summary.json"
  key_links:
    - from: "src/mgc_bt/backtest/runner.py"
      to: "src/mgc_bt/backtest/artifacts.py"
      via: "persist results after summary extraction"
      pattern: "summary.json|trades.csv|run_config.toml"
    - from: "src/mgc_bt/backtest/artifacts.py"
      to: "src/mgc_bt/backtest/plotting.py"
      via: "equity curve generation"
      pattern: "equity_curve"
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/backtest/artifacts.py"
      via: "final user-facing output path reporting"
      pattern: "latest|results"
---

<objective>
Add artifact persistence and equity-curve generation for completed backtest runs.

Purpose: Finish the Phase 2 runner contract with reproducible output bundles that Phase 4 and the user can both consume easily.
Output: Timestamped run folders, refreshed `latest/`, `summary.json`, `trades.csv`, `run_config.toml`, `equity_curve.png`, and artifact tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backtest-runner/02-CONTEXT.md
@.planning/phases/02-backtest-runner/02-RESEARCH.md
@.planning/phases/02-backtest-runner/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist the canonical backtest artifact bundle</name>
  <files>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/backtest/runner.py</files>
  <read_first>.planning/phases/02-backtest-runner/02-CONTEXT.md, src/mgc_bt/backtest/results.py, configs/settings.toml</read_first>
  <action>Create artifact helpers that write each canonical run to `results/backtests/YYYY-MM-DD_HHMMSS/` and persist `summary.json`, `trades.csv`, and `run_config.toml`. Ensure `summary.json` includes at minimum total PnL, Sharpe, win rate, max drawdown, total trades, start date, end date, instrument id, and all strategy parameters used. Keep the artifact schema stable and machine-readable so Phase 4 can consume it programmatically.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/artifacts.py` writes `summary.json`, `trades.csv`, and `run_config.toml`
    - `summary.json` includes the required minimum keys
    - persisted run config captures the exact settings used for that run
  </acceptance_criteria>
  <done>The backtest runner produces a reproducible, machine-readable canonical result bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the equity-curve PNG and refresh `latest/`</name>
  <files>src/mgc_bt/backtest/plotting.py, src/mgc_bt/backtest/artifacts.py</files>
  <read_first>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/backtest/results.py</read_first>
  <action>Add plotting support that turns structured equity data into `equity_curve.png`, then refresh `results/backtests/latest/` after each completed run by copying or overwriting the latest artifact set. Keep the canonical timestamped folder as the source of truth and ensure the `latest/` refresh is safe and deterministic on Windows.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/plotting.py` generates `equity_curve.png`
    - `src/mgc_bt/backtest/artifacts.py` refreshes `results/backtests/latest/`
    - the canonical timestamped run folder remains intact after `latest/` refresh
  </acceptance_criteria>
  <done>The user gets both reproducible archived runs and a quick-access latest result bundle.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize CLI output and artifact regression coverage</name>
  <files>src/mgc_bt/cli.py, tests/test_backtest_artifacts.py, tests/test_backtest_runner.py</files>
  <read_first>src/mgc_bt/cli.py, src/mgc_bt/backtest/artifacts.py, .planning/phases/02-backtest-runner/02-VALIDATION.md</read_first>
  <action>Update the CLI so a completed `backtest` run prints a concise summary plus the canonical and latest output locations. Add regression tests that lock the artifact bundle schema, the `latest/` refresh behavior, and the expectation that the CLI still depends on the shared runner instead of building artifacts independently.</action>
  <verify>uv run pytest tests/test_backtest_runner.py tests/test_backtest_artifacts.py -q</verify>
  <acceptance_criteria>
    - `tests/test_backtest_artifacts.py` covers artifact presence and `latest/` refresh
    - `tests/test_backtest_runner.py` still proves the shared runner path
    - CLI output references the canonical and latest result locations
  </acceptance_criteria>
  <done>Phase 2 ends with a stable, user-friendly artifact contract and regression coverage around it.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Structured backtest results into persisted JSON/CSV/TOML artifacts
- Artifact generation into the user-visible `latest/` convenience path
- Equity data into plotting output
</trust_boundaries>
<threats>
- threat_id: T-02-05
  category: Repudiation
  component: artifact persistence
  disposition: mitigate
  mitigation_plan: Persist machine-readable summaries and exact run config snapshots so runs are reproducible and auditable.
- threat_id: T-02-06
  category: Integrity
  component: latest-result refresh
  disposition: mitigate
  mitigation_plan: Write canonical timestamped outputs first, then refresh `latest/` as a separate deterministic copy/update step.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_backtest_artifacts.py -q` passes
- [ ] `uv run pytest tests/test_backtest_runner.py tests/test_backtest_artifacts.py -q` passes
- [ ] A completed run writes `summary.json`, `trades.csv`, `run_config.toml`, and `equity_curve.png`, then refreshes `latest/`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Completed backtests produce stable artifact bundles and an equity-curve PNG
- The result bundle is ready for machine consumption in Phase 4

</success_criteria>

<output>
After completion, create `.planning/phases/02-backtest-runner/02-03-SUMMARY.md`
</output>
