---
phase: 02-backtest-runner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/settings.toml
  - src/mgc_bt/config.py
  - src/mgc_bt/cli.py
  - src/mgc_bt/backtest/__init__.py
  - src/mgc_bt/backtest/contracts.py
  - src/mgc_bt/backtest/configuration.py
  - tests/test_backtest_contracts.py
autonomous: true
requirements: [BT-01, BT-02, BT-03, BT-04]
user_setup: []
must_haves:
  truths:
    - "The backtest command can resolve either a specific MGC contract or an auto-rolled contract sequence from the existing catalog."
    - "The backtest setup encodes completed-bar evaluation with next-bar execution assumptions through Nautilus-native configuration."
    - "Phase 2 planning keeps the Phase 1 Databento decode split visible anywhere catalog continuity is referenced."
  artifacts:
    - path: "src/mgc_bt/backtest/contracts.py"
      provides: "Contract selection and roll scheduling helpers"
      min_lines: 80
    - path: "src/mgc_bt/backtest/configuration.py"
      provides: "Nautilus backtest venue/data/run configuration assembly"
      min_lines: 100
    - path: "tests/test_backtest_contracts.py"
      provides: "Contract-mode and roll-rule coverage"
      contains: "roll"
  key_links:
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/backtest/configuration.py"
      via: "backtest command setup"
      pattern: "build_backtest|run_backtest|contracts"
    - from: "src/mgc_bt/backtest/configuration.py"
      to: "src/mgc_bt/backtest/contracts.py"
      via: "contract resolution before run-config assembly"
      pattern: "resolve|roll"
    - from: "src/mgc_bt/backtest/configuration.py"
      to: "configs/settings.toml"
      via: "backtest section expansion"
      pattern: "commission|slippage|roll|mode"
---

<objective>
Build the catalog-backed Nautilus backtest configuration and venue setup layer.

Purpose: Give the project an official Nautilus backtest setup path with explicit contract-mode and execution assumptions before any result export or optimization reuse is added.
Output: Expanded backtest config, contract-resolution helpers, Nautilus run-config builders, and focused tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backtest-runner/02-CONTEXT.md
@.planning/phases/02-backtest-runner/02-RESEARCH.md
@.planning/phases/01-catalog-foundation/01-02-SUMMARY.md
@nt_docs/concepts/backtesting.md
@nt_docs/getting_started/backtest_high_level.py
@nt_docs/api_reference/backtest.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend settings and CLI inputs for backtest mode selection</name>
  <files>configs/settings.toml, src/mgc_bt/config.py, src/mgc_bt/cli.py</files>
  <read_first>.planning/phases/02-backtest-runner/02-CONTEXT.md, configs/settings.toml, src/mgc_bt/config.py</read_first>
  <action>Expand the `[backtest]` config section with stable keys for default execution mode, roll preference, calendar fallback behavior, venue identity, and bounded run windows. Update the typed settings loader so these values are validated at load time. Extend the `backtest` CLI subcommand to accept an optional `--instrument-id` override for single-contract mode while keeping auto-roll as the default when no override is supplied.</action>
  <verify>uv run pytest tests/test_cli.py tests/test_backtest_contracts.py -q</verify>
  <acceptance_criteria>
    - `configs/settings.toml` contains additional `[backtest]` keys for mode and rolling behavior
    - `src/mgc_bt/config.py` validates the expanded backtest settings
    - `src/mgc_bt/cli.py` contains `--instrument-id`
  </acceptance_criteria>
  <done>The CLI and settings layer can express the locked Phase 2 execution modes without hardcoded values.</done>
</task>

<task type="auto">
  <name>Task 2: Implement contract resolution and roll fallback helpers</name>
  <files>src/mgc_bt/backtest/__init__.py, src/mgc_bt/backtest/contracts.py, tests/test_backtest_contracts.py</files>
  <read_first>.planning/phases/02-backtest-runner/02-RESEARCH.md, .planning/phases/01-catalog-foundation/01-VERIFICATION.md, nt_docs/api_reference/persistence.md</read_first>
  <action>Create a backtest package and implement helpers that inspect the existing catalog to resolve either a single explicit contract or an auto-rolled sequence of outright MGC futures. Prefer open-interest-based rolling when that data is available through the selected catalog path. If it is not, compute a calendar fallback that rolls 5 business days before the third-to-last business day of the delivery month. Keep this logic deterministic, testable, and separated from result exporting.</action>
  <verify>uv run pytest tests/test_backtest_contracts.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/contracts.py` contains both single-contract and auto-roll resolution paths
    - `src/mgc_bt/backtest/contracts.py` contains fallback logic for 5 business days before last trading day
    - `tests/test_backtest_contracts.py` contains assertions covering the fallback roll behavior
  </acceptance_criteria>
  <done>The project can decide what MGC contract data a run should use before the backtest engine is configured.</done>
</task>

<task type="auto">
  <name>Task 3: Build Nautilus venue and run configuration assembly</name>
  <files>src/mgc_bt/backtest/configuration.py, src/mgc_bt/backtest/contracts.py</files>
  <read_first>nt_docs/concepts/backtesting.md, nt_docs/getting_started/backtest_high_level.py, nt_docs/api_reference/backtest.md</read_first>
  <action>Before building the configuration layer, verify whether installed `nautilus_trader 1.225.0` uses `BacktestEngineConfig`, `BacktestRunConfig`, or another top-level object for this workflow by checking `nt_docs/api_reference/backtest.md` and the actual installed package with `uv run python -c "from nautilus_trader.backtest.engine import BacktestEngine; help(BacktestEngine.__init__)"`. Use the API the installed version actually accepts, not an older docs or memory-based assumption. Then create configuration helpers that assemble `BacktestVenueConfig`, data configs, and the correct top-level run configuration using the catalog and chosen contract mode. Encode the locked cost model with `$0.50` per-side commission and 1 tick slippage through Nautilus-native configuration. Verify and document the Nautilus 1.225.0 approach for the intended completed-bar, next-bar execution behavior instead of inventing a custom post-fill workaround. Add a small code comment or docstring reminding future catalog-touching work that definitions were ingested with legacy Cython objects while bars and trades were ingested with `as_legacy_cython=False`.</action>
  <verify>uv run pytest tests/test_backtest_contracts.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/configuration.py` contains Nautilus backtest config objects accepted by installed `nautilus_trader 1.225.0`, such as `BacktestVenueConfig`, `BacktestRunConfig`, or the verified equivalent
    - `src/mgc_bt/backtest/configuration.py` contains the commission and slippage configuration path
    - the catalog continuity note about definitions legacy Cython and bars/trades `as_legacy_cython=False` is preserved in the code or adjacent module docstring
  </acceptance_criteria>
  <done>The project has a reusable, official Nautilus configuration layer for backtest execution.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Project config and CLI options into contract selection and venue setup
- Catalog metadata into roll scheduling and backtest data configuration
- Nautilus venue/fill configuration into simulated execution outcomes
</trust_boundaries>
<threats>
- threat_id: T-02-01
  category: Integrity
  component: contract selection
  disposition: mitigate
  mitigation_plan: Keep single-contract and auto-roll resolution explicit, deterministic, and covered by targeted tests.
- threat_id: T-02-02
  category: Integrity
  component: execution modeling
  disposition: mitigate
  mitigation_plan: Use Nautilus-native venue and fill-model configuration for commission, slippage, and next-bar assumptions instead of mutating results after the run.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_backtest_contracts.py -q` passes
- [ ] `uv run pytest tests/test_cli.py tests/test_backtest_contracts.py -q` passes
- [ ] The backtest setup path explicitly encodes commission, slippage, and contract mode assumptions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The project can build official Nautilus backtest configs for single-contract or auto-roll execution
- The Phase 1 catalog decode split is still documented anywhere catalog continuity matters

</success_criteria>

<output>
After completion, create `.planning/phases/02-backtest-runner/02-01-SUMMARY.md`
</output>
