---
phase: 10-full-5-year-optimization-run
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md
autonomous: false
requirements: [RES-01, RES-02]
user_setup:
  - "Run `uv run python -m mgc_bt --config configs/smoke_optimization.toml health` before the smoke run."
  - "Run `uv run python -m mgc_bt --config configs/smoke_optimization.toml optimize --walk-forward` for the smoke run and paste the terminal summary plus the run directory."
  - "If the smoke run passes, run `uv run python -m mgc_bt health` before the full run."
  - "Run `uv run python -m mgc_bt optimize --walk-forward` for the full 5-year study, using `--resume` if interrupted, then paste the terminal summary plus the final run directory."
  - "Open the final `tearsheet.html` in a browser and provide a short note on the equity curve and walk-forward chart shape for inclusion in the gate report."
must_haves:
  truths:
    - "Phase 10 findings are produced from persisted smoke/full-run artifacts rather than rerunning optimization analysis inside the report step."
    - "Smoke results are reviewed before the full 5-year optimization is considered valid for the phase."
    - "The phase hard-stops on integrity failures but still reports weak-performance findings as research outcomes."
  artifacts:
    - path: ".planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md"
      provides: "Saved stop-gate findings, anomalies, artifact paths, and Phase 10 go/no-go summary"
      contains: "walk-forward aggregated Sharpe"
    - path: "results/optimization/<timestamp>/walk_forward/aggregated_summary.json"
      provides: "Canonical walk-forward aggregate metrics for smoke/full-run interpretation"
      contains: "aggregated_oos_sharpe"
    - path: "results/optimization/<timestamp>/monte_carlo/monte_carlo_summary.json"
      provides: "Canonical Monte Carlo p-value and pass/fail signal"
      contains: "p_value"
  key_links:
    - from: ".planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md"
      to: "results/optimization/<timestamp>/ranked_results.csv"
      via: "top-3 ranked parameter extraction"
      pattern: "rank|objective_score"
    - from: ".planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md"
      to: "results/optimization/<timestamp>/walk_forward/params_over_time.csv"
      via: "best walk-forward-selected parameter path"
      pattern: "window_index"
    - from: ".planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md"
      to: "results/optimization/<timestamp>/tearsheet.html"
      via: "saved visual summary of the final report"
      pattern: "tearsheet"
---

<objective>
Validate the smoke and full 5-year optimization artifacts, hard-stop on integrity failures, and write the Phase 10 research findings package that pauses the milestone for human review.

Purpose: RES-01 and RES-02 are satisfied only when the actual persisted optimization outputs are interpreted and summarized, not merely produced.
Output: Smoke-run validation, full-run artifact validation, `.planning/.../10-RESEARCH-FINDINGS.md`, and a concise stop-gate report in chat.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-full-5-year-optimization-run/10-CONTEXT.md
@.planning/phases/10-full-5-year-optimization-run/10-RESEARCH.md
@.planning/phases/10-full-5-year-optimization-run/10-VALIDATION.md
@src/mgc_bt/cli.py
@src/mgc_bt/optimization/results.py
@src/mgc_bt/reporting/loaders.py
@src/mgc_bt/reporting/tearsheet.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate the smoke-run artifact bundle before approving the full run</name>
  <files>.planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md</files>
  <read_first>src/mgc_bt/cli.py, src/mgc_bt/optimization/results.py, src/mgc_bt/reporting/loaders.py, .planning/phases/10-full-5-year-optimization-run/10-CONTEXT.md</read_first>
  <action>After the human runs the smoke health and optimize commands and pastes the terminal summary plus the optimization run directory, validate the persisted smoke artifact bundle directly from disk. Confirm that `ranked_results.csv`, `optimization_summary.json`, `walk_forward/window_results.csv`, `walk_forward/aggregated_summary.json`, `monte_carlo/monte_carlo_summary.json`, `stability/stability_summary.json`, `tearsheet.html`, and `manifest.json` all exist, that `ranked_results.csv` is non-empty, and that the smoke run surfaces enough signal to justify the full run. Hard-stop the phase if the smoke run already exhibits an integrity failure such as missing Monte Carlo output or an empty ranked-results file; otherwise produce a concise smoke summary and explicitly mark the full run as approved to start.</action>
  <verify>Smoke artifacts exist at the pasted run directory and `ranked_results.csv` contains at least one data row.</verify>
  <acceptance_criteria>
    - smoke validation is performed from persisted files only
    - smoke validation explicitly checks for `ranked_results.csv`, `walk_forward/aggregated_summary.json`, `monte_carlo/monte_carlo_summary.json`, `stability/stability_summary.json`, and `tearsheet.html`
    - missing Monte Carlo output or an empty ranked-results file hard-stop the phase before the full run
    - the smoke review yields a concise go/no-go note for the full run
  </acceptance_criteria>
  <done>The human does not start the hours-long full run until the smoke artifact bundle is confirmed healthy.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the full run, extract the gate metrics, and write the saved findings report</name>
  <files>.planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md</files>
  <read_first>src/mgc_bt/optimization/results.py, src/mgc_bt/reporting/loaders.py, src/mgc_bt/reporting/tearsheet.py, .planning/phases/10-full-5-year-optimization-run/10-DISCUSSION-LOG.md</read_first>
  <action>After the human completes the full 5-year optimization and pastes the terminal summary, final run directory, and brief browser note about `tearsheet.html`, validate the canonical artifact bundle from disk. Extract `walk-forward aggregated Sharpe` from `walk_forward/aggregated_summary.json`, the Monte Carlo `p_value` from `monte_carlo/monte_carlo_summary.json`, the top `3` raw parameter sets from `ranked_results.csv`, and the best walk-forward-selected parameter path from `walk_forward/params_over_time.csv` or `selected_params` in the aggregate summary. Apply the locked anomaly policy: hard-stop integrity failures when there are fewer than `3` conclusive windows, when Monte Carlo output is missing, or when `ranked_results.csv` is empty; otherwise warn but continue for negative aggregated Sharpe, no-edge Monte Carlo evidence, or mostly inconclusive windows. Write `.planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md` with artifact paths, extracted metrics, warnings/anomalies, overfitting observations when raw top-3 params materially diverge from the walk-forward path, and the human-provided tearsheet visual note.</action>
  <verify>The saved findings file exists and cites the final run directory plus the extracted walk-forward, Monte Carlo, ranked-results, and tearsheet sources.</verify>
  <acceptance_criteria>
    - `10-RESEARCH-FINDINGS.md` is written after the full run completes
    - the report includes aggregated walk-forward Sharpe, Monte Carlo p-value, top 3 raw parameter sets, and best walk-forward-selected parameter path
    - the report flags hard-stop integrity failures separately from weak-performance warnings
    - the report includes a brief tearsheet visual note about the equity curve and walk-forward chart
  </acceptance_criteria>
  <done>The phase has a durable saved research finding artifact that can drive the human go/no-go decision for Phase 11.</done>
</task>

<task type="auto">
  <name>Task 3: Produce the concise chat gate report and stop the milestone at the Phase 10 boundary</name>
  <files>.planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md</files>
  <read_first>.planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>From the saved findings report, produce the concise Phase 10 gate report in chat: aggregated walk-forward Sharpe, Monte Carlo p-value, top `3` raw parameter sets, best walk-forward-selected path summary, warnings or anomalies, and the final recommendation on whether Phase 11 should be approved. End by explicitly stating that Phase 10 is complete and that no Phase 11 work should begin until the human approves it.</action>
  <verify>The final chat report includes all locked stop-gate fields and ends with an explicit approval wait state.</verify>
  <acceptance_criteria>
    - the chat gate report is concise and decision-oriented
    - it includes all locked Phase 10 stop-gate metrics
    - it explicitly says Phase 11 is blocked pending human approval
  </acceptance_criteria>
  <done>The phase stops cleanly at the human review boundary with both saved and conversational outputs aligned.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Human-pasted command summaries into Codex interpretation
- Persisted optimization artifacts into the saved findings report
- Protected final-test policy into the phase stop-gate recommendation
</trust_boundaries>
<threats>
- threat_id: T-10-04
  category: Integrity
  component: smoke/full artifact interpretation
  disposition: mitigate
  mitigation_plan: Validate smoke and full-run findings from persisted files only so the stop-gate report cannot drift from the canonical optimization outputs.
- threat_id: T-10-05
  category: Integrity
  component: research completion gate
  disposition: mitigate
  mitigation_plan: Hard-stop on missing Monte Carlo output, empty ranked results, or fewer than three conclusive windows before approving the phase as a valid research artifact.
- threat_id: T-10-06
  category: Integrity
  component: hidden final test set
  disposition: mitigate
  mitigation_plan: Keep Phase 10 findings restricted to walk-forward, Monte Carlo, stability, and tearsheet outputs while explicitly blocking any use of `--final-test`.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Smoke run health and optimize commands have been executed by the human and their run directory has been reviewed
- [ ] Full run health and optimize commands have been executed by the human and their final run directory has been reviewed
- [ ] The final run directory contains `ranked_results.csv`, `walk_forward/aggregated_summary.json`, `monte_carlo/monte_carlo_summary.json`, `stability/stability_summary.json`, and `tearsheet.html`
- [ ] `.planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md` exists and contains the locked stop-gate metrics
- [ ] The final chat report explicitly blocks Phase 11 pending human approval
</verification>

<success_criteria>

- All tasks completed
- Smoke and full optimization artifacts are validated from disk
- The saved findings report captures all required Phase 10 metrics and anomalies
- The phase stops cleanly for human review before any Phase 11 work begins

</success_criteria>

<output>
After completion, create `.planning/phases/10-full-5-year-optimization-run/10-02-SUMMARY.md`
</output>
