---
phase: 10-full-5-year-optimization-run
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/mgc_bt/optimization/study.py
  - src/mgc_bt/optimization/results.py
  - src/mgc_bt/optimization/objective.py
  - src/mgc_bt/reporting/loaders.py
  - tests/test_optimization_results.py
  - tests/test_optimize_objective.py
  - tests/test_tearsheet.py
  - tests/test_cli.py
autonomous: true
requirements: [RES-01, RES-02]
user_setup: []
must_haves:
  truths:
    - "Walk-forward optimization always writes core artifacts and manifest.json even when optional post-processing stages fail."
    - "Walk-forward mode persists ranked_results.csv with full candidate rows plus selected-for-test visibility."
    - "Windows subprocess timeouts are explicit, logged, and cleaned up with full process-tree fallback."
  artifacts:
    - path: "src/mgc_bt/optimization/study.py"
      provides: "Manifest-driven best-effort walk-forward finalization orchestration"
      contains: "failed_stages"
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Walk-forward ranked-results writer and staged manifest/status helpers"
      contains: "ranked_results.csv"
    - path: "src/mgc_bt/optimization/objective.py"
      provides: "Windows timeout cleanup with psutil-backed process-tree termination"
      contains: "psutil"
  key_links:
    - from: "src/mgc_bt/optimization/study.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "stage-by-stage finalization writes artifacts and updates manifest/status"
      pattern: "manifest|status|failed_stages"
    - from: "src/mgc_bt/optimization/study.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "walk-forward branch writes ranked_results.csv from training and validation candidates"
      pattern: "ranked_results"
    - from: "src/mgc_bt/optimization/objective.py"
      to: "src/mgc_bt/backtest/runner.py"
      via: "subprocess trial execution remains disk-backed and timeout-protected"
      pattern: "timeout|terminate|kill"
---

<objective>
Fix the three blockers preventing Phase 10 smoke/full optimization completion: brittle finalization, missing walk-forward ranked results, and unreliable Windows subprocess timeout cleanup.

Purpose: Phase 10 cannot be closed until smoke/full runs always produce a trustworthy artifact bundle or a truthful degraded bundle with failures recorded explicitly.
Output: Manifest-driven finalization, walk-forward ranked_results.csv, hardened Windows subprocess cleanup, and regression coverage for all three contracts.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-full-5-year-optimization-run/10-CONTEXT.md
@.planning/phases/10-full-5-year-optimization-run/10-RESEARCH.md
@.planning/phases/10-full-5-year-optimization-run/10-DISCUSSION-LOG.md
@src/mgc_bt/optimization/study.py
@src/mgc_bt/optimization/results.py
@src/mgc_bt/optimization/objective.py
@src/mgc_bt/reporting/loaders.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement manifest-driven finalization robustness for walk-forward runs</name>
  <files>src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/reporting/loaders.py</files>
  <read_first>src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/reporting/loaders.py, .planning/phases/10-full-5-year-optimization-run/10-DISCUSSION-LOG.md</read_first>
  <action>Refactor the walk-forward finalization path so each stage runs independently and records its own status instead of living in one brittle chain. Core artifacts must be written first and guaranteed once walk-forward execution completes: `walk_forward/window_results.csv`, `walk_forward/aggregated_summary.json`, `failed_trials.json`, `optimization_summary.json`, `run_config.toml`, and `manifest.json`. Optional stages such as Monte Carlo, stability, analytics, and tearsheet must be isolated so a failure in one stage records a manifest-visible failure but never blocks later stages or the manifest write. Extend the run metadata so `manifest.json` always reflects actual produced artifacts plus failed stages, degraded sections, and final run status.</action>
  <verify>uv run pytest tests/test_optimization_results.py tests/test_tearsheet.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - `manifest.json` is always written for a completed walk-forward execution path even when stability or tearsheet generation fails
    - core artifacts are written before optional stages and remain present after an optional-stage exception
    - the manifest records produced artifacts and failed stages truthfully rather than implying full success
    - tearsheet loaders continue to render missing optional sections explicitly from the degraded artifact bundle
  </acceptance_criteria>
  <done>The optimization pipeline can finish truthfully and durably even when one research/presentation stage fails.</done>
</task>

<task type="auto">
  <name>Task 2: Write ranked_results.csv for walk-forward candidate surfaces and selected paths</name>
  <files>src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, tests/test_optimization_results.py, tests/test_cli.py</files>
  <read_first>src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, .planning/phases/10-full-5-year-optimization-run/10-RESEARCH-FINDINGS.md</read_first>
  <action>Add a walk-forward ranked-results writer that persists the full candidate surface from disk. `ranked_results.csv` must include one row per candidate with `window_index`, `trial_number`, parameter columns, `training_sharpe`, validation selection metrics, `selected_for_test`, and test metrics populated for selected rows only. Keep `params_over_time.csv` as the chosen walk-forward path, but ensure `ranked_results.csv` makes both the per-window candidate set and the selected path inspectable from disk. Wire the walk-forward branch to always emit this file before optional stages run.</action>
  <verify>uv run pytest tests/test_optimization_results.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - walk-forward runs write `ranked_results.csv` at the optimization run root
    - each ranked-results row includes `window_index`, `trial_number`, parameter columns, `training_sharpe`, validation metrics, and `selected_for_test`
    - selected rows include test metrics while non-selected candidate rows leave test-only fields blank
    - `ranked_results.csv` and `walk_forward/params_over_time.csv` together allow inspection of both the full candidate surface and chosen walk-forward path
  </acceptance_criteria>
  <done>Walk-forward optimization no longer hides the candidate ranking surface and can support Phase 10 gate analysis from disk alone.</done>
</task>

<task type="auto">
  <name>Task 3: Harden Windows subprocess timeout cleanup with psutil process-tree fallback</name>
  <files>src/mgc_bt/optimization/objective.py, tests/test_optimize_objective.py</files>
  <read_first>src/mgc_bt/optimization/objective.py, .planning/phases/10-full-5-year-optimization-run/10-DISCUSSION-LOG.md</read_first>
  <action>Replace the current timeout handling with the locked Windows cleanup pattern: `join(timeout)` then `terminate()` then brief wait then recursive process-tree kill through `psutil` if the child is still alive or has descendants. Close process handles cleanly, preserve disk-backed child outputs as canonical, and add explicit timeout/fallback logging so long silence is never ambiguous. Timeout or cleanup failures must still return a penalized trial result rather than hanging the whole window indefinitely.</action>
  <verify>uv run pytest tests/test_optimize_objective.py -q</verify>
  <acceptance_criteria>
    - timeout handling logs the timeout event and whether terminate or psutil kill fallback was required
    - Windows cleanup uses recursive process-tree termination instead of `terminate()` alone
    - timed-out trials return a penalty result and the parent optimization loop continues
    - regression coverage proves timeout cleanup runs without silently hanging the test process
  </acceptance_criteria>
  <done>The walk-forward optimization loop can survive stuck child processes on Windows without ambiguous silence or leaked descendants.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Walk-forward execution outputs into staged finalization and manifest reporting
- Candidate-trial metadata into persisted ranked-results artifacts
- Child subprocess lifecycle into parent optimization control flow on Windows
</trust_boundaries>
<threats>
- threat_id: T-10-07
  category: Integrity
  component: finalization pipeline
  disposition: mitigate
  mitigation_plan: Split artifact writing into independently recorded stages and always emit a truthful manifest describing success and failure.
- threat_id: T-10-08
  category: Integrity
  component: walk-forward parameter reporting
  disposition: mitigate
  mitigation_plan: Persist full candidate rows plus selected-path markers so overfitting and path divergence can be audited from disk.
- threat_id: T-10-09
  category: Availability
  component: Windows subprocess cleanup
  disposition: mitigate
  mitigation_plan: Add explicit timeout logging plus psutil-backed recursive kill fallback so stuck children cannot stall an optimization window indefinitely.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_optimization_results.py tests/test_optimize_objective.py tests/test_tearsheet.py tests/test_cli.py -q`
- [ ] A simulated stability failure still writes `walk_forward/*`, `failed_trials.json`, `optimization_summary.json`, `run_config.toml`, and `manifest.json`
- [ ] A walk-forward run writes non-empty `ranked_results.csv` with `window_index` and `selected_for_test`
- [ ] A simulated hung child process triggers explicit timeout logging and psutil-backed cleanup without blocking the parent loop
</verification>

<success_criteria>

- All tasks completed
- Walk-forward runs always produce a truthful manifest-aware artifact bundle even in degraded mode
- `ranked_results.csv` exists and exposes both candidate surfaces and selected rows
- Windows subprocess timeouts are explicit, recoverable, and do not leave hanging descendants

</success_criteria>

<output>
After completion, create `.planning/phases/10-full-5-year-optimization-run/10-03-SUMMARY.md`
</output>
