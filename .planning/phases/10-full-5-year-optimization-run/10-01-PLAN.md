---
phase: 10-full-5-year-optimization-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/settings.toml
  - configs/smoke_optimization.toml
  - tests/test_config.py
autonomous: true
requirements: [RES-01]
user_setup: []
must_haves:
  truths:
    - "Phase 10 prep aligns `configs/settings.toml` to the locked 5-year research windows before any optimization run starts."
    - "A reusable `configs/smoke_optimization.toml` exists and exercises the full walk-forward + Monte Carlo + stability pipeline without exposing the protected final test set."
    - "Smoke and full-run operator commands use the typed TOML config path rather than ad-hoc parameter overrides."
  artifacts:
    - path: "configs/smoke_optimization.toml"
      provides: "Reusable smoke optimization settings for future validation passes"
      contains: "[walk_forward]"
    - path: "tests/test_config.py"
      provides: "Regression coverage for the locked Phase 10 settings and smoke config contract"
      contains: "smoke_optimization"
  key_links:
    - from: "configs/settings.toml"
      to: "src/mgc_bt/config.py"
      via: "typed TOML loader validates the locked 5-year optimization windows"
      pattern: "in_sample_start|holdout_end"
    - from: "configs/smoke_optimization.toml"
      to: "src/mgc_bt/cli.py"
      via: "global --config supports smoke health and optimize runs"
      pattern: "--config"
---

<objective>
Prepare the Phase 10 research run safely: align the production settings to the locked 5-year dataset, create a reusable smoke config, and lock both through typed-config regression coverage.

Purpose: RES-01 begins with configuration correctness and repeatable operator entry points; this plan prevents burning hours on a misaligned optimization run.
Output: Updated production optimization windows, `configs/smoke_optimization.toml`, and tests proving both configs load with the intended contract.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-full-5-year-optimization-run/10-CONTEXT.md
@.planning/phases/10-full-5-year-optimization-run/10-RESEARCH.md
@.planning/phases/10-full-5-year-optimization-run/10-VALIDATION.md
@configs/settings.toml
@src/mgc_bt/config.py
@src/mgc_bt/cli.py
@src/mgc_bt/optimization/walk_forward.py
@src/mgc_bt/validation/preflight.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the production optimization windows to the locked Phase 10 dataset</name>
  <files>configs/settings.toml, tests/test_config.py</files>
  <read_first>configs/settings.toml, src/mgc_bt/config.py, src/mgc_bt/optimization/walk_forward.py, .planning/phases/10-full-5-year-optimization-run/10-CONTEXT.md</read_first>
  <action>Update `configs/settings.toml` so `[optimization]` matches the locked Phase 10 research contract exactly: `in_sample_start = "2021-03-09T00:00:00+00:00"`, `in_sample_end = "2024-12-31T23:59:00+00:00"`, `holdout_start = "2025-01-01T00:00:00+00:00"`, and `holdout_end = "2025-12-31T23:59:00+00:00"`. Keep `walk_forward.final_test_months = 6` unchanged so the protected final test remains the hidden last six months of 2025 when `--final-test` is eventually used. Extend `tests/test_config.py` with an additive regression that loads the production settings and asserts these exact optimization timestamps plus the locked six-month final-test constant.</action>
  <verify>uv run pytest tests/test_config.py -q -k "optimization or phase_ten"</verify>
  <acceptance_criteria>
    - `configs/settings.toml` contains the exact Phase 10 production windows
    - `tests/test_config.py` asserts the exact in-sample and holdout timestamps
    - the regression explicitly proves `walk_forward.final_test_months == 6`
  </acceptance_criteria>
  <done>The production config is aligned to the intended 2021-2025 research dataset before any human-run optimization begins.</done>
</task>

<task type="auto">
  <name>Task 2: Create the reusable smoke optimization config with the locked small-run contract</name>
  <files>configs/smoke_optimization.toml, tests/test_config.py</files>
  <read_first>configs/settings.toml, src/mgc_bt/config.py, .planning/phases/10-full-5-year-optimization-run/10-DISCUSSION-LOG.md</read_first>
  <action>Create `configs/smoke_optimization.toml` as a full typed settings file derived from the production config but locked for smoke validation: keep the same project paths, strategy selection, and research date coverage, set `[optimization].max_trials = 20`, keep `seed`, `storage_filename`, and results-root semantics valid for a resumable run, and set `[walk_forward]` to `train_months = 6`, `validation_months = 1`, `test_months = 1`, and `step_months = 1`. Do not enable `--final-test` in config or command guidance. Extend `tests/test_config.py` with a second regression that loads `configs/smoke_optimization.toml` and asserts the exact smoke values.</action>
  <verify>uv run pytest tests/test_config.py -q -k "smoke"</verify>
  <acceptance_criteria>
    - `configs/smoke_optimization.toml` exists as a full loadable settings file
    - smoke config locks `max_trials = 20`
    - smoke config locks walk-forward windows to `6/1/1/1`
    - `tests/test_config.py` proves the smoke config loads through `load_settings(...)`
  </acceptance_criteria>
  <done>The project has a reusable smoke-run config that exercises the full optimization pipeline without hand-edited one-off overrides.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the exact human-run command contract for smoke and full optimization</name>
  <files>configs/smoke_optimization.toml, .planning/phases/10-full-5-year-optimization-run/10-CONTEXT.md</files>
  <read_first>src/mgc_bt/cli.py, src/mgc_bt/validation/preflight.py, .planning/phases/10-full-5-year-optimization-run/10-CONTEXT.md</read_first>
  <action>Verify the exact operator command contract against the live CLI implementation and carry the final commands forward into the plan summary and phase execution context: smoke checks use `uv run python -m mgc_bt --config configs/smoke_optimization.toml health` followed by `uv run python -m mgc_bt --config configs/smoke_optimization.toml optimize --walk-forward`, and the full run uses `uv run python -m mgc_bt health` followed by `uv run python -m mgc_bt optimize --walk-forward`. Document in the plan summary that `--resume` is the allowed recovery path for interrupted full runs and that `--final-test` remains intentionally absent in Phase 10.</action>
  <verify>uv run pytest tests/test_config.py -q</verify>
  <acceptance_criteria>
    - the verified smoke commands use the global `--config` flag before the subcommand
    - the verified full-run commands omit `--final-test`
    - the verified recovery path is `optimize --resume`
  </acceptance_criteria>
  <done>The human has an exact command contract that matches the real CLI parser and the Phase 10 hidden-test policy.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Typed TOML settings into the optimize CLI and walk-forward scheduler
- Human-run command invocations into persistent Optuna storage and long-running optimization jobs
- Hidden final-test policy into production settings and future run behavior
</trust_boundaries>
<threats>
- threat_id: T-10-01
  category: Integrity
  component: optimization date windows
  disposition: mitigate
  mitigation_plan: Lock the exact Phase 10 timestamps in `configs/settings.toml` and regression-test them so the research run matches the intended 2021-2025 dataset.
- threat_id: T-10-02
  category: Availability
  component: smoke and full operator commands
  disposition: mitigate
  mitigation_plan: Create a reusable smoke config and verify the exact `uv run python -m mgc_bt --config ...` command contract against the live CLI parser before the human starts any long run.
- threat_id: T-10-03
  category: Integrity
  component: protected final test set
  disposition: mitigate
  mitigation_plan: Keep `walk_forward.final_test_months = 6` locked while explicitly omitting `--final-test` from all Phase 10 operator guidance.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_config.py -q`
- [ ] `configs/settings.toml` matches the exact locked 2021-03-09 -> 2025-12-31 window contract
- [ ] `configs/smoke_optimization.toml` loads successfully through the typed settings loader
- [ ] The smoke and full-run operator commands are verified against the live CLI parser
</verification>

<success_criteria>

- All tasks completed
- The production optimization windows match the locked Phase 10 research dataset
- A reusable smoke optimization config exists and is test-covered
- The human-run command contract is exact and does not expose the protected final test set

</success_criteria>

<output>
After completion, create `.planning/phases/10-full-5-year-optimization-run/10-01-SUMMARY.md`
</output>

