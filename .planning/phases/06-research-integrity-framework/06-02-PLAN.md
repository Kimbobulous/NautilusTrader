---
phase: 06-research-integrity-framework
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/mgc_bt/optimization/walk_forward.py, src/mgc_bt/optimization/objective.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/export.py, src/mgc_bt/cli.py, tests/test_cli.py, tests/test_optimize_objective.py, tests/test_optimization_results.py]
autonomous: true
requirements: [INT-01]
user_setup: []
must_haves:
  truths:
    - "User can run `mgc_bt optimize --walk-forward` and get repeated train/validate/test windows on the existing optimize command."
    - "Each walk-forward window re-optimizes on training data, chooses the winner on validation data, and reports only test-window out-of-sample performance in the aggregate."
    - "Skipped windows and inconclusive test windows are persisted explicitly instead of silently disappearing."
  artifacts:
    - path: "src/mgc_bt/optimization/walk_forward.py"
      provides: "Rolling walk-forward orchestration and window status tracking"
      contains: "class WalkForwardWindow"
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Walk-forward artifact writers"
      contains: "window_results.csv"
    - path: "src/mgc_bt/optimization/study.py"
      provides: "Optimize entry-point branch for walk-forward mode"
      contains: "walk_forward"
  key_links:
    - from: "src/mgc_bt/optimization/study.py"
      to: "src/mgc_bt/optimization/walk_forward.py"
      via: "walk-forward branch invokes coordinator"
      pattern: "walk_forward"
    - from: "src/mgc_bt/optimization/objective.py"
      to: "src/mgc_bt/backtest/runner.py"
      via: "window-specific parameter evaluation still calls run_backtest"
      pattern: "run_backtest"
---

<objective>
Implement the walk-forward optimizer that reuses the existing backtest runner, produces out-of-sample aggregate results, and exposes the full Phase 6 window contract through machine-readable artifacts.

Purpose: INT-01 is the core research-integrity workflow that turns single-window optimization into repeated out-of-sample validation.
Output: A new `optimize --walk-forward` branch, persisted walk-forward artifacts, and tests that prove the coordinator respects the locked Phase 6 rules.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-research-integrity-framework/06-CONTEXT.md
@.planning/phases/06-research-integrity-framework/06-RESEARCH.md
@src/mgc_bt/optimization/objective.py
@src/mgc_bt/optimization/study.py
@src/mgc_bt/optimization/results.py
@src/mgc_bt/optimization/export.py
@src/mgc_bt/backtest/runner.py
@src/mgc_bt/backtest/results.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the walk-forward window and candidate-selection coordinator</name>
  <files>src/mgc_bt/optimization/walk_forward.py, src/mgc_bt/optimization/objective.py</files>
  <read_first>src/mgc_bt/optimization/objective.py, src/mgc_bt/backtest/runner.py, src/mgc_bt/backtest/results.py, .planning/phases/06-research-integrity-framework/06-CONTEXT.md, .planning/phases/06-research-integrity-framework/06-RESEARCH.md</read_first>
  <action>Create `src/mgc_bt/optimization/walk_forward.py` with explicit records for `WalkForwardWindow`, `WalkForwardWindowResult`, and the aggregated OOS summary. Generate rolling windows from the new config using these exact calendar lengths: 12-month training, 3-month validation, 3-month test, 3-month step. Exclude the protected final six months reserved in Plan 01 from the rolling schedule. Before optimizing a window, count available one-minute bars from the catalog-backed selection and mark the window `status = "skipped"` with `skipped_reason = "insufficient_training_bars"` when the training slice has fewer than `50000` bars. Reuse the shared evaluation primitive by adding a helper in `src/mgc_bt/optimization/objective.py` that accepts `start_date`, `end_date`, and `evaluation_window` but still delegates to `run_backtest(settings, params) -> dict`. For each completed training study, rerun the top `5` completed training trials on the validation slice, and when fewer than `5` completed training trials exist, rerun all available completed trials; rank validation candidates by `validation_sharpe DESC`, `validation_max_drawdown_pct ASC`, `validation_total_pnl DESC`, then evaluate the selected params on the test slice. If a test slice produces fewer than `10` trades, persist `inconclusive = true`, keep the result row, and exclude that window from failure accounting.</action>
  <verify>uv run pytest tests/test_optimize_objective.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/optimization/walk_forward.py` contains `class WalkForwardWindow` and `class WalkForwardWindowResult`
    - `src/mgc_bt/optimization/walk_forward.py` contains the exact statuses `skipped` and `inconclusive`
    - `src/mgc_bt/optimization/objective.py` still calls `run_backtest(` while supporting explicit evaluation windows
    - validation candidate selection in code uses top `5` completed training trials and the tie-break order `validation_sharpe`, `validation_max_drawdown_pct`, `validation_total_pnl`
  </acceptance_criteria>
  <done>The walk-forward coordinator can build locked windows and evaluate train, validation, and test slices without changing the shared backtest runner contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `optimize --walk-forward` with progress updates, runtime warning, and aggregate OOS math</name>
  <files>src/mgc_bt/optimization/study.py, src/mgc_bt/cli.py</files>
  <read_first>src/mgc_bt/optimization/study.py, src/mgc_bt/cli.py, src/mgc_bt/optimization/walk_forward.py, .planning/phases/06-research-integrity-framework/06-CONTEXT.md</read_first>
  <action>In `src/mgc_bt/optimization/study.py`, add the walk-forward branch that calls the new coordinator when `walk_forward=True`. Print one progress line per completed window using this exact field set: window number, train date range, validation date range, test date range, training Sharpe, validation Sharpe, test Sharpe, and cumulative OOS PnL. After the first `3` completed trials of the first window, estimate total runtime from mean trial duration multiplied across the remaining trials and remaining windows; when the estimate exceeds `1800` seconds, print `Warning: estimated walk-forward runtime HH:MM:SS exceeds 00:30:00; estimated completion <timestamp>.` and continue. Aggregate out-of-sample performance from concatenated conclusive test-window equity and trade logs only, and compute aggregated Sharpe as a time-weighted average using each conclusive test window's one-minute bar count instead of a simple arithmetic mean. Keep the default optimize branch untouched. Update `_render_optimization_summary` in `src/mgc_bt/cli.py` so walk-forward runs append `Walk-forward summary: <path>` and `Walk-forward windows: completed=<n>, skipped=<n>, inconclusive=<n>` after the existing default summary lines.</action>
  <verify>uv run pytest tests/test_cli.py tests/test_optimization_results.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/optimization/study.py` contains the exact warning prefix `Warning: estimated walk-forward runtime`
    - `src/mgc_bt/optimization/study.py` computes aggregated walk-forward Sharpe with time weighting rather than simple mean wording or variable names
    - `src/mgc_bt/cli.py` prints `Walk-forward summary:` and `Walk-forward windows: completed=`
    - The no-flag optimize summary lines from Phase 5 remain present in `src/mgc_bt/cli.py`
  </acceptance_criteria>
  <done>The CLI can run a full walk-forward study with explicit progress, runtime estimation, and aggregated out-of-sample metrics while leaving the legacy optimize summary in place.</done>
</task>

<task type="auto">
  <name>Task 3: Persist walk-forward artifacts and add orchestration regression tests</name>
  <files>src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/export.py, tests/test_cli.py, tests/test_optimize_objective.py, tests/test_optimization_results.py</files>
  <read_first>src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/export.py, tests/test_cli.py, tests/test_optimize_objective.py, tests/test_optimization_results.py, src/mgc_bt/backtest/artifacts.py</read_first>
  <action>Extend `src/mgc_bt/optimization/results.py` and `src/mgc_bt/optimization/export.py` with walk-forward writers under `results/optimization/<timestamp>/walk_forward/`. Write exactly these files: `window_results.csv`, `aggregated_summary.json`, `equity_curve.png`, and `params_over_time.csv`. Make `aggregated_summary.json` additive and machine-readable with `schema_version = 1`, `run_type = "optimize"`, `analysis_type = "walk_forward"`, `completed_window_count`, `skipped_window_count`, `inconclusive_window_count`, `aggregated_oos_sharpe`, `aggregated_oos_total_pnl`, `selected_params`, and `status`. When `--final-test` ran, also write `walk_forward/final_test_summary.json` and `walk_forward/final_test_equity_curve.png`; otherwise persist `final_test_executed = false` inside `aggregated_summary.json` and do not create those files. Extend `tests/test_optimization_results.py` with a monkeypatched walk-forward run that asserts all exact walk-forward filenames exist and that `aggregated_summary.json` carries `schema_version`, `analysis_type`, `status`, and counts. Extend `tests/test_cli.py` and `tests/test_optimize_objective.py` to assert the walk-forward branch still uses the shared `run_backtest` contract and that inconclusive windows stay recorded instead of being dropped.</action>
  <verify>uv run pytest tests/test_cli.py tests/test_optimize_objective.py tests/test_optimization_results.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/optimization/results.py` contains `window_results.csv`, `aggregated_summary.json`, `equity_curve.png`, and `params_over_time.csv`
    - `aggregated_summary.json` is written with `schema_version` and `analysis_type = "walk_forward"`
    - `tests/test_optimization_results.py` asserts exact walk-forward artifact filenames
    - `tests/test_optimize_objective.py` proves walk-forward evaluation still routes through `run_backtest(settings, params)`
  </acceptance_criteria>
  <done>Walk-forward runs produce stable machine-readable outputs and test coverage strong enough to prevent silent contract drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_cli.py tests/test_optimize_objective.py tests/test_optimization_results.py -q`
- [ ] A walk-forward test run writes `walk_forward/window_results.csv`, `walk_forward/aggregated_summary.json`, `walk_forward/equity_curve.png`, and `walk_forward/params_over_time.csv`
- [ ] The walk-forward branch keeps using `run_backtest(settings, params)` instead of creating a second execution engine
</verification>

<success_criteria>

- All tasks completed
- `mgc_bt optimize --walk-forward` runs repeated train/validate/test windows on the existing optimize command
- Skipped and inconclusive windows are persisted explicitly
- Aggregate OOS metrics are built from conclusive test windows only and use time-weighted Sharpe
- Walk-forward artifacts are machine-readable and ready for Phase 8 tearsheet consumption

</success_criteria>

<output>
After completion, create `.planning/phases/06-research-integrity-framework/06-02-SUMMARY.md`
</output>
