---
phase: 06-research-integrity-framework
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified: [src/mgc_bt/optimization/stability.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py, tests/test_cli.py, tests/test_optimization_results.py, tests/test_optimization_stability.py]
autonomous: true
requirements: [INT-03]
user_setup: []
must_haves:
  truths:
    - "Optimization runs surface robust-versus-fragile parameter regions instead of only a single best trial."
    - "Stability analysis uses Optuna's built-in importance machinery for the top parameter pair and neighborhood reruns around the best parameter set."
    - "Fragile or underpowered stability analyses persist explicit skip reasons instead of silently failing."
  artifacts:
    - path: "src/mgc_bt/optimization/stability.py"
      provides: "Optuna importance, heatmap grid, and neighborhood robustness scoring"
      contains: "FanovaImportanceEvaluator"
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Stability artifact writers"
      contains: "top_pair_heatmap.csv"
    - path: "tests/test_optimization_stability.py"
      provides: "Stability regression coverage"
      contains: "robustness"
  key_links:
    - from: "src/mgc_bt/optimization/stability.py"
      to: "src/mgc_bt/optimization/search_space.py"
      via: "perturbation bounds and integer coercion"
      pattern: "optimized_param_names"
    - from: "src/mgc_bt/optimization/study.py"
      to: "src/mgc_bt/optimization/stability.py"
      via: "post-optimization stability hook"
      pattern: "stability"
---

<objective>
Implement parameter stability analysis with Optuna importance heatmaps and neighborhood robustness reruns, then lock the full Phase 6 regression surface with integration coverage.

Purpose: INT-03 is the final Phase 6 signal-quality check that distinguishes smooth robust regions from brittle point estimates.
Output: Stability analyzers, persisted heatmap and robustness artifacts, and end-to-end regression coverage for the full Phase 6 optimize surface.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-research-integrity-framework/06-CONTEXT.md
@.planning/phases/06-research-integrity-framework/06-RESEARCH.md
@src/mgc_bt/optimization/search_space.py
@src/mgc_bt/optimization/study.py
@src/mgc_bt/optimization/results.py
@src/mgc_bt/optimization/objective.py
@src/mgc_bt/backtest/runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Optuna importance and top-pair heatmap reruns</name>
  <files>src/mgc_bt/optimization/stability.py</files>
  <read_first>src/mgc_bt/optimization/search_space.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/objective.py, .planning/phases/06-research-integrity-framework/06-CONTEXT.md, .planning/phases/06-research-integrity-framework/06-RESEARCH.md</read_first>
  <action>Create `src/mgc_bt/optimization/stability.py` with a stability entry point that accepts the completed Optuna study, the selected best parameter set, and the evaluation context. Use `optuna.importance.get_param_importances(..., evaluator=FanovaImportanceEvaluator())` to rank parameters and pick the top two. Build a 5x5 heatmap grid around the winner using exact percentage offsets `[-20, -10, 0, 10, 20]` for both top parameters, clamp every candidate to the search-space bounds in `src/mgc_bt/optimization/search_space.py`, and coerce integer parameters back to integers before evaluation. For default optimize runs, evaluate heatmap cells on the holdout window. For walk-forward runs, reuse the same concatenated out-of-sample test schedule from Plan 02 instead of switching to an in-sample rerun. If there are fewer than `2` completed trials or fewer than `2` stable importance-ranked parameters, return `status = "skipped"` with `skipped_reason = "insufficient_completed_trials"`.</action>
  <verify>uv run pytest tests/test_optimization_stability.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/optimization/stability.py` contains `FanovaImportanceEvaluator`
    - `src/mgc_bt/optimization/stability.py` contains the exact offset list `[-20, -10, 0, 10, 20]`
    - `src/mgc_bt/optimization/stability.py` clamps perturbed values to the search-space bounds
    - `src/mgc_bt/optimization/stability.py` returns `skipped_reason = "insufficient_completed_trials"` when fANOVA cannot run meaningfully
  </acceptance_criteria>
  <done>The project can rank important parameters and rerun a concrete heatmap around the winner without inventing a custom surrogate model.</done>
</task>

<task type="auto">
  <name>Task 2: Add neighborhood robustness scoring, persistence, and CLI reporting</name>
  <files>src/mgc_bt/optimization/stability.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py</files>
  <read_first>src/mgc_bt/optimization/stability.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py, src/mgc_bt/optimization/search_space.py</read_first>
  <action>Extend `src/mgc_bt/optimization/stability.py` to compute neighborhood robustness by perturbing every optimized parameter independently at `-20%`, `-10%`, `+10%`, and `+20%` around the best parameter set, again clamped to the search-space bounds and integer-coerced when needed. Score each perturbed run as passing when `sharpe_ratio > 0.5`. Compute `robustness_score = passing_neighbors / total_neighbors * 100` and `robust = true` when the score is at least `70`. In `src/mgc_bt/optimization/results.py`, write exactly these files under `stability/`: `param_importance.json`, `top_pair_heatmap.csv`, `neighborhood_robustness.json`, and `stability_summary.json`. All JSON payloads must include `schema_version = 1`, `analysis_type = "stability"`, `top_parameters`, `robustness_score`, `robust`, `status`, and any `skipped_reason`. Wire the stability hook into `src/mgc_bt/optimization/study.py` so it runs automatically for walk-forward unless `skip_stability=True`, and for standard non-walk-forward optimization only when `stability=True` was explicitly requested. Append `Stability summary: <path>`, `Stability: skipped by flag`, or `Stability: not requested` in `_render_optimization_summary` as appropriate.</action>
  <verify>uv run pytest tests/test_cli.py tests/test_optimization_results.py tests/test_optimization_stability.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/optimization/results.py` contains `param_importance.json`, `top_pair_heatmap.csv`, `neighborhood_robustness.json`, and `stability_summary.json`
    - `src/mgc_bt/optimization/stability.py` computes `robustness_score` and `robust`
    - `src/mgc_bt/optimization/study.py` honors `skip_stability`
    - `src/mgc_bt/cli.py` prints `Stability summary:` and `Stability: skipped by flag`
  </acceptance_criteria>
  <done>Stability analysis runs automatically for walk-forward, can be explicitly opted into for normal optimize, and persists enough structured detail for future tearsheet plotting.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end Phase 6 regression protection for stability and combined optimize outputs</name>
  <files>tests/test_cli.py, tests/test_optimization_results.py, tests/test_optimization_stability.py</files>
  <read_first>tests/test_cli.py, tests/test_optimization_results.py, src/mgc_bt/optimization/stability.py, src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/study.py</read_first>
  <action>Create `tests/test_optimization_stability.py` with focused cases that assert the top-two importance selection, 5x5 heatmap shape, bounded perturbations, graceful skip behavior, and robustness threshold logic at `Sharpe > 0.5` and `robustness_score >= 70`. Extend `tests/test_optimization_results.py` so a fully instrumented optimize run can assert the exact `stability/` filenames plus coexistence with the `walk_forward/` and `monte_carlo/` directories added earlier, while a no-flag optimize run leaves stability absent. Extend `tests/test_cli.py` to cover `--skip-stability`, explicit `--stability`, and the new stability summary lines. Finish with a targeted regression command that exercises the Phase 6 test surface end to end: `uv run pytest tests/test_config.py tests/test_cli.py tests/test_optimize_objective.py tests/test_optimization_results.py tests/test_monte_carlo.py tests/test_optimization_stability.py -q`.</action>
  <verify>uv run pytest tests/test_config.py tests/test_cli.py tests/test_optimize_objective.py tests/test_optimization_results.py tests/test_monte_carlo.py tests/test_optimization_stability.py -q</verify>
  <acceptance_criteria>
    - `tests/test_optimization_stability.py` asserts a 5x5 heatmap and the `robustness_score` threshold logic
    - `tests/test_optimization_results.py` verifies `stability/`, `monte_carlo/`, and `walk_forward/` directories can coexist in one optimization run
    - `tests/test_cli.py` covers `--skip-stability`, explicit `--stability`, and the unchanged no-flag path
    - The targeted Phase 6 regression command exits successfully
  </acceptance_criteria>
  <done>The full Phase 6 optimize surface is protected by regression tests that catch contract drift before Phase 7 or Phase 8 build on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_config.py tests/test_cli.py tests/test_optimize_objective.py tests/test_optimization_results.py tests/test_monte_carlo.py tests/test_optimization_stability.py -q`
- [ ] A representative optimize run writes `stability/param_importance.json`, `stability/top_pair_heatmap.csv`, `stability/neighborhood_robustness.json`, and `stability/stability_summary.json`
- [ ] Stability analysis uses Optuna importance plus real reruns around the best parameter set instead of a custom synthetic score
</verification>

<success_criteria>

- All tasks completed
- Stability analysis reports top parameters, heatmap data, and a neighborhood robustness score
- Robustness uses the locked thresholds `Sharpe > 0.5` and `>= 70%` passing neighbors
- `--skip-stability` bypasses the analysis without affecting the rest of optimize, and standard optimize only runs stability when explicitly requested
- The combined Phase 6 regression suite passes and protects the additive artifact contracts

</success_criteria>

<output>
After completion, create `.planning/phases/06-research-integrity-framework/06-04-SUMMARY.md`
</output>
