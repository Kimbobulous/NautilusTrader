---
phase: 06-research-integrity-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [configs/settings.toml, src/mgc_bt/config.py, src/mgc_bt/cli.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/export.py, src/mgc_bt/validation/preflight.py, tests/test_cli.py, tests/test_config.py, tests/test_optimization_results.py]
autonomous: true
requirements: [INT-04]
user_setup: []
must_haves:
  truths:
    - "Default `mgc_bt optimize` preserves the existing in-sample plus holdout flow when no new flags are passed."
    - "The protected final six-month test window stays out of parameter selection and only runs when `mgc_bt optimize --walk-forward --final-test` is explicitly requested."
    - "New `[walk_forward]` and `[monte_carlo]` TOML sections load through typed config objects and fail fast on invalid ranges."
  artifacts:
    - path: "configs/settings.toml"
      provides: "Phase 6 default research-integrity settings"
      contains: "[walk_forward]"
    - path: "src/mgc_bt/config.py"
      provides: "Typed walk-forward and monte-carlo config validation"
      contains: "class WalkForwardConfig"
    - path: "src/mgc_bt/cli.py"
      provides: "Backward-compatible optimize flags"
      contains: "--walk-forward"
  key_links:
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/optimization/study.py"
      via: "run_optimization keyword arguments"
      pattern: "run_optimization\\("
    - from: "src/mgc_bt/config.py"
      to: "configs/settings.toml"
      via: "load_settings parses dedicated sections"
      pattern: "walk_forward|monte_carlo"
---

<objective>
Create the Phase 6 foundations that keep the current optimize path unchanged by default while adding the protected train/validate/test contract, hidden final-test gating, and typed Phase 6 settings.

Purpose: INT-04 is the leakage-prevention guardrail for every later Phase 6 feature.
Output: Config dataclasses, CLI flags, optimization entry-point options, and regression tests that prove the old optimize path is still intact.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-research-integrity-framework/06-CONTEXT.md
@.planning/phases/06-research-integrity-framework/06-RESEARCH.md
@configs/settings.toml
@src/mgc_bt/config.py
@src/mgc_bt/cli.py
@src/mgc_bt/optimization/study.py
@src/mgc_bt/optimization/results.py
@src/mgc_bt/optimization/export.py
@src/mgc_bt/validation/preflight.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed Phase 6 settings and validation for protected research windows</name>
  <files>configs/settings.toml, src/mgc_bt/config.py</files>
  <read_first>configs/settings.toml, src/mgc_bt/config.py, .planning/phases/06-research-integrity-framework/06-CONTEXT.md, .planning/phases/06-research-integrity-framework/06-RESEARCH.md</read_first>
  <action>Add `WalkForwardConfig` and `MonteCarloConfig` dataclasses to `src/mgc_bt/config.py`, attach them to `Settings`, and load them from new top-level TOML sections `[walk_forward]` and `[monte_carlo]`. Populate `configs/settings.toml` with these exact defaults: `[walk_forward] train_months = 12`, `validation_months = 3`, `test_months = 3`, `step_months = 3`, `validation_top_n = 5`, `min_training_bars = 50000`, `min_test_trades = 10`, `final_test_months = 6`, `runtime_warning_minutes = 30`; `[monte_carlo] simulations = 1000`, `confidence_level = 0.95`, `percentile_points = [5, 25, 50, 75, 95]`, `random_seed_offset = 10000`. Validate all Phase 6 fields at load time: every month-count and threshold must be `> 0`, `validation_top_n >= 1`, `confidence_level` must be between `0` and `1`, `percentile_points` must stay sorted and within `0..100`, and `final_test_months` must stay exactly `6` so the hidden test window remains locked to the Phase 6 decision.</action>
  <verify>uv run pytest tests/test_config.py -q</verify>
  <acceptance_criteria>
    - `configs/settings.toml` contains `[walk_forward]` plus `train_months = 12`, `validation_months = 3`, `test_months = 3`, `step_months = 3`, `min_training_bars = 50000`, `min_test_trades = 10`, and `final_test_months = 6`
    - `configs/settings.toml` contains `[monte_carlo]` plus `simulations = 1000` and `percentile_points = [5, 25, 50, 75, 95]`
    - `src/mgc_bt/config.py` contains `class WalkForwardConfig` and `class MonteCarloConfig`
    - `src/mgc_bt/config.py` validates `confidence_level` and `final_test_months`
  </acceptance_criteria>
  <done>Phase 6 settings load through typed config objects and invalid Phase 6 values raise `ConfigError` before any backtest work starts.</done>
</task>

<task type="auto">
  <name>Task 2: Extend optimize with backward-compatible flags and final-test gating</name>
  <files>src/mgc_bt/cli.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/export.py, src/mgc_bt/validation/preflight.py</files>
  <read_first>src/mgc_bt/cli.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/export.py, src/mgc_bt/validation/preflight.py, .planning/phases/06-research-integrity-framework/06-CONTEXT.md</read_first>
  <action>Add these exact `optimize` flags in `src/mgc_bt/cli.py`: `--walk-forward`, `--final-test`, `--monte-carlo`, `--stability`, `--skip-monte-carlo`, and `--skip-stability`. Extend `run_optimization(...)` in `src/mgc_bt/optimization/study.py` to accept matching keyword arguments and keep the existing single-study flow byte-for-byte equivalent whenever `walk_forward`, `final_test`, `monte_carlo`, and `stability` are all false. In that default no-flag path, do not create `walk_forward/`, `monte_carlo/`, or `stability/` directories. For non-walk-forward runs, `--monte-carlo` and `--stability` explicitly opt into those analyses; for walk-forward runs, Monte Carlo and stability are enabled automatically unless their matching skip flags are passed. Inside `study.py`, create the protected split contract used by later plans: reserve the final six months of the available data as `final_test_window`, leave it untouched unless `final_test=True`, and reject `--final-test` without `--walk-forward` by raising `CLIError(\"--final-test requires --walk-forward so the protected six-month test window stays hidden from the default optimize flow.\")`. Before any final-test execution, print the exact warning `Warning: final test window will be evaluated and can no longer be treated as untouched.`. Update `write_optimization_run_config` and `write_optimization_summary_json` so Phase 6 metadata is additive only when flags are enabled: include `analysis_flags`, `walk_forward_enabled`, `final_test_requested`, `monte_carlo_enabled`, and `stability_enabled` for flagged runs, but do not remove or rename any existing default-run keys. Keep preflight backward-compatible by continuing to validate the existing in-sample and holdout dates and by allowing the new flags to ride on the existing optimize command instead of creating a new command or a new storage root.</action>
  <verify>uv run pytest tests/test_cli.py tests/test_optimization_results.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/cli.py` contains `--walk-forward`, `--final-test`, `--monte-carlo`, `--stability`, `--skip-monte-carlo`, and `--skip-stability`
    - `src/mgc_bt/optimization/study.py` rejects `--final-test` without `--walk-forward`
    - `src/mgc_bt/optimization/study.py` contains the exact warning string `Warning: final test window will be evaluated and can no longer be treated as untouched.`
    - `src/mgc_bt/optimization/results.py` still writes `optimization_summary.json`, `run_config.toml`, and `manifest.json` for the default optimize path
  </acceptance_criteria>
  <done>The optimize entry point can accept Phase 6 flags without changing the current default run behavior or exposing the final six-month test window accidentally.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for config loading, CLI parsing, and untouched default optimize behavior</name>
  <files>tests/test_cli.py, tests/test_config.py, tests/test_optimization_results.py</files>
  <read_first>tests/test_cli.py, tests/test_config.py, tests/test_optimization_results.py, src/mgc_bt/cli.py, src/mgc_bt/config.py, src/mgc_bt/optimization/study.py</read_first>
  <action>Extend `tests/test_config.py` with failing cases for invalid `[walk_forward]` and `[monte_carlo]` values plus one success case that asserts the new defaults load from `configs/settings.toml`. Extend `tests/test_cli.py` so `build_parser()` recognizes the six new flags and `main([...])` proves `--final-test` without `--walk-forward` exits with status `2`. Also cover that `--monte-carlo` and `--stability` can opt in analysis on non-walk-forward runs while the no-flag path remains unchanged. Extend `tests/test_optimization_results.py` with a no-flag optimization run that asserts the current artifact set still contains only the existing root files (`ranked_results.csv`, `optimization_summary.json`, `run_config.toml`, `failed_trials.json`, `manifest.json`, `best_run/`, `top_10/`, and optional `latest/`) and that no `walk_forward/`, `monte_carlo/`, or `stability/` directories are created unless flags later request them.</action>
  <verify>uv run pytest tests/test_config.py tests/test_cli.py tests/test_optimization_results.py -q</verify>
  <acceptance_criteria>
    - `tests/test_cli.py` asserts `--final-test` requires `--walk-forward`
    - `tests/test_config.py` covers invalid Phase 6 config values
    - `tests/test_optimization_results.py` asserts a no-flag optimize run does not create `walk_forward`, `monte_carlo`, or `stability` artifact directories
  </acceptance_criteria>
  <done>The old optimize path stays protected by tests before any walk-forward, Monte Carlo, or stability behavior is layered on top.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_config.py tests/test_cli.py tests/test_optimization_results.py -q`
- [ ] `uv run python -m mgc_bt --config configs/settings.toml optimize --help` shows the four new flags on the existing `optimize` command
- [ ] A no-flag optimization test still passes without creating `walk_forward/`, `monte_carlo/`, or `stability/`
</verification>

<success_criteria>

- All tasks completed
- `mgc_bt optimize` remains backward-compatible when no Phase 6 flags are passed
- The hidden final six-month test window is gated behind `--walk-forward --final-test`
- Typed `[walk_forward]` and `[monte_carlo]` sections exist and validate at load time
- Regression tests prove the current optimize output contract is still intact

</success_criteria>

<output>
After completion, create `.planning/phases/06-research-integrity-framework/06-01-SUMMARY.md`
</output>
