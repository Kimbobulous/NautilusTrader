---
phase: 06-research-integrity-framework
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified: [src/mgc_bt/optimization/monte_carlo.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py, tests/test_cli.py, tests/test_optimization_results.py, tests/test_monte_carlo.py]
autonomous: true
requirements: [INT-02]
user_setup: []
must_haves:
  truths:
    - "Optimization can evaluate edge-versus-luck with both trade-order permutation and bootstrap resampling without changing the default no-flag optimize path."
    - "Monte Carlo analysis uses realized trade logs rather than rerunning the strategy."
    - "Monte Carlo output is deterministic under a fixed seed and persists explicit p-values, percentiles, and pass/fail verdicts."
  artifacts:
    - path: "src/mgc_bt/optimization/monte_carlo.py"
      provides: "Permutation and bootstrap analysis from trade logs"
      contains: "run_monte_carlo_analysis"
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Monte Carlo artifact writers"
      contains: "permutation_summary.json"
    - path: "tests/test_monte_carlo.py"
      provides: "Deterministic Monte Carlo regression coverage"
      contains: "bootstrap"
  key_links:
    - from: "src/mgc_bt/optimization/study.py"
      to: "src/mgc_bt/optimization/monte_carlo.py"
      via: "post-optimization analysis hook"
      pattern: "monte_carlo"
    - from: "src/mgc_bt/optimization/monte_carlo.py"
      to: "src/mgc_bt/backtest/results.py"
      via: "trade_log-derived realized PnL sequence"
      pattern: "trade_log"
---

<objective>
Implement deterministic Monte Carlo analysis on realized trade logs so the optimization workflow can distinguish edge from favorable sequencing luck.

Purpose: INT-02 is the statistical hardening layer that turns raw backtest outcomes into evidence about robustness.
Output: Monte Carlo analyzers, persisted permutation and bootstrap artifacts, and tests that lock the exact result schema.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-research-integrity-framework/06-CONTEXT.md
@.planning/phases/06-research-integrity-framework/06-RESEARCH.md
@src/mgc_bt/backtest/results.py
@src/mgc_bt/optimization/study.py
@src/mgc_bt/optimization/results.py
@src/mgc_bt/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic trade-order permutation and bootstrap resampling</name>
  <files>src/mgc_bt/optimization/monte_carlo.py</files>
  <read_first>src/mgc_bt/backtest/results.py, src/mgc_bt/optimization/study.py, .planning/phases/06-research-integrity-framework/06-CONTEXT.md, .planning/phases/06-research-integrity-framework/06-RESEARCH.md</read_first>
  <action>Create `src/mgc_bt/optimization/monte_carlo.py` with a single entry point `run_monte_carlo_analysis(trade_log, *, simulations, seed, percentiles, confidence_level)` that never reruns Nautilus. Method 1 must shuffle realized trade order exactly `1000` times by default and recompute cumulative equity, final PnL, and Sharpe from the permuted realized PnL sequence. Method 2 must bootstrap-resample the trade list with replacement exactly `1000` times, with each synthetic path using `len(trade_log)` draws. Use a deterministic RNG seeded from `settings.optimization.seed + settings.monte_carlo.random_seed_offset`. Return both method summaries plus a fan-chart-ready confidence-band table keyed by trade index. If the input trade log has fewer than `2` trades, return `status = "skipped"` with `skipped_reason = "insufficient_trades"` rather than fabricating statistics.</action>
  <verify>uv run pytest tests/test_monte_carlo.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/optimization/monte_carlo.py` contains `run_monte_carlo_analysis`
    - `src/mgc_bt/optimization/monte_carlo.py` contains both `permutation` and `bootstrap`
    - `src/mgc_bt/optimization/monte_carlo.py` derives its seed from `random_seed_offset`
    - `src/mgc_bt/optimization/monte_carlo.py` returns `skipped_reason = "insufficient_trades"` for tiny trade logs
  </acceptance_criteria>
  <done>The project has deterministic Monte Carlo math for both required methods without introducing another backtest loop.</done>
</task>

<task type="auto">
  <name>Task 2: Run Monte Carlo after optimization and persist the exact Phase 6 artifact set</name>
  <files>src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py</files>
  <read_first>src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py, src/mgc_bt/optimization/monte_carlo.py, .planning/phases/06-research-integrity-framework/06-CONTEXT.md</read_first>
  <action>Wire the Monte Carlo hook into `src/mgc_bt/optimization/study.py` so it runs in exactly two cases: automatically for walk-forward runs unless `skip_monte_carlo=True`, and for standard non-walk-forward optimization only when `monte_carlo=True` was explicitly requested. The default no-flag optimize path must not execute Monte Carlo or create `monte_carlo/` artifacts. Use the most trustworthy available trade ledger: concatenated walk-forward out-of-sample `trade_log` when walk-forward ran, otherwise `holdout_result["trade_log"]`, otherwise `in_sample_result["trade_log"]` for edge cases with no holdout export. Persist outputs under `results/optimization/<timestamp>/monte_carlo/` with exactly these files: `permutation_summary.json`, `bootstrap_summary.json`, `equity_confidence_bands.csv`, and `monte_carlo_summary.json`. Every JSON file must be additive and machine-readable with `schema_version = 1`, `run_type = "optimize"`, `analysis_type = "monte_carlo"`, `method`, `simulations`, `sample_size`, `p_value`, `percentiles`, `pass_95`, and `status`. Update `_render_optimization_summary` in `src/mgc_bt/cli.py` to append `Monte Carlo summary: <path>` when the analysis ran, `Monte Carlo: skipped by flag` when `--skip-monte-carlo` was passed, and `Monte Carlo: not requested` for the unchanged default optimize path.</action>
  <verify>uv run pytest tests/test_cli.py tests/test_optimization_results.py tests/test_monte_carlo.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/optimization/results.py` contains `permutation_summary.json`, `bootstrap_summary.json`, `equity_confidence_bands.csv`, and `monte_carlo_summary.json`
    - Monte Carlo JSON payloads include `schema_version`, `analysis_type`, `p_value`, `percentiles`, and `pass_95`
    - `src/mgc_bt/cli.py` prints `Monte Carlo summary:` and `Monte Carlo: skipped by flag`
    - `src/mgc_bt/optimization/study.py` chooses walk-forward OOS trades before holdout trades when both are available
  </acceptance_criteria>
  <done>Optimization runs can persist reproducible Monte Carlo evidence while leaving the default no-flag optimize path unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic Monte Carlo regression coverage and schema assertions</name>
  <files>tests/test_cli.py, tests/test_optimization_results.py, tests/test_monte_carlo.py</files>
  <read_first>tests/test_cli.py, tests/test_optimization_results.py, src/mgc_bt/optimization/monte_carlo.py, src/mgc_bt/optimization/results.py</read_first>
  <action>Create `tests/test_monte_carlo.py` with deterministic fixed-seed fixtures that assert both Monte Carlo methods produce stable percentile sets, p-values, and `pass_95` verdicts from a known trade ledger. Extend `tests/test_optimization_results.py` so a monkeypatched optimize run creates the exact `monte_carlo/` filenames only for walk-forward or explicit `--monte-carlo` runs, and so `monte_carlo_summary.json` records `schema_version = 1` and `analysis_type = "monte_carlo"`. Extend `tests/test_cli.py` to prove `--skip-monte-carlo` bypasses artifact creation without affecting the rest of the optimize summary, and that no-flag optimize leaves Monte Carlo unrequested. Keep the new tests additive so the existing 47-test baseline remains valid.</action>
  <verify>uv run pytest tests/test_cli.py tests/test_optimization_results.py tests/test_monte_carlo.py -q</verify>
  <acceptance_criteria>
    - `tests/test_monte_carlo.py` covers both permutation and bootstrap paths
    - `tests/test_optimization_results.py` asserts the exact `monte_carlo/` filenames
    - `tests/test_cli.py` covers `--skip-monte-carlo` and the unchanged no-flag optimize path
    - At least one Monte Carlo test asserts deterministic `p_value` and `pass_95` outputs under a fixed seed
  </acceptance_criteria>
  <done>The Monte Carlo layer has repeatable tests that will catch schema drift, flag regressions, and nondeterministic math changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_cli.py tests/test_optimization_results.py tests/test_monte_carlo.py -q`
- [ ] A representative optimize run writes `monte_carlo/permutation_summary.json`, `monte_carlo/bootstrap_summary.json`, `monte_carlo/equity_confidence_bands.csv`, and `monte_carlo/monte_carlo_summary.json`
- [ ] Monte Carlo analysis runs from realized `trade_log` data without calling `run_backtest` again
</verification>

<success_criteria>

- All tasks completed
- Both required Monte Carlo methods exist and are reproducible under fixed seeds
- Monte Carlo runs automatically for walk-forward, and for normal optimize only when explicitly requested
- Artifact JSON includes p-values, percentile bands, and 95% pass/fail verdicts
- Regression coverage protects the Monte Carlo schema and skip behavior

</success_criteria>

<output>
After completion, create `.planning/phases/06-research-integrity-framework/06-03-SUMMARY.md`
</output>
