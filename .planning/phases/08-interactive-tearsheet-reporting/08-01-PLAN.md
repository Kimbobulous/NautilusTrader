---
phase: 08-interactive-tearsheet-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/mgc_bt/reporting/__init__.py, src/mgc_bt/reporting/loaders.py, src/mgc_bt/reporting/tearsheet.py, tests/test_tearsheet.py]
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03]
user_setup: []
must_haves:
  truths:
    - "Phase 8 uses a shared filesystem-first tearsheet foundation instead of rerunning analytics or backtests."
    - "Missing optional inputs render explicit unavailable notices rather than silently disappearing."
    - "The HTML output is fully self-contained and does not rely on any CDN or external runtime."
  artifacts:
    - path: "src/mgc_bt/reporting/loaders.py"
      provides: "Filesystem readers and section-availability contracts for backtest and optimization run folders"
      contains: "Section unavailable"
    - path: "src/mgc_bt/reporting/tearsheet.py"
      provides: "Shared HTML/Plotly document assembly and dark-theme shell"
      contains: "tearsheet.html"
    - path: "tests/test_tearsheet.py"
      provides: "Phase 8 foundation coverage for self-contained HTML and missing-section behavior"
      contains: "plotly"
  key_links:
    - from: "src/mgc_bt/reporting/loaders.py"
      to: "src/mgc_bt/backtest/artifacts.py"
      via: "backtest run folders are parsed from persisted artifacts"
      pattern: "summary.json|analytics"
    - from: "src/mgc_bt/reporting/loaders.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "optimization run folders are parsed from persisted artifacts"
      pattern: "walk_forward|monte_carlo|stability|best_run"
    - from: "src/mgc_bt/reporting/tearsheet.py"
      to: "src/mgc_bt/reporting/loaders.py"
      via: "renderer consumes discovered files instead of recomputing analytics"
      pattern: "availability|missing"
---

<objective>
Build the shared tearsheet foundation: filesystem loaders, explicit missing-section contracts, and a self-contained dark-theme HTML shell.

Purpose: Phase 8 needs one reusable reporting base that both `backtest` and `optimize` can use without diverging or redoing earlier analytics work.
Output: Shared run-folder loaders, availability/missing-section modeling, self-contained Plotly HTML assembly, and foundation tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-interactive-tearsheet-reporting/08-CONTEXT.md
@.planning/phases/08-interactive-tearsheet-reporting/08-RESEARCH.md
@.planning/phases/08-interactive-tearsheet-reporting/08-VALIDATION.md
@src/mgc_bt/backtest/artifacts.py
@src/mgc_bt/backtest/analytics.py
@src/mgc_bt/optimization/results.py
@src/mgc_bt/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build filesystem-first backtest and optimization tearsheet loaders</name>
  <files>src/mgc_bt/reporting/__init__.py, src/mgc_bt/reporting/loaders.py</files>
  <read_first>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/backtest/analytics.py, src/mgc_bt/optimization/results.py, .planning/phases/08-interactive-tearsheet-reporting/08-CONTEXT.md, .planning/phases/08-interactive-tearsheet-reporting/08-RESEARCH.md</read_first>
  <action>Create `src/mgc_bt/reporting/loaders.py` with explicit loader models for backtest and optimization run folders. The loader contract must read persisted files from disk only, starting from `summary.json`, `analytics/`, `best_run/`, `walk_forward/`, `monte_carlo/`, and `stability/`, and it must never call `run_backtest`, `run_optimization`, or recompute Phase 6/7 analytics. Return structured section inputs plus explicit unavailable notices like `Section unavailable - audit_log.csv not found` whenever optional files are missing. Optimization loaders must treat `best_run/` as the shared-section base and the parent run directory as the source for optimization-only inputs.</action>
  <verify>uv run pytest tests/test_tearsheet.py -q -k "loader or missing"</verify>
  <acceptance_criteria>
    - `src/mgc_bt/reporting/loaders.py` reads run folders from persisted files only
    - loader notices use the exact prefix `Section unavailable - `
    - optimization loading uses `best_run/` for shared backtest sections and root optimization artifacts for extra sections
    - no loader path calls `run_backtest` or reruns Phase 7 analytics generation
  </acceptance_criteria>
  <done>The project can discover what a tearsheet can render from the filesystem alone.</done>
</task>

<task type="auto">
  <name>Task 2: Build the shared self-contained HTML shell and dark-theme section chrome</name>
  <files>src/mgc_bt/reporting/tearsheet.py</files>
  <read_first>.planning/phases/08-interactive-tearsheet-reporting/08-CONTEXT.md, .planning/phases/08-interactive-tearsheet-reporting/08-RESEARCH.md, src/mgc_bt/backtest/plotting.py</read_first>
  <action>Create `src/mgc_bt/reporting/tearsheet.py` with a shared document generator that writes `tearsheet.html` as one scrollable page. The shell must embed Plotly locally with no CDN usage, implement the locked dark trading aesthetic, render section collapse/expand toggles with minimal vanilla JavaScript, and keep headline stat cards plus the primary equity/drawdown region above the fold. Use `plotly.io.to_html()` with `include_plotlyjs=True` for the first embedded chart in the document and `include_plotlyjs=False` for all subsequent charts so the Plotly JS bundle is embedded exactly once. The document generator must accept shared loader output and render section-level unavailable notices instead of dropping missing optional content, and it should keep the final `tearsheet.html` under the locked `10 MB` target whenever practical.</action>
  <verify>uv run pytest tests/test_tearsheet.py -q -k "html or self_contained"</verify>
  <acceptance_criteria>
    - `src/mgc_bt/reporting/tearsheet.py` writes `tearsheet.html`
    - generated HTML contains no `cdn.plot.ly`, `https://`, or external script tags for Plotly
    - Plotly JS is embedded exactly once via `include_plotlyjs=True` on the first chart and `include_plotlyjs=False` on later charts
    - the shell includes section toggle support with minimal inline JavaScript
    - missing optional inputs render visible unavailable notices instead of skipping sections silently
  </acceptance_criteria>
  <done>The project has a reusable tearsheet shell that is self-contained and consistent with the locked visual contract.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the shared tearsheet foundation with artifact-driven tests</name>
  <files>tests/test_tearsheet.py</files>
  <read_first>tests/test_backtest_artifacts.py, tests/test_optimization_results.py, src/mgc_bt/reporting/loaders.py, src/mgc_bt/reporting/tearsheet.py</read_first>
  <action>Create `tests/test_tearsheet.py` with filesystem-fixture coverage for both backtest-like and optimization-like run directories. Assert that the foundation loader can read existing Phase 7 and Phase 6 artifact layouts, that missing optional inputs produce explicit unavailable strings, and that the rendered HTML stays self-contained. Keep the tests additive and aligned with current run-folder contracts rather than introducing a parallel artifact shape.</action>
  <verify>uv run pytest tests/test_tearsheet.py -q</verify>
  <acceptance_criteria>
    - `tests/test_tearsheet.py` covers both backtest and optimization run-folder discovery
    - at least one test asserts the exact unavailable prefix `Section unavailable - `
    - at least one test asserts the rendered HTML is self-contained and does not use a CDN
    - tests consume realistic run-folder fixtures rather than a synthetic parallel artifact layout
  </acceptance_criteria>
  <done>The tearsheet foundation is protected against drift before section-specific rendering starts.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Filesystem artifacts under run directories into tearsheet loader parsing
- Embedded Plotly data and inline HTML generation into the final self-contained report
- Missing optional files into section availability and warning text
</trust_boundaries>
<threats>
- threat_id: T-08-01
  category: Integrity
  component: tearsheet data source
  disposition: mitigate
  mitigation_plan: Force the loader to consume persisted files only so the report cannot drift from the canonical backtest and optimization outputs.
- threat_id: T-08-02
  category: Integrity
  component: missing-data handling
  disposition: mitigate
  mitigation_plan: Render explicit unavailable notices whenever optional inputs are missing so the report does not create false confidence.
- threat_id: T-08-03
  category: Availability
  component: offline HTML report
  disposition: mitigate
  mitigation_plan: Embed Plotly and section logic locally with no CDN dependency so the tearsheet opens offline on any browser.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_tearsheet.py -q`
- [ ] A rendered tearsheet contains no external CDN references
- [ ] Missing optional files render visible `Section unavailable - ...` notices
- [ ] The loader consumes persisted run-folder files without rerunning backtests or analytics
</verification>

<success_criteria>

- All tasks completed
- Shared tearsheet foundation can read both backtest and optimization run folders from disk
- HTML output is self-contained and offline-friendly
- Missing optional data is surfaced explicitly instead of being hidden

</success_criteria>

<output>
After completion, create `.planning/phases/08-interactive-tearsheet-reporting/08-01-SUMMARY.md`
</output>
