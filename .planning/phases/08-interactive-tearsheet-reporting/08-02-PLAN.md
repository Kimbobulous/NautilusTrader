---
phase: 08-interactive-tearsheet-reporting
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified: [src/mgc_bt/reporting/tearsheet.py, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/cli.py, tests/test_backtest_artifacts.py, tests/test_tearsheet.py, tests/test_cli.py]
autonomous: true
requirements: [VIZ-01, VIZ-03]
user_setup: []
must_haves:
  truths:
    - "Every backtest run automatically emits `tearsheet.html` from persisted artifacts."
    - "Backtest tearsheet generation is non-blocking and never prevents the canonical result bundle from being saved."
    - "The backtest tearsheet renders the locked shared sections in executive-summary-first order."
  artifacts:
    - path: "src/mgc_bt/reporting/tearsheet.py"
      provides: "Backtest shared-section rendering and write helper"
      contains: "Executive summary"
    - path: "src/mgc_bt/backtest/artifacts.py"
      provides: "Backtest tearsheet attachment and manifest inclusion"
      contains: "tearsheet.html"
    - path: "tests/test_tearsheet.py"
      provides: "Backtest tearsheet rendering coverage"
      contains: "audit diagnostics"
  key_links:
    - from: "src/mgc_bt/backtest/artifacts.py"
      to: "src/mgc_bt/reporting/tearsheet.py"
      via: "backtest artifact pipeline attaches tearsheet after analytics"
      pattern: "tearsheet"
    - from: "src/mgc_bt/reporting/tearsheet.py"
      to: "src/mgc_bt/backtest/analytics.py"
      via: "drawdown, breakdown, and audit sections consume persisted analytics"
      pattern: "analytics"
---

<objective>
Render the shared backtest tearsheet sections and attach `tearsheet.html` automatically to the backtest artifact pipeline.

Purpose: VIZ-01 is satisfied only when normal backtest runs emit a polished, self-contained report without any extra command.
Output: Backtest section rendering, automatic attachment in the artifact pipeline, manifest inclusion, and non-blocking tests.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-interactive-tearsheet-reporting/08-CONTEXT.md
@.planning/phases/08-interactive-tearsheet-reporting/08-RESEARCH.md
@.planning/phases/08-interactive-tearsheet-reporting/08-VALIDATION.md
@src/mgc_bt/backtest/artifacts.py
@src/mgc_bt/backtest/analytics.py
@src/mgc_bt/backtest/results.py
@src/mgc_bt/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the shared backtest tearsheet sections from persisted artifacts</name>
  <files>src/mgc_bt/reporting/tearsheet.py</files>
  <read_first>src/mgc_bt/reporting/loaders.py, src/mgc_bt/backtest/analytics.py, src/mgc_bt/backtest/results.py, .planning/phases/08-interactive-tearsheet-reporting/08-CONTEXT.md</read_first>
  <action>Extend the shared tearsheet renderer with the always-present backtest sections in the locked order: header, executive summary, equity curve, drawdown analysis, trade analysis, performance breakdowns, audit diagnostics, and footer. Use `summary.json`, `analytics/drawdown_episodes.csv`, `analytics/underwater_curve.csv`, `analytics/breakdowns/*.csv`, and `analytics/audit_log.csv` as the source of truth. Above the fold, render strategy name, instrument, date range, generated timestamp, the six locked stat cards, and the primary equity-plus-drawdown view. For audit diagnostics, summarize `audit_log.csv` into compact charts such as rejection-reason counts and signal-quality-over-time instead of embedding raw audit rows.</action>
  <verify>uv run pytest tests/test_tearsheet.py -q -k "backtest_render"</verify>
  <acceptance_criteria>
    - backtest tearsheet sections follow the locked order from Phase 8 context
    - stat cards include `Total PnL`, `Sharpe ratio`, `Win rate`, `Max drawdown`, `Total trades`, and `Profit factor`
    - audit diagnostics summarize `audit_log.csv` instead of embedding the raw file contents
    - the footer includes run-config summary and relevant file paths
  </acceptance_criteria>
  <done>The shared tearsheet framework can tell the backtest story from profitability through diagnostics in one report.</done>
</task>

<task type="auto">
  <name>Task 2: Attach backtest tearsheet generation non-blockingly and update manifests</name>
  <files>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/cli.py</files>
  <read_first>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/reporting/tearsheet.py, src/mgc_bt/cli.py, .planning/phases/08-interactive-tearsheet-reporting/08-RESEARCH.md</read_first>
  <action>Wire backtest tearsheet generation into `src/mgc_bt/backtest/artifacts.py` after the core bundle and analytics are written but before the final manifest pass. Write the file as `tearsheet.html` in the canonical run directory, include it in `manifest.json`, and allow `latest/` refresh to pick it up automatically by copying the canonical folder as-is. If tearsheet generation fails, print a clear warning to stderr and continue returning the normal artifact paths. Update CLI summary output only additively if needed; do not remove the existing summary lines.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py tests/test_cli.py -q -k "tearsheet or backtest"</verify>
  <acceptance_criteria>
    - backtest runs write `tearsheet.html` in the run folder
    - `manifest.json` lists `tearsheet.html`
    - tearsheet failures warn and continue without aborting the run
    - existing CLI summary lines remain present
  </acceptance_criteria>
  <done>Backtest tearsheets are attached automatically without making the reporting step operationally fragile.</done>
</task>

<task type="auto">
  <name>Task 3: Add backtest tearsheet regression coverage for manifest and failure behavior</name>
  <files>tests/test_backtest_artifacts.py, tests/test_tearsheet.py, tests/test_cli.py</files>
  <read_first>tests/test_backtest_artifacts.py, tests/test_tearsheet.py, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/reporting/tearsheet.py</read_first>
  <action>Extend backtest-side tests so they assert `tearsheet.html` exists, is included in `manifest.json`, and remains present in refreshed `latest/`. Add a simulated tearsheet failure test that proves core bundle persistence still succeeds and a warning is emitted. Keep assertions focused on contract markers such as section headers, unavailable notices, and manifest inclusion instead of brittle full-HTML snapshots.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py tests/test_tearsheet.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - tests assert `tearsheet.html` exists in both canonical backtest output and refreshed `latest/`
    - tests assert `manifest.json` includes `tearsheet.html`
    - at least one test proves tearsheet failures do not block backtest result persistence
    - tests avoid full-document snapshot brittleness and instead assert stable contract markers
  </acceptance_criteria>
  <done>Backtest tearsheet behavior is locked in with manifest and failure-path coverage.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Backtest run-folder artifacts into section rendering
- Generated HTML into manifest and latest-copy flow
- Audit and analytics summaries into visual diagnostics
</trust_boundaries>
<threats>
- threat_id: T-08-02
  category: Integrity
  component: backtest report content
  disposition: mitigate
  mitigation_plan: Render all backtest sections from canonical persisted files so the tearsheet cannot disagree with the saved artifacts.
- threat_id: T-08-04
  category: Availability
  component: backtest artifact pipeline
  disposition: mitigate
  mitigation_plan: Make tearsheet generation best-effort with warnings only so report failures never block core backtest persistence.
- threat_id: T-08-05
  category: Integrity
  component: audit diagnostics
  disposition: mitigate
  mitigation_plan: Summarize the audit log into compact diagnostics rather than embedding raw rows, preserving both usability and the original on-disk source of truth.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_backtest_artifacts.py tests/test_tearsheet.py tests/test_cli.py -q`
- [ ] A representative backtest run writes `tearsheet.html` and includes it in `manifest.json`
- [ ] Simulated tearsheet failures still return the core backtest bundle
</verification>

<success_criteria>

- All tasks completed
- Backtest runs automatically emit `tearsheet.html`
- The report includes the locked shared sections and stays self-contained
- Failures remain non-blocking and manifest-aware

</success_criteria>

<output>
After completion, create `.planning/phases/08-interactive-tearsheet-reporting/08-02-SUMMARY.md`
</output>
