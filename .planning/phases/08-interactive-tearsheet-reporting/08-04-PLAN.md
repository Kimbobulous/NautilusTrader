---
phase: 08-interactive-tearsheet-reporting
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified: [src/mgc_bt/reporting/tearsheet.py, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py, tests/test_backtest_artifacts.py, tests/test_optimization_results.py, tests/test_tearsheet.py, tests/test_cli.py]
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03]
user_setup: []
must_haves:
  truths:
    - "Tearsheet generation is best-effort, explicit about gaps, and stable across both backtest and optimize pipelines."
    - "Section-level notices, manifest updates, and `latest/` copies stay consistent after the Phase 8 integration."
    - "Phase 8 introduces no separate tearsheet command and does not regress existing result contracts."
  artifacts:
    - path: "src/mgc_bt/reporting/tearsheet.py"
      provides: "Final section-availability and helper stabilization for both run types"
      contains: "Section unavailable"
    - path: "src/mgc_bt/backtest/artifacts.py"
      provides: "Final backtest manifest and failure-path integration"
      contains: "Warning: tearsheet generation failed"
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Final optimization manifest and failure-path integration"
      contains: "Warning: tearsheet generation failed"
    - path: "tests/test_tearsheet.py"
      provides: "Cross-run contract and manifest regression protection"
      contains: "tearsheet.html"
  key_links:
    - from: "src/mgc_bt/backtest/artifacts.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "shared non-blocking tearsheet contract"
      pattern: "Warning: tearsheet generation failed"
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/backtest/artifacts.py"
      via: "user-visible output remains additive while artifacts gain tearsheets"
      pattern: "Run directory|Latest directory"
---

<objective>
Harden the shared tearsheet integration so both run types preserve manifest, latest-copy, and failure-path behavior while meeting the Phase 8 contract.

Purpose: Phase 8 is only complete when tearsheets behave like a stable reporting layer rather than a fragile optional add-on.
Output: Final hardening of warning text, manifest/latest integration, and broad regression coverage across both command paths.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-interactive-tearsheet-reporting/08-CONTEXT.md
@.planning/phases/08-interactive-tearsheet-reporting/08-RESEARCH.md
@.planning/phases/08-interactive-tearsheet-reporting/08-VALIDATION.md
@src/mgc_bt/backtest/artifacts.py
@src/mgc_bt/optimization/results.py
@src/mgc_bt/cli.py
@tests/test_backtest_artifacts.py
@tests/test_optimization_results.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize best-effort failure handling and explicit gap notices across both run types</name>
  <files>src/mgc_bt/reporting/tearsheet.py, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py</files>
  <read_first>src/mgc_bt/reporting/tearsheet.py, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, .planning/phases/08-interactive-tearsheet-reporting/08-CONTEXT.md</read_first>
  <action>Finalize the Phase 8 failure contract so both backtest and optimization use the same best-effort behavior. When `summary.json` is missing, log a clear warning and skip tearsheet generation entirely. When optional section inputs are missing, render `Section unavailable - ...` notices inside the document. When the renderer raises unexpectedly, emit `Warning: tearsheet generation failed ...` and continue without interrupting result persistence. Keep the warning path consistent across backtest and optimize so operational behavior is predictable.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py tests/test_optimization_results.py tests/test_tearsheet.py -q -k "warning or missing"</verify>
  <acceptance_criteria>
    - both backtest and optimization use consistent `Warning: tearsheet generation failed` messaging
    - missing `summary.json` causes a warning and tearsheet skip rather than a run failure
    - missing optional inputs render `Section unavailable - ...` notices inside the document
    - no code path converts tearsheet generation into a required success condition
  </acceptance_criteria>
  <done>Tearsheets behave predictably and safely under partial-data and failure conditions.</done>
</task>

<task type="auto">
  <name>Task 2: Lock manifest and latest-directory behavior for tearsheet outputs</name>
  <files>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py</files>
  <read_first>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py, tests/test_backtest_artifacts.py, tests/test_optimization_results.py</read_first>
  <action>Finalize manifest and `latest/` handling so `tearsheet.html` is always included in the canonical run manifest when generated and automatically appears in refreshed `latest/` copies through the existing copytree flow. Do not add a separate tearsheet copy path or special-case `latest`. Keep CLI output additive; if you expose tearsheet paths, do so without removing the existing path/status lines established in earlier phases.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py tests/test_optimization_results.py tests/test_cli.py -q -k "manifest or latest or tearsheet"</verify>
  <acceptance_criteria>
    - `manifest.json` includes `tearsheet.html` whenever the file is generated
    - refreshed `latest/` directories contain `tearsheet.html`
    - no separate `latest` tearsheet logic is introduced
    - existing CLI summary lines remain intact
  </acceptance_criteria>
  <done>Tearsheet files travel through manifests and `latest/` exactly like the rest of the result bundle.</done>
</task>

<task type="auto">
  <name>Task 3: Add cross-run regression coverage and full Phase 8 verification</name>
  <files>tests/test_backtest_artifacts.py, tests/test_optimization_results.py, tests/test_tearsheet.py, tests/test_cli.py</files>
  <read_first>tests/test_backtest_artifacts.py, tests/test_optimization_results.py, tests/test_tearsheet.py, tests/test_cli.py, .planning/phases/08-interactive-tearsheet-reporting/08-VALIDATION.md</read_first>
  <action>Expand the Phase 8 tests into one final regression net covering both run types: self-contained HTML, visible unavailable notices, manifest inclusion, refreshed `latest/`, and non-blocking failure behavior. Add contract assertions that no separate tearsheet command was introduced and that backtest/optimize still complete with the existing artifact contracts intact. Finish by running the full suite so Phase 8 proves it did not regress the 67-test post-Phase-7 baseline.</action>
  <verify>uv run pytest -q</verify>
  <acceptance_criteria>
    - tests cover both backtest and optimization tearsheet outputs
    - tests assert self-contained HTML, manifest inclusion, and `latest/` presence
    - tests assert tearsheet failures remain non-blocking
    - full-suite verification is part of the plan completion gate
  </acceptance_criteria>
  <done>Phase 8 ends with broad regression coverage and a verified non-regressing tearsheet contract.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Generated tearsheet files into canonical run manifests and latest copies
- Renderer failure paths into the core result-persistence flow
- User-visible CLI output into operational expectations
</trust_boundaries>
<threats>
- threat_id: T-08-09
  category: Availability
  component: report generation in artifact pipelines
  disposition: mitigate
  mitigation_plan: Standardize warning-only failure handling so the tearsheet layer can fail without blocking core result persistence.
- threat_id: T-08-10
  category: Integrity
  component: result bundle contract
  disposition: mitigate
  mitigation_plan: Keep manifests and latest copies authoritative, with `tearsheet.html` participating through the existing copy flow rather than custom side paths.
- threat_id: T-08-11
  category: Integrity
  component: workflow surface area
  disposition: mitigate
  mitigation_plan: Preserve the existing `backtest` and `optimize` command contracts and avoid adding a separate tearsheet command.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q`
- [ ] Backtest and optimize outputs include `tearsheet.html` in canonical runs and refreshed `latest/`
- [ ] Missing optional inputs render explicit unavailable notices
- [ ] Simulated tearsheet failures never block core result persistence
</verification>

<success_criteria>

- All tasks completed
- Both run types have stable, manifest-aware, non-blocking tearsheet behavior
- `tearsheet.html` participates in canonical and `latest/` result distribution
- Full-suite verification proves Phase 8 does not regress existing contracts

</success_criteria>

<output>
After completion, create `.planning/phases/08-interactive-tearsheet-reporting/08-04-SUMMARY.md`
</output>
