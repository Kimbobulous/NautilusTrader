---
phase: 08-interactive-tearsheet-reporting
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified: [src/mgc_bt/reporting/tearsheet.py, src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/study.py, tests/test_optimization_results.py, tests/test_tearsheet.py, tests/test_cli.py]
autonomous: true
requirements: [VIZ-02, VIZ-03]
user_setup: []
must_haves:
  truths:
    - "Optimization runs use the same tearsheet framework as backtests, with extra sections only when optimization artifacts exist."
    - "Optimization tearsheets read walk-forward, Monte Carlo, stability, sensitivity, and best-run analytics from disk rather than rerunning analyses."
    - "Missing optional optimization analyses render explicit unavailable sections rather than failing the tearsheet."
  artifacts:
    - path: "src/mgc_bt/reporting/tearsheet.py"
      provides: "Optimization-only tearsheet sections layered onto the shared shell"
      contains: "Walk-forward"
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Optimization tearsheet attachment and manifest inclusion"
      contains: "tearsheet.html"
    - path: "tests/test_tearsheet.py"
      provides: "Optimization tearsheet contract coverage"
      contains: "Monte Carlo"
  key_links:
    - from: "src/mgc_bt/reporting/tearsheet.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "optimization-only sections consume persisted artifacts"
      pattern: "walk_forward|monte_carlo|stability|parameter_sensitivity"
    - from: "src/mgc_bt/optimization/results.py"
      to: "src/mgc_bt/reporting/tearsheet.py"
      via: "post-results pipeline attaches optimization tearsheet"
      pattern: "tearsheet"
---

<objective>
Render optimization-only tearsheet sections and attach `tearsheet.html` automatically to optimization runs without changing the default optimize execution path.

Purpose: VIZ-02 requires optimization reports to extend the shared tearsheet rather than diverge from it, while still reflecting walk-forward, Monte Carlo, and stability research outputs when present.
Output: Optimization-only section rendering, automatic optimize attachment, and regression coverage for optional analysis visibility.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-interactive-tearsheet-reporting/08-CONTEXT.md
@.planning/phases/08-interactive-tearsheet-reporting/08-RESEARCH.md
@.planning/phases/08-interactive-tearsheet-reporting/08-VALIDATION.md
@src/mgc_bt/optimization/results.py
@src/mgc_bt/optimization/study.py
@src/mgc_bt/optimization/analytics.py
@src/mgc_bt/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optimization-only sections to the shared tearsheet</name>
  <files>src/mgc_bt/reporting/tearsheet.py</files>
  <read_first>src/mgc_bt/reporting/loaders.py, src/mgc_bt/optimization/results.py, .planning/phases/08-interactive-tearsheet-reporting/08-CONTEXT.md, .planning/phases/08-interactive-tearsheet-reporting/08-RESEARCH.md</read_first>
  <action>Extend the shared tearsheet renderer so optimization runs append an optimization section after audit diagnostics whenever optimization artifacts exist. Render the locked content from disk: parameter heatmaps and sensitivity views from `analytics/parameter_sensitivity.csv` and `stability/`, walk-forward results from `walk_forward/`, Monte Carlo fan-chart and summary from `monte_carlo/`, and ranked-result context from `ranked_results.csv` or `optimization_summary.json`. Use `best_run/` as the source for shared sections and avoid rerunning analytics or optimization logic. For any missing optional optimization artifact subtree, render an explicit unavailable section notice instead of suppressing the section.</action>
  <verify>uv run pytest tests/test_tearsheet.py -q -k "optimization_render"</verify>
  <acceptance_criteria>
    - optimization tearsheets append an optimization-only section after audit diagnostics
    - walk-forward, Monte Carlo, stability, and parameter sensitivity views are loaded from disk artifacts
    - shared sections still come from `best_run/` rather than a parallel data path
    - missing optional optimization analysis files render explicit unavailable notices
  </acceptance_criteria>
  <done>The shared tearsheet framework can represent optimization runs without becoming a second reporting system.</done>
</task>

<task type="auto">
  <name>Task 2: Attach optimization tearsheet generation and preserve optimize contracts</name>
  <files>src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/study.py, src/mgc_bt/cli.py</files>
  <read_first>src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/study.py, src/mgc_bt/reporting/tearsheet.py, src/mgc_bt/cli.py</read_first>
  <action>Attach `tearsheet.html` generation to the optimization output pipeline after best-run export and any optional analysis outputs are written, but before the final manifest pass and `latest/` refresh. Keep the default optimize behavior intact: the existing CLI path and study flow should only gain additive report generation, not new required flags or changed analysis execution. Add the tearsheet path to the manifest automatically and update CLI summary output additively if useful, without removing any Phase 4 or Phase 6 summary lines.</action>
  <verify>uv run pytest tests/test_optimization_results.py tests/test_cli.py -q -k "tearsheet or optimize"</verify>
  <acceptance_criteria>
    - optimization runs write `tearsheet.html` in the run folder
    - `manifest.json` lists `tearsheet.html`
    - no default optimize flags or existing CLI summary lines are removed
    - the optimization tearsheet is generated after optional analyses are written so it can discover them from disk
  </acceptance_criteria>
  <done>Optimization runs automatically emit a full tearsheet while preserving the existing optimize contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add optimization tearsheet tests for optional analysis visibility and non-blocking behavior</name>
  <files>tests/test_optimization_results.py, tests/test_tearsheet.py, tests/test_cli.py</files>
  <read_first>tests/test_optimization_results.py, tests/test_tearsheet.py, src/mgc_bt/optimization/results.py, src/mgc_bt/reporting/tearsheet.py</read_first>
  <action>Extend optimization-side tests to assert `tearsheet.html` exists for standard optimize and walk-forward optimize runs, that optimization-only sections appear when the corresponding `walk_forward/`, `monte_carlo/`, or `stability/` artifacts exist, and that missing optional analysis folders produce unavailable notices instead of failures. Add at least one failure-path test proving optimization result persistence still succeeds if tearsheet generation raises. Keep tests focused on stable markers and manifest inclusion rather than brittle full-document snapshots.</action>
  <verify>uv run pytest tests/test_optimization_results.py tests/test_tearsheet.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - tests assert `tearsheet.html` exists for optimization outputs
    - tests assert optimization-only sections appear when their artifact folders exist
    - tests assert missing optional optimization analyses render unavailable notices
    - at least one test proves tearsheet generation failure does not block optimization result persistence
  </acceptance_criteria>
  <done>The optimization tearsheet contract is guarded against drift, missing-data silence, and failure regressions.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Optimization artifact subtrees into optional tearsheet sections
- Best-run folder into shared section rendering
- Final manifest and latest refresh into visible report distribution
</trust_boundaries>
<threats>
- threat_id: T-08-06
  category: Integrity
  component: optimization report content
  disposition: mitigate
  mitigation_plan: Drive optimization sections from persisted `walk_forward/`, `monte_carlo/`, `stability/`, and `analytics/` outputs so the report mirrors canonical research results.
- threat_id: T-08-07
  category: Integrity
  component: shared-vs-optimization section split
  disposition: mitigate
  mitigation_plan: Reuse `best_run/` for shared sections and confine optimization-only content to additive sections after audit diagnostics.
- threat_id: T-08-08
  category: Availability
  component: optimize output pipeline
  disposition: mitigate
  mitigation_plan: Keep tearsheet generation best-effort and manifest-aware so report failures do not block study outputs.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_optimization_results.py tests/test_tearsheet.py tests/test_cli.py -q`
- [ ] A representative optimize run writes `tearsheet.html` and includes it in `manifest.json`
- [ ] Missing `walk_forward/`, `monte_carlo/`, or `stability/` inputs render explicit unavailable notices instead of failing the report
</verification>

<success_criteria>

- All tasks completed
- Optimization runs automatically emit `tearsheet.html`
- Optimization-only sections appear when corresponding analysis artifacts exist
- Missing optional analyses are surfaced explicitly and generation remains non-blocking

</success_criteria>

<output>
After completion, create `.planning/phases/08-interactive-tearsheet-reporting/08-03-SUMMARY.md`
</output>
