---
phase: 07-analytics-and-audit-layer
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified: [src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/analytics.py, tests/test_optimization_results.py, tests/test_optimization_analytics.py, tests/test_cli.py]
autonomous: true
requirements: [ANL-04, ANL-02]
user_setup: []
must_haves:
  truths:
    - "Optimization runs derive parameter sensitivity from completed trials only, without running new backtests."
    - "Optimization analytics reuse best-run trades for the same breakdown structure Phase 7 defines for backtests."
    - "Phase 6 research outputs remain intact while Phase 7 analytics are added alongside them."
  artifacts:
    - path: "src/mgc_bt/optimization/analytics.py"
      provides: "Parameter sensitivity and optimization analytics helpers"
      contains: "parameter_sensitivity.csv"
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Optimization analytics artifact writers"
      contains: "analytics/"
    - path: "tests/test_optimization_analytics.py"
      provides: "Parameter sensitivity regression coverage"
      contains: "sharpe_range_across_buckets"
  key_links:
    - from: "src/mgc_bt/optimization/results.py"
      to: "src/mgc_bt/optimization/analytics.py"
      via: "artifact layer writes optimization analytics bundle"
      pattern: "parameter_sensitivity|analytics"
    - from: "src/mgc_bt/optimization/study.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "best-run optimization outputs gain analytics attachment"
      pattern: "best_run|analytics"
---

<objective>
Implement the optimization-side analytics bundle: parameter sensitivity from existing Optuna results plus best-run performance breakdowns under `analytics/`.

Purpose: ANL-04 completes the Phase 7 analytics surface and makes optimization runs explainable without extra backtests.
Output: `analytics/parameter_sensitivity.csv`, optimization-side breakdown CSVs based on best-run trades, and regression coverage for the new artifact contract.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md
@.planning/phases/07-analytics-and-audit-layer/07-RESEARCH.md
@src/mgc_bt/optimization/results.py
@src/mgc_bt/optimization/study.py
@src/mgc_bt/optimization/stability.py
@src/mgc_bt/backtest/analytics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build parameter sensitivity analysis from ranked trial results only</name>
  <files>src/mgc_bt/optimization/analytics.py, src/mgc_bt/optimization/results.py</files>
  <read_first>src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/stability.py, .planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md, .planning/phases/07-analytics-and-audit-layer/07-RESEARCH.md</read_first>
  <action>Create `src/mgc_bt/optimization/analytics.py` with a parameter sensitivity entry point that consumes completed trial rows or the equivalent ranked-results dataset and never calls `run_backtest`. For each optimized parameter, bin values into `5` equal-width buckets, compute mean Sharpe per bucket, derive `sharpe_range_across_buckets` as the max-minus-min bucket mean, compute Pearson `correlation_with_objective`, and mark the top `3` parameters by Sharpe-range sensitivity as `most_sensitive = true`. Persist `analytics/parameter_sensitivity.csv` with the exact columns `parameter_name`, `correlation_with_objective`, `sharpe_range_across_buckets`, and `most_sensitive`.</action>
  <verify>uv run pytest tests/test_optimization_analytics.py -q -k "sensitivity"</verify>
  <acceptance_criteria>
    - `src/mgc_bt/optimization/analytics.py` never calls `run_backtest`
    - `analytics/parameter_sensitivity.csv` contains the exact four locked columns
    - sensitivity uses `5` equal-width buckets per parameter
    - `most_sensitive = true` is assigned to exactly the top `3` parameters by Sharpe range when at least three parameters exist
  </acceptance_criteria>
  <done>Optimization runs can quantify parameter sensitivity honestly from existing trial results only.</done>
</task>

<task type="auto">
  <name>Task 2: Attach optimization analytics using the best-run trade set</name>
  <files>src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/study.py, src/mgc_bt/optimization/analytics.py</files>
  <read_first>src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/study.py, src/mgc_bt/backtest/analytics.py, .planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md</read_first>
  <action>Extend the optimization artifact pipeline so every optimization run writes an `analytics/` subtree in the main run directory. Persist `analytics/parameter_sensitivity.csv` plus the same trade breakdown structure used by backtests, derived from the best-run trade set already exported by the optimization workflow. Keep all existing Phase 4 and Phase 6 outputs intact, and add the Phase 7 analytics bundle alongside them rather than nesting it inside `best_run/` or rewriting `stability/` outputs. Update manifest generation so optimization analytics files are included.</action>
  <verify>uv run pytest tests/test_optimization_results.py -q</verify>
  <acceptance_criteria>
    - optimization runs write `analytics/parameter_sensitivity.csv`
    - optimization runs write `analytics/breakdowns/` using the same CSV names as backtests
    - existing `walk_forward/`, `monte_carlo/`, and `stability/` outputs remain unchanged
    - `manifest.json` includes optimization analytics files
  </acceptance_criteria>
  <done>The optimization result folder gains a filesystem-first analytics bundle ready for Phase 8 tearsheets.</done>
</task>

<task type="auto">
  <name>Task 3: Add optimization analytics regression coverage and CLI contract checks</name>
  <files>tests/test_optimization_results.py, tests/test_optimization_analytics.py, tests/test_cli.py</files>
  <read_first>tests/test_optimization_results.py, tests/test_cli.py, src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/analytics.py</read_first>
  <action>Create `tests/test_optimization_analytics.py` to lock bucketed sensitivity math and Pearson correlation outputs on synthetic ranked-trial fixtures. Extend `tests/test_optimization_results.py` so a representative optimization run asserts `analytics/parameter_sensitivity.csv` and `analytics/breakdowns/*.csv` exist alongside the existing optimization artifacts. Extend CLI coverage only as needed to ensure Phase 7 optimization analytics do not remove or reorder the current summary lines unexpectedly.</action>
  <verify>uv run pytest tests/test_optimization_results.py tests/test_optimization_analytics.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - parameter sensitivity tests assert `sharpe_range_across_buckets` and `correlation_with_objective`
    - optimization artifact tests assert `analytics/parameter_sensitivity.csv` exists
    - optimization artifact tests assert breakdown CSVs exist under `analytics/breakdowns/`
    - CLI tests confirm existing optimization summary behavior remains additive
  </acceptance_criteria>
  <done>The optimization analytics contract is locked down by tests before Phase 8 depends on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_optimization_results.py tests/test_optimization_analytics.py tests/test_cli.py -q`
- [ ] A representative optimization run writes `analytics/parameter_sensitivity.csv`
- [ ] Optimization analytics coexist with `walk_forward/`, `monte_carlo/`, and `stability/` outputs
</verification>

<success_criteria>

- All tasks completed
- Optimization runs produce `parameter_sensitivity.csv` without triggering new backtests
- Best-run trades are available through the same breakdown contract used by backtests
- Existing optimization outputs remain additive and manifest-aware

</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-and-audit-layer/07-03-SUMMARY.md`
</output>
