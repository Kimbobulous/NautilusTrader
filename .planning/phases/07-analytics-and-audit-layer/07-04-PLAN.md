---
phase: 07-analytics-and-audit-layer
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified: [src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, src/mgc_bt/cli.py, tests/test_backtest_artifacts.py, tests/test_optimization_results.py, tests/test_cli.py, tests/test_backtest_analytics.py, tests/test_optimization_analytics.py]
autonomous: true
requirements: [ANL-01, ANL-02, ANL-03, ANL-04]
user_setup: []
must_haves:
  truths:
    - "Phase 7 analytics are fully additive to the existing run outputs."
    - "Core result bundles still save even if analytics generation fails."
    - "Phase 8 can consume all required Phase 7 analytics from the filesystem alone."
  artifacts:
    - path: "src/mgc_bt/backtest/artifacts.py"
      provides: "Final backtest analytics attachment and manifest behavior"
      contains: "manifest.json"
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Final optimization analytics attachment and manifest behavior"
      contains: "manifest.json"
    - path: "tests/test_cli.py"
      provides: "End-to-end additive contract coverage"
      contains: "analytics"
  key_links:
    - from: "src/mgc_bt/backtest/artifacts.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "shared analytics and manifest contract across run types"
      pattern: "analytics|manifest"
    - from: "tests/test_backtest_artifacts.py"
      to: "tests/test_optimization_results.py"
      via: "mirrored filesystem-first assertions"
      pattern: "manifest|analytics"
---

<objective>
Harden and verify the full Phase 7 analytics contract across backtest and optimization outputs, including non-blocking behavior and manifest completeness.

Purpose: This plan turns the three analytics feature slices into a stable, additive platform contract for future tearsheets and reuse work.
Output: final integration polish, consistent manifest coverage, warning behavior, and a regression surface that protects every Phase 7 artifact.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md
@.planning/phases/07-analytics-and-audit-layer/07-RESEARCH.md
@.planning/phases/07-analytics-and-audit-layer/07-VALIDATION.md
@src/mgc_bt/backtest/artifacts.py
@src/mgc_bt/optimization/results.py
@src/mgc_bt/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize manifest and filesystem contracts across backtest and optimization analytics</name>
  <files>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py</files>
  <read_first>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, .planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md</read_first>
  <action>Review the combined Phase 7 artifact writers and normalize the final contract so both backtest and optimization result folders expose analytics in a consistent filesystem-first shape. Ensure manifests include all generated analytics files and do not drop existing Phase 4 or Phase 6 files. Keep backtest analytics rooted under `results/backtests/<timestamp>/analytics/` and optimization analytics under `results/optimization/<timestamp>/analytics/`, with no extra command or alternate storage root.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py tests/test_optimization_results.py -q</verify>
  <acceptance_criteria>
    - backtest and optimization manifests both include analytics files
    - analytics remain under the locked `analytics/` subdirectory for each run type
    - no existing Phase 4 or Phase 6 artifact directories are removed or relocated
  </acceptance_criteria>
  <done>The Phase 7 filesystem contract is stable and mirrored across run types.</done>
</task>

<task type="auto">
  <name>Task 2: Lock non-blocking analytics warnings and additive CLI behavior</name>
  <files>src/mgc_bt/cli.py, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, tests/test_cli.py</files>
  <read_first>src/mgc_bt/cli.py, tests/test_cli.py, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, .planning/phases/07-analytics-and-audit-layer/07-RESEARCH.md</read_first>
  <action>Standardize how Phase 7 analytics failures are surfaced to the user: warn clearly, preserve the main result, and keep the existing backtest and optimize summary output additive rather than rewritten. If useful, add concise run-summary references to the new analytics directory, but do not remove or reorder established lines that current tests and user workflows already depend on. Add regression tests that simulate analytics write failures and prove the commands still exit successfully after the core artifacts are saved.</action>
  <verify>uv run pytest tests/test_cli.py tests/test_backtest_artifacts.py tests/test_optimization_results.py -q</verify>
  <acceptance_criteria>
    - analytics failure warnings are explicit and non-fatal
    - core run commands still succeed when analytics attachment is forced to fail
    - CLI output stays additive to the existing contract
  </acceptance_criteria>
  <done>The user-facing Phase 7 contract is resilient and does not destabilize existing workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Phase 7 regression surface and protect Phase 8 readiness</name>
  <files>tests/test_backtest_artifacts.py, tests/test_backtest_analytics.py, tests/test_optimization_results.py, tests/test_optimization_analytics.py, tests/test_cli.py</files>
  <read_first>tests/test_backtest_artifacts.py, tests/test_backtest_analytics.py, tests/test_optimization_results.py, tests/test_optimization_analytics.py, tests/test_cli.py, .planning/phases/07-analytics-and-audit-layer/07-VALIDATION.md</read_first>
  <action>Finish Phase 7 by adding or refining whatever integration assertions are still missing so the analytics bundle is safe for Phase 8 tearsheet consumption. The final regression suite should prove: backtests write `analytics/audit_log.csv`, breakdown CSVs, drawdown episodes, and underwater curve; optimizations write `analytics/parameter_sensitivity.csv` and best-run breakdown analytics; manifests include analytics files; analytics failures are non-blocking; and existing Phase 6 outputs still coexist cleanly. Use the bounded Phase 7 regression command from the validation strategy as the final automated gate.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py tests/test_backtest_analytics.py tests/test_optimization_results.py tests/test_optimization_analytics.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - regression tests cover backtest analytics existence and manifest inclusion
    - regression tests cover optimization analytics existence and coexistence with Phase 6 outputs
    - regression tests cover non-blocking analytics failure behavior
    - the bounded Phase 7 regression command exits successfully
  </acceptance_criteria>
  <done>Phase 7 is protected end to end and ready for Phase 8 to consume its analytics artifacts directly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_backtest_artifacts.py tests/test_backtest_analytics.py tests/test_optimization_results.py tests/test_optimization_analytics.py tests/test_cli.py -q`
- [ ] Backtest and optimization manifests both list analytics files
- [ ] Phase 6 outputs still coexist with the new Phase 7 analytics bundle
</verification>

<success_criteria>

- All tasks completed
- Phase 7 analytics are additive, non-blocking, and manifest-aware
- Backtest and optimization analytics folders are ready for Phase 8 filesystem-first tearsheet generation
- The regression suite protects the full Phase 7 artifact contract

</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-and-audit-layer/07-04-SUMMARY.md`
</output>
