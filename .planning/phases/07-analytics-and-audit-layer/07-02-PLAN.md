---
phase: 07-analytics-and-audit-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/mgc_bt/backtest/results.py, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/backtest/analytics.py, tests/test_backtest_artifacts.py, tests/test_backtest_analytics.py, tests/test_cli.py]
autonomous: true
requirements: [ANL-02, ANL-03]
user_setup: []
must_haves:
  truths:
    - "Backtest runs automatically produce breakdown and drawdown analytics under `analytics/`."
    - "Breakdown totals reconcile with the canonical trade log and summary metrics."
    - "Analytics failures warn and continue instead of blocking the core backtest bundle."
  artifacts:
    - path: "src/mgc_bt/backtest/analytics.py"
      provides: "Breakdown and drawdown analytics generators"
      contains: "underwater_curve.csv"
    - path: "src/mgc_bt/backtest/artifacts.py"
      provides: "Analytics attachment and manifest updates"
      contains: "analytics/"
    - path: "tests/test_backtest_analytics.py"
      provides: "Backtest analytics contract coverage"
      contains: "drawdown_episodes.csv"
  key_links:
    - from: "src/mgc_bt/backtest/results.py"
      to: "src/mgc_bt/backtest/analytics.py"
      via: "equity curve and trade log feed analytics derivation"
      pattern: "equity_curve|trade_log"
    - from: "src/mgc_bt/backtest/artifacts.py"
      to: "src/mgc_bt/backtest/analytics.py"
      via: "artifact pipeline attaches analytics and updates manifest"
      pattern: "manifest|analytics"
---

<objective>
Generate the locked Phase 7 backtest analytics package: performance breakdowns, drawdown episodes, underwater series, and manifest-aware persistence.

Purpose: ANL-02 and ANL-03 turn raw backtest outputs into interpretable performance diagnostics.
Output: `analytics/breakdowns/*.csv`, `drawdown_episodes.csv`, `underwater_curve.csv`, summary drawdown enrichments, and non-blocking artifact integration.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md
@.planning/phases/07-analytics-and-audit-layer/07-RESEARCH.md
@.planning/phases/07-analytics-and-audit-layer/07-VALIDATION.md
@src/mgc_bt/backtest/results.py
@src/mgc_bt/backtest/artifacts.py
@src/mgc_bt/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build trade breakdown analytics from the canonical trade set</name>
  <files>src/mgc_bt/backtest/analytics.py, src/mgc_bt/backtest/results.py</files>
  <read_first>src/mgc_bt/backtest/results.py, .planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md, .planning/phases/07-analytics-and-audit-layer/07-RESEARCH.md</read_first>
  <action>Create breakdown generators that derive all Phase 7 trade-performance breakdowns from one normalized trade analytics dataset built from the canonical `trade_log` plus the Phase 7 enrichments. Write exactly these CSVs under `analytics/breakdowns/`: `by_session.csv`, `by_volatility_regime.csv`, `by_month.csv`, `by_year.csv`, `by_day_of_week.csv`, and `by_hour.csv`. Use the locked session buckets (`rth`, `asian`, `london`, `globex_overnight`) in UTC, use Adaptive SuperTrend cluster at entry for volatility regime, and compute for each bucket: `trade_count`, `win_rate`, `total_pnl`, `average_pnl_per_trade`, `sharpe_ratio`, and `max_drawdown`.</action>
  <verify>uv run pytest tests/test_backtest_analytics.py -q -k "breakdown"</verify>
  <acceptance_criteria>
    - breakdown writers create the six exact CSV filenames under `analytics/breakdowns/`
    - session bucket labels are `rth`, `asian`, `london`, and `globex_overnight`
    - volatility regime uses entry cluster values `1`, `2`, and `3`
    - each breakdown row includes `trade_count`, `win_rate`, `total_pnl`, `average_pnl_per_trade`, `sharpe_ratio`, and `max_drawdown`
  </acceptance_criteria>
  <done>Backtest runs can be segmented by session, regime, and time slices from one canonical trade dataset.</done>
</task>

<task type="auto">
  <name>Task 2: Compute drawdown episodes, recovery metrics, and underwater equity series</name>
  <files>src/mgc_bt/backtest/results.py, src/mgc_bt/backtest/analytics.py</files>
  <read_first>src/mgc_bt/backtest/results.py, .planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md, .planning/phases/07-analytics-and-audit-layer/07-RESEARCH.md</read_first>
  <action>Add reusable drawdown helpers that operate on the canonical equity curve and persist the locked Phase 7 outputs: `analytics/drawdown_episodes.csv` and `analytics/underwater_curve.csv`. Each drawdown episode must include episode timing, duration in bars and days, drawdown depth in percent and dollars, recovery timing and duration, and `recovered`. Extend the backtest summary payload additively to include the locked summary drawdown metrics: max/avg drawdown depth in dollars and percent, max/avg drawdown duration, max/avg recovery duration, total drawdown episodes, and percent time in drawdown.</action>
  <verify>uv run pytest tests/test_backtest_analytics.py -q -k "drawdown"</verify>
  <acceptance_criteria>
    - `analytics/drawdown_episodes.csv` and `analytics/underwater_curve.csv` are generated
    - drawdown episodes include `recovery_duration_days` and `recovered`
    - `summary.json` gains additive drawdown summary metrics without dropping existing keys
    - underwater rows are stored as a time series derived from equity versus running peak
  </acceptance_criteria>
  <done>The project persists drawdown depth, duration, recovery, and underwater information from the canonical equity curve.</done>
</task>

<task type="auto">
  <name>Task 3: Attach backtest analytics non-blockingly and update manifests/tests</name>
  <files>src/mgc_bt/backtest/artifacts.py, tests/test_backtest_artifacts.py, tests/test_backtest_analytics.py, tests/test_cli.py</files>
  <read_first>src/mgc_bt/backtest/artifacts.py, tests/test_backtest_artifacts.py, tests/test_cli.py, .planning/phases/07-analytics-and-audit-layer/07-VALIDATION.md</read_first>
  <action>Wire backtest analytics generation into `src/mgc_bt/backtest/artifacts.py` so the canonical bundle (`summary.json`, `trades.csv`, `run_config.toml`, `equity_curve.png`) is written first, analytics are generated second, and the manifest is updated last. If analytics generation raises an exception, print a clear warning and continue returning the core artifact paths instead of failing the backtest command. Extend manifest generation so every Phase 7 analytics file is listed. Add tests for file existence, manifest inclusion, and non-blocking failure behavior.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py tests/test_backtest_analytics.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - core backtest artifacts are written before analytics attachment
    - analytics failures warn and continue instead of aborting the run
    - `manifest.json` lists analytics files under `analytics/`
    - tests assert both existence and non-blocking behavior
  </acceptance_criteria>
  <done>Backtest analytics are attached safely and the contract is protected by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_backtest_artifacts.py tests/test_backtest_analytics.py tests/test_cli.py -q`
- [ ] A representative backtest run writes `analytics/breakdowns/`, `analytics/drawdown_episodes.csv`, and `analytics/underwater_curve.csv`
- [ ] Simulated analytics failures still return the core backtest result bundle
</verification>

<success_criteria>

- All tasks completed
- Backtest runs automatically generate the locked breakdown and drawdown analytics artifacts
- `summary.json` includes additive Phase 7 drawdown summary metrics
- Analytics remain non-blocking and manifest-aware

</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-and-audit-layer/07-02-SUMMARY.md`
</output>
