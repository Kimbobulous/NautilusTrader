---
phase: 07-analytics-and-audit-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/state.py, src/mgc_bt/backtest/results.py, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/backtest/analytics.py, tests/test_strategy_logic.py, tests/test_backtest_analytics.py]
autonomous: true
requirements: [ANL-01]
user_setup: []
must_haves:
  truths:
    - "Backtests produce a full signal audit, not just executed-trade rows."
    - "Every `PULLBACK_ARMED` bar can be explained after the run from persisted analytics artifacts."
    - "Trade audit enrichment is captured without changing strategy entry or exit behavior."
  artifacts:
    - path: "src/mgc_bt/backtest/analytics.py"
      provides: "Audit-row models and analytics CSV writers"
      contains: "audit_log.csv"
    - path: "src/mgc_bt/backtest/strategy.py"
      provides: "Signal-state audit capture at the strategy/runtime boundary"
      contains: "PULLBACK_ARMED"
    - path: "tests/test_backtest_analytics.py"
      provides: "Audit-log regression coverage"
      contains: "entry_rejected_reason"
  key_links:
    - from: "src/mgc_bt/backtest/strategy.py"
      to: "src/mgc_bt/backtest/analytics.py"
      via: "runtime emits audit records for persistence"
      pattern: "audit|entry_rejected_reason|optional_confirmation_count"
    - from: "src/mgc_bt/backtest/results.py"
      to: "src/mgc_bt/backtest/analytics.py"
      via: "trade log enrichments become analytics rows"
      pattern: "bars_held|max_favorable_excursion|max_adverse_excursion"
---

<objective>
Implement the Phase 7 audit foundation so every considered setup and every executed trade can be explained from a saved analytics artifact.

Purpose: ANL-01 is the explainability backbone for all later analytics and tearsheet work.
Output: Strategy/runtime audit capture, canonical audit row models, streamed `analytics/audit_log.csv`, and focused tests that lock the audit contract.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md
@.planning/phases/07-analytics-and-audit-layer/07-RESEARCH.md
@.planning/phases/07-analytics-and-audit-layer/07-VALIDATION.md
@src/mgc_bt/backtest/strategy.py
@src/mgc_bt/backtest/state.py
@src/mgc_bt/backtest/results.py
@src/mgc_bt/backtest/artifacts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit capture models for armed-state bars and executed trades</name>
  <files>src/mgc_bt/backtest/state.py, src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/analytics.py</files>
  <read_first>src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/state.py, .planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md, .planning/phases/07-analytics-and-audit-layer/07-RESEARCH.md</read_first>
  <action>Create `src/mgc_bt/backtest/analytics.py` with explicit audit row models and a streaming CSV writer for `analytics/audit_log.csv`. Extend the strategy runtime state so every bar evaluated while `StrategyPhase.PULLBACK_ARMED` can produce an audit record containing the locked Phase 7 fields: timestamp, instrument ID, OHLCV, SuperTrend direction/value, VWAP value and price-vs-VWAP state, WaveTrend z-score/divergence, delta values and pass flags, volume average and pass flag, absorption flag, candle formation, optional confirmation count, `entry_fired`, and `entry_rejected_reason` when the entry does not fire. Keep the strategy behavior unchanged: this task only captures state and reasons, it does not change trigger thresholds or order submission logic.</action>
  <verify>uv run pytest tests/test_strategy_logic.py -q</verify>
  <acceptance_criteria>
    - `src/mgc_bt/backtest/analytics.py` contains `audit_log.csv`
    - `src/mgc_bt/backtest/strategy.py` emits audit records while the strategy is in `PULLBACK_ARMED`
    - audit rows capture `optional_confirmation_count`, `entry_fired`, and `entry_rejected_reason`
    - no trigger thresholds or order-submission branches are changed as part of audit capture
  </acceptance_criteria>
  <done>The strategy runtime can explain each considered setup without altering trading behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich executed trades with lifecycle metadata required by Phase 7</name>
  <files>src/mgc_bt/backtest/results.py, src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/analytics.py</files>
  <read_first>src/mgc_bt/backtest/results.py, src/mgc_bt/backtest/strategy.py, src/mgc_bt/backtest/analytics.py, .planning/phases/07-analytics-and-audit-layer/07-CONTEXT.md</read_first>
  <action>Extend the canonical backtest result path so executed-trade analytics rows include the locked executed-trade fields: entry timestamp, exit timestamp, entry price, exit price, direction, PnL, PnL dollars, exit reason, max favorable excursion, max adverse excursion, bars held, volatility cluster at entry, and session at entry. Use the strategy/runtime side to capture trade-only context such as exit reason and volatility cluster at entry, then merge those enrichments into the canonical result payload in `src/mgc_bt/backtest/results.py` without replacing the existing `trade_log` contract. Preserve the existing summary keys and `trades.csv` behavior; add Phase 7 enrichments additively.</action>
  <verify>uv run pytest tests/test_backtest_runner.py tests/test_strategy_logic.py -q</verify>
  <acceptance_criteria>
    - executed-trade analytics rows include `exit_reason`, `max_favorable_excursion`, `max_adverse_excursion`, `bars_held`, `volatility_cluster_at_entry`, and `session_at_entry`
    - `src/mgc_bt/backtest/results.py` keeps the current `trade_log` contract additive rather than replacing it
    - trade direction is available as long/short semantics in the analytics output
  </acceptance_criteria>
  <done>Executed trades carry enough context for Phase 7 analytics and future tearsheet use.</done>
</task>

<task type="auto">
  <name>Task 3: Stream the audit artifact and lock its schema with tests</name>
  <files>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/backtest/analytics.py, tests/test_backtest_analytics.py, tests/test_strategy_logic.py</files>
  <read_first>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/backtest/analytics.py, tests/test_strategy_logic.py, .planning/phases/07-analytics-and-audit-layer/07-VALIDATION.md</read_first>
  <action>Attach audit-log writing to the backtest artifact pipeline by creating `analytics/audit_log.csv` inside the run folder after the core bundle is written. Use a streaming writer or chunked row emission pattern rather than creating one giant in-memory dataframe for the full audit file. Add tests that assert the file exists, contains the locked key columns, records both executed and rejected setups, and does not require changes to the existing `summary.json` or `trades.csv` filenames.</action>
  <verify>uv run pytest tests/test_backtest_analytics.py tests/test_backtest_artifacts.py -q</verify>
  <acceptance_criteria>
    - backtest runs write `analytics/audit_log.csv`
    - audit writing does not rely on `pandas.to_csv` over one giant dataframe
    - tests assert rejected setups remain present in the audit file
    - existing core backtest artifact filenames remain unchanged
  </acceptance_criteria>
  <done>The audit log is persisted efficiently and protected by regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_strategy_logic.py tests/test_backtest_analytics.py tests/test_backtest_artifacts.py -q`
- [ ] A representative backtest run writes `analytics/audit_log.csv`
- [ ] The audit artifact includes both rejected setup rows and executed-trade rows
</verification>

<success_criteria>

- All tasks completed
- `analytics/audit_log.csv` exists for backtest runs
- The audit log explains each `PULLBACK_ARMED` decision without changing strategy behavior
- Executed trades carry the required lifecycle enrichments for later analytics and reporting

</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-and-audit-layer/07-01-SUMMARY.md`
</output>
