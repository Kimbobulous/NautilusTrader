---
phase: 05-validation-and-hardening
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/mgc_bt/cli.py
  - src/mgc_bt/backtest/artifacts.py
  - src/mgc_bt/optimization/results.py
  - src/mgc_bt/optimization/study.py
  - tests/test_backtest_artifacts.py
  - tests/test_optimization_results.py
  - tests/test_cli.py
  - USAGE.md
autonomous: true
requirements: [CLI-01, BT-06, OPT-05]
user_setup: []
must_haves:
  truths:
    - "Result directories gain manifest files and safer overwrite behavior without changing the established artifact layout."
    - "Refreshing `latest/` becomes an explicit operator choice through `--force` for backtest and optimize."
    - "Phase 5 closes with practical operator guidance rather than architectural churn."
  artifacts:
    - path: "src/mgc_bt/backtest/artifacts.py"
      provides: "Safer backtest run-directory creation, manifest writing, and `latest/` overwrite controls"
      min_lines: 140
    - path: "src/mgc_bt/optimization/results.py"
      provides: "Optimization manifest writing, collision-safe directories, and safer `latest/` handling"
      min_lines: 180
    - path: "USAGE.md"
      provides: "Operator guide for ingest, backtest, optimize, health, resume, reruns, and common fixes"
      min_lines: 80
  key_links:
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/backtest/artifacts.py"
      via: "backtest force-controlled latest refresh"
      pattern: "force|latest"
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "optimize force-controlled latest refresh"
      pattern: "force|latest"
    - from: "USAGE.md"
      to: "src/mgc_bt/cli.py"
      via: "documented commands and options"
      pattern: "health|backtest|optimize|ingest"
---

<objective>
Finish Phase 5 by making persisted outputs safer to operate and the local workflow easier to run repeatedly.

Purpose: Add manifests, collision-safe output directories, explicit `--force` behavior for `latest/`, and practical operator documentation that matches the hardened CLI.
Output: Safer persistence helpers, updated CLI flags/help text, and a root `USAGE.md`.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validation-and-hardening/05-CONTEXT.md
@.planning/phases/05-validation-and-hardening/05-RESEARCH.md
@src/mgc_bt/backtest/artifacts.py
@src/mgc_bt/optimization/results.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden result-directory creation, manifests, and `latest/` overwrite behavior</name>
  <files>src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, src/mgc_bt/optimization/study.py, tests/test_backtest_artifacts.py, tests/test_optimization_results.py, tests/test_cli.py</files>
  <read_first>.planning/phases/05-validation-and-hardening/05-CONTEXT.md, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, tests/test_backtest_artifacts.py, tests/test_optimization_results.py</read_first>
  <action>Extend the existing backtest and optimization persistence helpers so each result directory writes a `manifest.json` describing what files were created and when. If the canonical timestamped run directory already exists, append a suffix instead of overwriting it. Add explicit `--force` support to backtest and optimize so refreshing `latest/` is opt-in rather than automatic. Preserve the existing bundle layout and file names so Phase 2 and Phase 4 contracts remain stable.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py tests/test_optimization_results.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - every persisted backtest and optimization run includes `manifest.json`
    - timestamp collisions create suffixed directories instead of overwriting
    - `latest/` refresh for backtest and optimize requires explicit `--force`
  </acceptance_criteria>
  <done>Result persistence is safer for repeated local operation without changing the artifact contract.</done>
</task>

<task type="auto">
  <name>Task 2: Improve help text and add a practical root usage guide</name>
  <files>src/mgc_bt/cli.py, USAGE.md, tests/test_cli.py</files>
  <read_first>src/mgc_bt/cli.py, .planning/phases/05-validation-and-hardening/05-DISCUSSION-LOG.md, AGENTS.md</read_first>
  <action>Improve command and option help text so `ingest`, `backtest`, `optimize`, and `health` are self-explanatory from `--help`. Add a root `USAGE.md` documenting exact commands, expected output structure, optimization resume steps, how to rerun best params manually, and common actionable error messages with fixes. Keep the documentation practical and aligned with the actual CLI behavior after Phase 5 hardening.</action>
  <verify>uv run pytest tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - CLI help text explains the command options without requiring source reading
    - `USAGE.md` covers ingest, backtest, optimize, health, resume, reruns, outputs, and common fixes
    - docs do not introduce new features beyond the hardened existing workflow
  </acceptance_criteria>
  <done>The repo includes operator-facing guidance that matches the hardened CLI and persisted output behavior.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- CLI `--force` flags into file-overwrite behavior
- Generated result artifacts into manifest integrity
- Documentation text into operator expectations
</trust_boundaries>
<threats>
- threat_id: T-05-05
  category: Integrity
  component: result persistence
  disposition: mitigate
  mitigation_plan: Prevent accidental overwrite through collision-safe directories, explicit `--force`, and manifest recording.
- threat_id: T-05-06
  category: Reliability
  component: operator workflow
  disposition: mitigate
  mitigation_plan: Align CLI help and `USAGE.md` with the actual hardened command behavior so users can recover from common failures quickly.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_backtest_artifacts.py tests/test_optimization_results.py tests/test_cli.py -q` passes
- [ ] persisted backtest and optimization bundles include `manifest.json`
- [ ] `latest/` refresh behavior is gated by `--force`
- [ ] `USAGE.md` matches the current CLI commands and output layout
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No warnings or failures introduced
- The local workflow is safer to repeat and better documented for a new operator

</success_criteria>

<output>
After completion, create `.planning/phases/05-validation-and-hardening/05-03-SUMMARY.md`
</output>
