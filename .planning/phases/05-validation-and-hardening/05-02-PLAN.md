---
phase: 05-validation-and-hardening
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/mgc_bt/cli.py
  - src/mgc_bt/config.py
  - src/mgc_bt/validation/__init__.py
  - src/mgc_bt/validation/preflight.py
  - src/mgc_bt/validation/health.py
  - src/mgc_bt/ingest/service.py
  - src/mgc_bt/backtest/runner.py
  - src/mgc_bt/optimization/study.py
  - tests/test_cli.py
  - tests/test_config.py
autonomous: true
requirements: [CLI-01, DATA-06, BT-06, OPT-05]
user_setup: []
must_haves:
  truths:
    - "Shared preflight validation powers both the command handlers and the `health` command."
    - "Integrity issues fail fast with actionable messages; convenience issues become warnings."
    - "Phase 5 hardens the existing ingest/backtest/optimize workflow instead of introducing a second execution path."
  artifacts:
    - path: "src/mgc_bt/validation/preflight.py"
      provides: "Reusable ingest, backtest, and optimize readiness checks with actionable findings"
      min_lines: 160
    - path: "src/mgc_bt/validation/health.py"
      provides: "Aggregated health-report rendering built on shared preflight results"
      min_lines: 80
    - path: "src/mgc_bt/cli.py"
      provides: "Health command, preflight invocation, and clear failure routing"
      contains: "health"
  key_links:
    - from: "src/mgc_bt/cli.py"
      to: "src/mgc_bt/validation/preflight.py"
      via: "command readiness checks before execution"
      pattern: "preflight|health"
    - from: "src/mgc_bt/validation/health.py"
      to: "src/mgc_bt/validation/preflight.py"
      via: "shared readiness aggregation"
      pattern: "ingest|backtest|optimize"
    - from: "src/mgc_bt/optimization/study.py"
      to: "src/mgc_bt/validation/preflight.py"
      via: "resume and storage validation"
      pattern: "resume|study|storage"
---

<objective>
Implement the shared readiness and operator-safety layer for the local workflow.

Purpose: Add strong preflight checks, clearer actionable failures, and a unified `health` command without changing the underlying trading or optimization feature set.
Output: Shared preflight modules, `health` CLI command, and updated command handlers that fail or warn according to the Phase 5 contract.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validation-and-hardening/05-CONTEXT.md
@.planning/phases/05-validation-and-hardening/05-RESEARCH.md
@.planning/phases/01-catalog-foundation/01-CONTEXT.md
@.planning/phases/04-optimization-workflow/04-CONTEXT.md
@nt_docs/concepts/backtesting.md
@nt_docs/api_reference/backtest.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared preflight checks for ingest, backtest, and optimize</name>
  <files>src/mgc_bt/validation/__init__.py, src/mgc_bt/validation/preflight.py, src/mgc_bt/config.py, src/mgc_bt/ingest/service.py, src/mgc_bt/backtest/runner.py, src/mgc_bt/optimization/study.py</files>
  <read_first>.planning/phases/05-validation-and-hardening/05-CONTEXT.md, src/mgc_bt/ingest/service.py, src/mgc_bt/backtest/runner.py, src/mgc_bt/optimization/study.py, src/mgc_bt/config.py</read_first>
  <action>Introduce a shared validation surface that performs the locked preflight checks for `ingest`, `backtest`, and `optimize`. Fail hard on integrity issues such as missing data roots, missing catalog contents, invalid date coverage, invalid instrument IDs, overlapping optimization windows, future holdout end dates, unwritable storage, and nonexistent resume targets. Emit warnings instead of failures for convenience issues such as preexisting catalog overlap or non-fatal continuity concerns. Preserve the current command architecture by invoking these checks before the existing workflow functions execute rather than moving execution logic into the validator.</action>
  <verify>uv run pytest tests/test_config.py tests/test_cli.py -q -k "config or missing or resume"</verify>
  <acceptance_criteria>
    - shared preflight helpers exist for ingest, backtest, and optimize
    - `--resume` validates that the target study exists before optimization starts
    - failure messages tell the user what is wrong and how to fix it without exposing a traceback
  </acceptance_criteria>
  <done>The project has a reusable readiness layer that enforces the Phase 5 fail/warn contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add the `health` command and wire CLI execution through shared readiness checks</name>
  <files>src/mgc_bt/cli.py, src/mgc_bt/validation/health.py, tests/test_cli.py</files>
  <read_first>src/mgc_bt/cli.py, .planning/phases/05-validation-and-hardening/05-RESEARCH.md, tests/test_cli.py</read_first>
  <action>Add `python -m mgc_bt health` as a read-only operator command that runs the combined ingest/backtest/optimize preflight checks and prints a summary of what is ready, what is missing, and what needs attention. Update the existing CLI command handlers to call shared readiness checks before executing their workflows. Keep `health` honest by making it consume the same validation functions as the real commands rather than a separate approximation.</action>
  <verify>uv run pytest tests/test_cli.py -q -k "health or optimize or backtest or ingest"</verify>
  <acceptance_criteria>
    - `mgc_bt health` is a registered CLI command
    - `health` output distinguishes ready, missing, and attention-needed states
    - `ingest`, `backtest`, and `optimize` route through the shared readiness layer before execution
  </acceptance_criteria>
  <done>The CLI exposes one unified readiness surface and uses it consistently across commands.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Local config and filesystem state into preflight classification
- Catalog metadata and storage state into readiness reporting
- CLI flags such as `--resume` into workflow gating
</trust_boundaries>
<threats>
- threat_id: T-05-03
  category: Integrity
  component: command readiness
  disposition: mitigate
  mitigation_plan: Fail fast before execution on missing catalog, bad windows, bad instrument IDs, and invalid resume targets.
- threat_id: T-05-04
  category: Reliability
  component: health reporting
  disposition: mitigate
  mitigation_plan: Build `health` directly on the same shared preflight helpers used by command execution so summaries cannot drift from real behavior.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_config.py tests/test_cli.py -q` passes
- [ ] `mgc_bt health` is covered by CLI tests
- [ ] `optimize --resume` fails cleanly for missing studies
- [ ] Shared preflight logic is reused by both `health` and command execution
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No warnings or failures introduced
- The CLI fails early on integrity issues and exposes a unified readiness check for local setup

</success_criteria>

<output>
After completion, create `.planning/phases/05-validation-and-hardening/05-02-SUMMARY.md`
</output>
