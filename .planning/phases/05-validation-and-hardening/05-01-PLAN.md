---
phase: 05-validation-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_config.py
  - tests/test_cli.py
  - tests/test_optimization_results.py
  - tests/test_backtest_artifacts.py
  - tests/conftest.py
autonomous: true
requirements: [CLI-01, DATA-06, BT-06, OPT-05]
user_setup: []
must_haves:
  truths:
    - "Phase 5 starts by locking regression coverage around fragile non-strategy workflow edges rather than adding features."
    - "Regression tests must cover malformed config, missing catalog, nonexistent optimization resume targets, and output integrity expectations."
    - "The new hardening surfaces should be protected before later plans refactor command validation and result persistence."
  artifacts:
    - path: "tests/test_cli.py"
      provides: "CLI-facing regression coverage for health, preflight failures, and operator guidance"
      min_lines: 80
    - path: "tests/test_optimization_results.py"
      provides: "Optimization result-integrity coverage including ranked CSV shape and persisted files"
      min_lines: 120
    - path: "tests/conftest.py"
      provides: "Reusable fixtures/helpers for Phase 5 workflow-hardening tests"
      min_lines: 20
  key_links:
    - from: "tests/test_cli.py"
      to: "src/mgc_bt/cli.py"
      via: "user-facing command failures and help output"
      pattern: "health|resume|config|force"
    - from: "tests/test_optimization_results.py"
      to: "src/mgc_bt/optimization/results.py"
      via: "persisted optimization output integrity"
      pattern: "ranked_results|manifest|latest"
---

<objective>
Build the Phase 5 regression safety net around the most failure-prone workflow edges before hardening implementation changes land.

Purpose: Ensure later Phase 5 work is constrained by tests covering user-facing failure modes, output integrity, and resume behavior rather than relying on ad hoc manual checking.
Output: New or expanded targeted tests and reusable fixtures for malformed config, missing catalog, bad resume, result bundle presence, and ranked CSV integrity.
</objective>

<execution_context>
@C:/dev/nautilustrader/.codex/get-shit-done/workflows/execute-plan.md
@C:/dev/nautilustrader/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-validation-and-hardening/05-CONTEXT.md
@.planning/phases/05-validation-and-hardening/05-RESEARCH.md
@.planning/phases/04-optimization-workflow/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand regression tests for common user mistakes and config failures</name>
  <files>tests/test_config.py, tests/test_cli.py, tests/conftest.py</files>
  <read_first>.planning/phases/05-validation-and-hardening/05-CONTEXT.md, src/mgc_bt/cli.py, src/mgc_bt/config.py, tests/test_config.py, tests/test_cli.py</read_first>
  <action>Add focused tests covering malformed or structurally invalid settings files, missing catalog failures, and resume attempts against missing studies. Keep the assertions user-facing: check for clear actionable error text rather than implementation-specific exception internals. Introduce shared fixtures or helpers in `tests/conftest.py` only where they reduce duplication across Phase 5 tests.</action>
  <verify>uv run pytest tests/test_config.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - `tests/test_config.py` covers malformed or invalid optimization/backtest settings with user-facing failure expectations
    - `tests/test_cli.py` covers missing catalog and nonexistent `--resume` study behavior
    - shared test helpers avoid repeated ad hoc temporary config setup across the new Phase 5 tests
  </acceptance_criteria>
  <done>Fragile operator-error scenarios are captured in fast automated tests before command hardening changes begin.</done>
</task>

<task type="auto">
  <name>Task 2: Lock result-output integrity expectations for backtest and optimization artifacts</name>
  <files>tests/test_backtest_artifacts.py, tests/test_optimization_results.py, tests/conftest.py</files>
  <read_first>.planning/phases/05-validation-and-hardening/05-RESEARCH.md, src/mgc_bt/backtest/artifacts.py, src/mgc_bt/optimization/results.py, tests/test_backtest_artifacts.py, tests/test_optimization_results.py</read_first>
  <action>Add regression tests that verify result directories contain the expected files after persistence, that optimization ranked results include the required columns, and that ranked CSV output is not empty when complete trial rows are present. Shape the tests so later manifest and safer-overwrite changes can extend the same expectations without replacing the tests entirely.</action>
  <verify>uv run pytest tests/test_backtest_artifacts.py tests/test_optimization_results.py -q</verify>
  <acceptance_criteria>
    - backtest artifact tests assert expected bundle contents
    - optimization result tests assert `ranked_results.csv` columns exactly match the required contract
    - optimization result tests assert non-empty ranked CSV output when completed rows exist
  </acceptance_criteria>
  <done>The persisted output contract is protected by automated regression coverage instead of assumptions.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
- Temporary test configs and fixtures into production config loading
- Simulated CLI invocations into user-facing error handling assertions
- Generated persistence outputs into integrity-contract tests
</trust_boundaries>
<threats>
- threat_id: T-05-01
  category: Integrity
  component: regression coverage
  disposition: mitigate
  mitigation_plan: Add tests for operator-facing failure modes before changing validation and persistence behavior.
- threat_id: T-05-02
  category: Reliability
  component: persisted outputs
  disposition: mitigate
  mitigation_plan: Lock expected files and CSV-column contracts in automated tests so later hardening cannot silently drift the artifact bundle.
</threats>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_config.py tests/test_cli.py -q` passes
- [ ] `uv run pytest tests/test_backtest_artifacts.py tests/test_optimization_results.py -q` passes
- [ ] Missing-catalog and bad-resume behavior are covered by tests
- [ ] Ranked-results CSV columns and non-empty output are covered by tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No warnings or failures introduced
- Phase 5 has a targeted automated regression baseline for fragile non-strategy workflow edges

</success_criteria>

<output>
After completion, create `.planning/phases/05-validation-and-hardening/05-01-SUMMARY.md`
</output>
